# Independent real-memory result audit

PASS — independently recomputed the reported required-group, complete-query, note, exact-span, byte-union and latency summaries from frozen IDs and byte offsets. No scorer functions were imported. Tokenization reused only the pinned offline cl100k encoding. No labels, policies, corpus or official results changed.

- 192 distinct cases = 32 exact prompts × 3 arms × 2 budgets; 576 measured timings, three per case. Every packet retokenizes to its recorded count and fits 128/256 tokens. Candidate lists contain at most 20 IDs; selected IDs are candidates, packet IDs are selected. All referenced source chunks match exact UTF-8 body bytes and rendered claims. Input/gold hashes match provenance.
- Ordered candidate lists are identical across all three arms in all 64 prompt/budget comparisons. They reach a required source note for 10/13 positives, mean required-group coverage 47.4359%, and every required group for 3/13. This is the observed candidate ceiling, not a causal proof that alternative graph retrieval cannot help.
- Raw BM25 preserves all candidates at selection; packet allocation lowers note recall to 5/13 at 128 tokens and 7/13 at 256. Mean required-group coverage is 21.7949% / 28.2051%; complete required evidence is 1/13 at both budgets. Byte-union coverage is 18.0512% / 19.8112%. The packet stage loses useful evidence even when retrieval finds its note.
- Both frozen selectors discard all candidates for every one of 32 prompts: 0/13 positive coverage at either budget. Therefore the primary observed failure is selection/applicability, before packing. Structured parsing yields 62 unsupported request clauses across 32 queries (not 62 queries), zero ready requests, zero graph seeds/probes/visited bindings/examined assertions/emitted semantic facts.
- All 3 negative cases get empty packets from both selectors and nonempty packets from raw BM25. Because both selectors also return empty for every positive, 100% abstention is not evidence of useful discrimination. Fourteen ambiguous and two unavailable prompts are excluded from abstention and positive-coverage denominators.
- Six positive supporting notes have recorded creation times before their corresponding prompts; seven after. Raw BM25 note recall in the before stratum is 1/6 at both budgets, versus 4/7 and 6/7 after. Mean group coverage before is 16.6667% at both budgets, after 26.1905% / 38.0952%. Both selectors score zero in both strata. Creation-before does not establish unchanged content or historical availability; this remains historical prompts against current snapshot.
- Gold contains minimal useful supporting passages for 13 positives and is nonexhaustive. Retrieving an unannotated chunk is not proof that chunk is irrelevant. Interpret relevant-chunk fraction as annotation agreement, not fully judged precision. One useful subtask passage is not complete answer correctness. There are no independently corroborated accepted KG assertions, so this run cannot estimate KG factual precision or prove graphs intrinsically useless.
- Median/p95 latency independently matches summary: raw BM25 37.75/110.55 ms at 128 tokens and 38.77/111.33 ms at 256; old combined 251.36/1236.98 and 249.99/1226.65 ms; structured 37.45/111.21 and 37.71/113.35 ms. These are local observed timings; no new speed claim beyond this run.

Sanitized aggregate recomputation: `/tmp/real-memory-results-audit-aggregate.json`. Private source judgments and official outputs remain outside Git. Parent continues report/preservation; no further ranking or label changes are needed for this audit.
