# Session Pause - 2026-09-14T22:12:24.694331+00:00

## Summary
Session 2026-09-14 leg 11 pause (tm-dogfood), resumed from the 19:56 leg-10 snapshot. Main checkout on main at 0c5012b69, clean. Three leg-10 agents (B2 critic, B1 critic, B7 version-control) survived the relaunch and reported, so a snapshot must not assume agents die on relaunch. Four PRs landed this leg (B7, B8, exit-code-3 docs, full-UUID pointer rule). B2 and B1 second-round engineers are in flight in isolated worktrees, plus a version-control agent verifying and reclaiming the stale pointer-rule worktree. Process facts learned: writer dispatches from the main checkout need isolation worktree declared explicitly; tm pr open needs a checkout whose HEAD is the branch, so version-control adds a scratch worktree with git worktree add --force and lets tm pr cleanup reclaim it; a rebase done in a scratch worktree leaves the original engineer worktree showing phantom deletions of files added to main since the branch point; the credential scan is mandatory before every push, prose included, and gitleaks over the diff text is the result that counts because detect-secrets on paths silently no-ops for a branch not checked out; cargo test -p trusty-mpm needs --test-threads=2 on this host or exits 137.

## Completed
- PR #7938 B7 fix/b7-pr-metadata merged, squash 6ddf63f18; #7869 #7868 #7786 #7646 at status:merged, live verification pending
- PR #7957 B8 fix/b8-scaffold-gitignore merged, squash 2eb726ce3; #7932 #7875 at status:merged; regression test an_unreadable_gitignore_is_reported_not_treated_as_empty red at a612ec38f, green on main
- PR #7959 docs: tm pr open exit 3 (EXIT_PARTIAL, #7869) documented in crates/trusty-agents-common/src/assets/agents/version-control.md and crates/trusty-mpm/src/assets/skills/tm-workflow.md, squash 980103146
- PR #7963 docs: cross-session pointer rule in crates/trusty-mpm/src/assets/instructions/sections/core.md now requires the full session UUID from tm session ls, goldens regenerated, squash 0c5012b69
- Issue #7956 filed: tm wait --for check single call with --timeout 600 exits 137 near the outer Bash timeout; --timeout 300 --slice 100 reruns work
- Comment on #7804: check_test_pointers.sh tracked-only git grep reads a new untracked *_tests.rs as 26 dangling pointers until git add
- Ticketing verified: #7728 #7863 #7844 reverted to open with comments; all eleven B1/B2/B8 issues carry status:in-progress claims for tm-dogfood 2026-09-14; #7833 #7479 untouched
- Replied to tm-code (6d591f29-8633-44f1-a523-d07d952c5251): no claim on worktree agent-aa90a4ec829f7fd78, it is tm-code's, prune it

## In Progress
- B2: rust-engineer on fix/b2-worktree-lifecycle-r2 cut from 76ad591ac, closing critic WARN. HIGH: gate 4 agent_ownership_blocks (worktree_reclaim.rs:747) returns None for SentinelOwner::Known regardless of session liveness, so after the ForeignNested gate-2 narrowing (worktree_reclaim_claim.rs:200-217) a live session's directly created nested worktree with a merged PR and clean tree is reclaimable; needs a liveness check plus a regression test red at 76ad591ac. LOW: audit_removal in decommission.rs:349 says removing before a refusable removal. On hand-back: security scan (gitleaks over git diff origin/main...branch) and code-critic round 2 in parallel, then version-control PR at rung 5 via scratch worktree, merge, ticketing coded+merged for #7652 #7889 #7885 #7884. The old worktree agent-a9644cd80fbb11ac0 still holds fix/b2-worktree-lifecycle
- B1: rust-engineer on fix/b1-guard-tokenizer-r2 cut from df02eb056, closing critic BLOCK. CRITICAL: pm_guard_bash/heredoc.rs:255-280 quoted_body_spans fallback is quote-blind, so echo "note: use <<'PY' syntax" / rm -rf /important-data / PY evaluates ALLOW (verified live). Fallback triggers on literal_spans.is_empty(), not an unbalanced-quote signal. Brief asked for regression tests red at df02eb056 covering the shape plus single-quoted outer, <<"WORD", and a real heredoc after a closed quoted string; the #7190 apostrophe case must stay green; Fail-Open Check on the fallback trigger; all fallback callers traced. On hand-back: security scan and mandatory code-critic round 2 (trust boundary, rung 5), then version-control PR, merge, ticketing coded+merged for #7839 #7744 #7738 #7743 #7190 (#7833 #7479 pinned by tests on the same branch). The old worktree agent-aa26a6c7c4b48143c still holds fix/b1-guard-tokenizer
- version-control verifying worktree agent-a4714679ef41d5dba: it shows 29 changed files after PR #7963's rebase moved the branch ref under it; the brief asks it to prove the status paths equal git diff --name-only 75b0d360b <rebased head> and stash list is empty before git worktree remove --force, then delete local branch docs/pm-pointer-full-uuid. If any path is not in that diff it stops and reports
- Agents from this leg that may still notify after a relaunch: the two -r2 engineers and the worktree verifier. Check for live task notifications before redispatching; continuation otherwise is a fresh agent from each branch's committed HEAD (git branch --list 'fix/b*-r2'), never git -C into another worktree

## Next Steps
- After B1 merges: reinstall trusty-mpm via local-ops (cargo install, never cp), then dispatch the guard follow-up batches as file-cluster PRs against the shared tokenizer: B3 #7477 #7850 #7487 guard policy; B6 #7905 #7838 #7778 pm_guard source-write classification; B4 #7748 #7739 #7730 pre-push scan gate; plus #7766 from B2. #7477 and #7839 carry the grep-refusal and heredoc-false-positive observations
- After B2 and B1 merge: one trusty-mpm reinstall, live-verify each merged issue's reproduction command (#7869 #7868 #7786 #7646 #7932 #7875 plus the B2/B1 sets), close from status:tested with tm issue transition N closed --note
- Owner to confirm #7868's resolution: seven-field contract enforced at open only (exit 2 pre-flight), merge only reports contract gaps and refuses solely on a missing attribution footer
- Owner decisions still open: #7914 (--path removal vs ADR-0057, reverted to open); #5206 epic decomposition; #7722 half-verified; where to track #7728/#7863 since the refusal originates in the Claude Code binary; guard-blocked worktrees in the owner's shell (pr-7667, agent-a4ab4f65228f830e3, agent-acbddb8f61e9b54de); new locked worktree agent-a2278a6f4f4ce2d01 on docs/7937-adr-tui-primary belongs to another session, leave it
- Improvement recs not yet ticketed: HeredocBodies::scan quote-scan gap may also affect has_file_write_redirection (B1 critic rec, fold into the B1 -r2 PR or file after); bug template should require the exact repro command (B1 rec 3); pause skill should not state that agents die on relaunch and should record agent ids from dispatch results (this leg's snapshot mislabelled a2615654d96bcb4a9 as ticketing when it was the B1 critic); delegation-patterns skill should say to declare isolation explicitly on every writer dispatch from a main checkout; security brief template should name gitleaks-over-diff-text as primary and warn that the sandbox refuses any path containing the word secrets
- B2 critic found two parallel-interference flakes worth a serial_test comment on an existing flake issue: worktrees_exclude_entry_protects_against_double_force_clean, parity_doctor_agrees_across_transports
- Labeling gaps unfixed: #7804 #7634 #7812 no crate label; #7732 #7731 mislabelled trusty-mpm
- Standing rules: claim at dispatch; owner-waived close from status:merged via gh; never brief --watch; check_test_pointers.sh in rung-6 gates; verify new MCP params over raw stdio after a daemon restart; tm-code owns trusty-code; declare isolation: worktree explicitly on every writer and read-only qa dispatch; version-control never gets isolation and only one runs at a time in the main checkout; tm wait --for check at --timeout 300 --slice 100, never 600 in one call; TodoWrite is absent in this harness build, carry todos in the snapshot

## Git Context
Branch: main
Last commit: 0c5012b69 docs(trusty-mpm): cross-session pointer names its session by full UUID (#7963)
Uncommitted changes: clean

## Tmux Window
tm-dogfood:0:@1
