# Session Pause - 2026-09-17T12:11:26.331678+00:00

## Summary
Addendum to the 2026-09-17T12:02:23Z snapshot of session tm-code (read that one first: .trusty-mpm/sessions/d6d1e7a0-1b65-4beb-9542-660ebc188e78/session-20260917-120223.md; everything in it still holds except the #8237 diagnosis). CORRECTION: the #8237 experiment (tcode 0.7.0 e6de61cd4, anthropic/claude-sonnet-5, TCODE_DEBUG_TRANSCRIPT=1) shows bash execution is CORRECT: the parsed command keeps its newline and both statements ran. #8237 is a display-only TUI bug: apply_permission_requested at crates/trusty-code-tui/src/app/reduce.rs:459-464 puts pending.subject verbatim into one scrollback row and a raw newline in a ratatui Span renders as nothing; the boxed widget already folds with ' · ' (fold_to_one_line, crates/trusty-code-tui/src/widgets/permission_prompt.rs:142-148). Parity task P3 changed no files because the model used GNU-style `sed -i` on macOS BSD sed, not because of tcode. The write_file/edit blast-radius concern is withdrawn. #8237 is no longer HIGH and no longer ahead of #8182 in priority. Limit: the debug capture records arguments after stream-delta concatenation, so pre-concatenation corruption is not formally excluded. Second defect seen: input arriving while a turn is in flight loses newlines and the pane stops updating for minutes (observed under scripted tmux send-keys; human repro owed). Recipe note: tcode's fixed DEFAULT_HTTP_PORT collides when another tcode serve is running (pid 2678 was live and untouched); workaround was a manual `tcode serve --port 0` under the same TRUSTY_DATA_DIR_OVERRIDE and letting the TUI attach to it; prefer one prompt per TUI session and poll the debug-capture file for turn progress.

## In Progress
- ticketing agent a10efb250e58b2949 (sonnet): corrects #8237 (comment, retitle, body, severity), searches/files the in-flight-input render defect, notes on #8238 that the bash collapse is not its cause
- code-critic agent a443af9aba8e08068 (opus): round on e6de61cd4..2a2429f68, branch fix/8205-elision-connect-line; push HELD until its verdict

## Next Steps
- #8237 fix is now small and TUI-only (rung 3/6): one shared fold helper used by the scrollback permission line and the widget, regression test with a multi-line subject; it touches crates/trusty-code-tui/src/app/reduce.rs, so sequence it after the #8205 follow-up PR merges, and it can ride with #8182 slice 1
- Parity fixture: P3's pass criteria are fine, but note in TASKS.md/REPORT.md that the P3 failure was BSD sed, and correct gap 1 in runs/2026-09-17/REPORT.md
- All other next steps and owner decisions: see the 12:02:23Z snapshot

## Git Context
Branch: main
Last commit: 90b6aeb94 docs(trusty-code): verify_gate intra-doc link resolves; Rustdoc gate green again (#8239)
Uncommitted changes: clean

## Tmux Window
tm-code:0:@13
