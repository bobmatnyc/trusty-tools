# Session Pause - 2026-09-04T01:48:36.322682+00:00

## Summary
Pausing so the owner can restart the tm-dogfood pane. Session diagnosed and unblocked a bare-`tm` failure: running `tm` for this project printed Claude Code's background-session notice and dropped the owner at a bare shell instead of connecting.

ROOT CAUSE (confirmed, not a regression — latent for weeks): crates/trusty-mpm/src/runtime/claude_code.rs builds a bare `claude --continue` at :512 (spawn_resume path) and :846 (in-pane exec path). Whether to do that is decided by `has_prior_conversation` at :548-553, which hardcodes `~/.claude/projects` — it was never migrated to `projects_dir_for` (:597-603), unlike its sibling `session_id_exists` (:630-635) which correctly resolves <CLAUDE_CONFIG_DIR>/projects. The spawned claude runs with CLAUDE_CONFIG_DIR=/Users/masa/.trusty-tools/trusty-mpm/claude-config, so it resolves `--continue` against the MANAGED store. Three stores, three answers: eligibility check reads ~/.claude, staleness check reads the managed dir, the process resolves against the managed dir.

WHY IT SURFACED NOW: this session was started by a hand-typed `claude --resume` (PID 31451, no CLAUDE_CONFIG_DIR in its env, none of tm's flags — no --setting-sources, no --append-system-prompt-file). Its transcript landed in ~/.claude, flipping has_prior_conversation to true for a store the relaunch never reads. guided.rs:606-612 warns about exactly this anti-pattern.

TM IS BLIND TO THE FAILURE: zero source hits repo-wide for the notice text. The tmux path is `tmux send-keys` (src/session_manager/real_tmux.rs:135-156) with no output channel; spawn_resume (:1215-1230) returns Ok(()) regardless. The `;`-sequenced trailing hint runs unconditionally, leaving the pane at a bare shell. Nothing in the crate reads <config_dir>/jobs/*/state.json or checks a launcher PID, so tm cannot exclude a dead background job — it does not know the concept exists.

OWNER REQUIREMENT, verbatim in spirit: "The user does not need to know this, we need to handle the complexity for the user." Running `tm` must land the user in a working session — never the notice, never `claude agents`, never `claude --resume`, never needing to know CLAUDE_CONFIG_DIR exists.

IMMEDIATE UNBLOCK DONE: archived the blocked background job 768edbae-952f-46a0-b510-dcfba7800191 (state "blocked", dead launcher PID 71387, 1,025,620 tokens, 3 results captured, name "tm-session-resume tmux window behavior", intent "keep all three parts on 3764, park the rest"). Moved (not deleted) to ~/.trusty-tools/trusty-mpm/claude-config/_archived-jobs/20260904-768edbae/ with RESTORE.md holding the inverse commands. CAVEAT recorded there: its respawnFlags carry --permission-mode bypassPermissions and 25 --allowed-tools mcp__computer-use__* pairs with bgIsolation "none" — a naive restore-and-respawn relaunches with full computer-use access and no permission gating. The sibling directory projects/-Users-.../768edbae-952f-.../ was deliberately NOT archived (it holds tool-results/ and subagents/ symlinks into the live ca9d58d8 session). Newest transcript is now ca9d58d8-e6a4-4ff0-8818-38a639ff51f3.jsonl, verified not a background job. tm is clear to connect.

NOT a tmux reattach problem — the owner's first framing was corrected by him. tm-dogfood (id 0b318c84-bae9-4a50-8832-65ed61f8ab22, pane %6869) was live the whole time.

## Completed
- Diagnosed the reported 'cannot resume' as three separate signals, none a resume bug: `tm session list` is a deprecated verb reading a different store than `tm sessions ls`; `tm-trusty-tools-02` never was a runtime session (only pause-snapshot dirs); the background job 768edbae was not in tmux at all
- Root-caused the real bug — bare `claude --continue` decided against ~/.claude but resolved against the managed store (claude_code.rs:512, :846, :548-553)
- Established tm never observes the failed spawn (send-keys has no output channel; spawn_resume returns Ok(()) regardless) and never reads <config>/jobs/
- Confirmed NOT a regression: no commit in the last three weeks touched the --resume/--continue selection or has_prior_conversation. Latent since projects_dir_for was added for #2013 and the sibling check was left behind
- Searched prior art — no existing issue covered it. #4335 (CLOSED) is the closest sibling: same trigger, gate layer, and it explicitly says not to widen Gate A's stopped-only check
- Archived blocked background job 768edbae with RESTORE.md and a bypassPermissions caveat; verified dead PID, no live refs, no open file handles, and that the next-newest transcript is not itself a background job
- Verified tm is clear to connect — newest transcript is now ca9d58d8-e6a4-4ff0-8818-38a639ff51f3.jsonl

## In Progress
- ENGINEER LIVE IN FLIGHT at pause (isolation worktree, general-purpose agent). Filed #6765 (bug: bare --continue decided against ~/.claude but resolved against the managed store) and #6766 (follow-up: post-spawn pane verification safety net). Implementing TWO changes in ONE PR against #6765: (1) thread the config dir through has_prior_conversation so it resolves via projects_dir_for, updating call sites :846 and :1195; (2) replace the bare --continue branches at :512 and :846 — extend the scan in has_prior_conversation_in (:567-579) to return the newest eligible .jsonl by mtime, EXCLUDING transcripts whose 8-hex stem prefix matches a <config_dir>/jobs/<prefix>/ dir whose state.json reports a non-terminal state, and pass --resume <id> explicitly. Eligibility check MUST fail open — any parse error, missing dir, or unexpected shape leaves the candidate IN the set. Rung 5 (process lifecycle), changelog fragment required, Refs #6765 never Closes. THIS AGENT DIES ON RESTART — verify its worktree for uncommitted work and whether the PR was opened before re-dispatching.

## Next Steps
- ON RESUME: check whether the engineer opened its PR before the restart killed it — `gh pr list --search 6765` and inspect .claude/worktrees/ for uncommitted work. Re-dispatch from the brief in the In Progress section if it did not finish
- Verify the fix live: run bare `tm` for this project and confirm it connects without the background-session notice
- File the two ADR-0048 guard defects the owner flagged: (a) stale delegation records are never reaped — a record naming `api-qa` as running in this main checkout with no live process behind it blocked every subsequent dispatch; (b) the guard denies a dispatch it admits was already granted a worktree, saying it 'cannot confirm' the grant applied rather than verifying. Owner's words: 'We should not get these errors either'
- Reinstall `tm` to clear the `tm doctor` binary_provenance failure — 1.5.17 was installed with `cargo install --path` from .claude/worktrees/agent-a242ea133c899a71d/crates/trusty-mpm, which no longer exists, so the binary has no provenance and no upgrade path (issue #4033)
- Consider filing: `tm session list` reports 'no sessions' for this project while `tm sessions ls` shows one at slot 24 — a deprecated verb that reads a different store and lies rather than forwarding
- #6766 (post-spawn pane verification) remains unimplemented by design — it was split out of the #6765 PR deliberately. Tradeoffs recorded on the issue: added resume latency, string-matching a message Anthropic can reword, double-spawn risk on slow start

## Git Context
Branch: main
Last commit: 059677f60 chore(release): assemble changelogs for the audit train and bump tga to 7.0.0 (#6762)
Uncommitted changes: 4 file(s) changed

## Tmux Window
tm-dogfood:0:@6869
