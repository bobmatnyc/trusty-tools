# Session Pause - 2026-08-18T15:38:56.802710+00:00

## Summary
Session tm-search (renamed mid-session from tm-trusty-tools-01). Started by resuming a paused session's five open owner decisions, became a full defect-fix wave plus a machine recovery. ELEVEN PRs MERGED, THREE PRs closed as superseded, semantic search recovered from a dead external volume without touching the daemon, and branch protection hardened 11 -> 13 required contexts.

THE SESSION'S THROUGH-LINE, and it recurred at every layer: a surface reporting a state its own evidence does not support. A gate that runs but is not required (trusty-audit-ui clippy went red on #5929 and the PR merged anyway; main did not compile for hours). A test whose tokio::time::timeout result was discarded, so it PASSED after timing out in exactly 120.01s. A counter named indexes_corpus_failed that counts any failed lane. A grandfather pass that fired only when a file was absent, so 103 of 121 index registrations were silently dropped on upgrade. A required context with no check run, which reads as UNMET and blocks forever while gh pr checks shows nothing red at all.

🔴 I WAS A SOURCE OF THIS TOO, TWICE, AND BOTH WERE CAUGHT BY AGENTS RATHER THAN BY ME. I relayed a palace claim that "Vercel, Claude, Supabase, Railway never settle on this repo" into roughly eight merge briefs without once verifying it; a documentation agent asked to write it into CLAUDE.md checked first and found Vercel completes normally and the other three are not checks on this repo at all (drawer 1c92ac12). And I invented a conflict between #5966 and #5962 in plain_cli_repl.rs by conflating two agents' reports, then carried it through four briefs; #5966 never touches that file and update-branch merged clean. Both are recorded. The lesson is the one the agents modelled: when told to write a fact, verify the fact.

MACHINE STATE IS MATERIALLY BETTER THAN AT START. Semantic search was down daemon-wide; it is now live (4218ms cold, 26ms warm, embedder ready, recent-timeout count 65 -> 0) and NO daemon restart was needed. Two duplicate unmanaged trusty-memory daemons killed. Worktrees 28 -> 13, ~225 GB reclaimed with preserve-before-delete throughout. gworkspace-mcp registered durably at user scope. Linear CLI installed. main compiles as a workspace again, verified by hand because no required context compiles that crate.

## Completed
- DECISION 3 — killed the two unmanaged duplicate trusty-memory daemons (PIDs 19643, 79461, cwd inside .claude/worktrees/full-build-install-20260817); kept launchd-managed 77858 holding the LISTEN socket. Daemon health verified after.
- DECISIONS 2+4 — worktree cleanup 28 -> 13, ~225.6 GB reclaimed. Every removal file-list-verified first; two authored-source worktrees preserved and later landed, three orphaned dirs removed after proving every blob already existed in git history.
- DECISION 5 — gworkspace-mcp registered durably at user scope. Root cause was a binary RENAME (gworkspace-mcp -> trusty-gworkspace-mcp), not a path bug. Four stale .mcp.json files were inert artifacts of an injector DELETED in #5409 under ADR-0042 — nothing regenerates them, so no code fix exists or is needed.
- DECISION 1 — PR triage. #5881 MERGED (58d31ad8, launchd fail-closed, the bug behind the morning's duplicate daemons). #5880/#5882/#5883 all CLOSED as superseded, each with evidence: #5882's trusty-memory diff was byte-identical to #5048's squash; #5883 collided semantically with #5061/#5350's shipped design.
- Salvaged an abandoned 263-commit-stale branch into four merged PRs by issue: #5893 bounded inference retry (#2272), #5894 incremental context floor (#3948), #5895 memory durability signal (#2425, BREAKING trusty-code API), #5928 RUST_LOG default-level fix. Owner chose the split; the fourth change had no issue and I had failed to assign it.
- #5905 — replaced the stash-first fallback recipe with a throwaway worktree, narrowed per owner ruling to DROP the git-stash prohibition #4730 had already declined. Agent used .claude/worktrees/ not /tmp because the harness blocks /tmp worktrees (#3955).
- #3655 FIXED and closed (#5939) — 7 plain_cli_repl tests hung 120s each. Root cause: #[tokio::main]'s runtime drop calls BlockingPool::shutdown with NO ceiling while a worker blocked in open() on the model cache. Fix adds run_to_completion() with shutdown_timeout(5s) and moves the blocking build to the blocking pool. 10x re-run PASS=10 FAIL=0, 8.66-10.43s per run.
- #5926 FIXED and closed (#5932) — #5686's grandfather pass keyed on 'allowlist.toml exists' rather than on the durable .grandfathered stamp it had itself added, so 103 of 121 index registrations were dropped on upgrade. The buggy early return never wrote the stamp, so the fix self-heals every affected machine.
- #5927 FIXED and closed (#5933) — indexes_corpus_failed now reads corpus_open_failed; the any-lane count moved to a new indexes_stage_failed. Chose fixing semantics over renaming because 'did any corpus fail to open?' had no answer anywhere. The Fail-Open Check caught a NEW fail-open the fix itself would have introduced in trusty-review's degraded_reason().
- #5935 FIXED and closed (#5938) — main did not compile as a workspace. WorkDir::resolve gained a param in #5929 and two guided.rs call sites were never updated. Agent passed dirs::home_dir() not None, because guided.rs's own doc asserts CLI parity. cargo check --workspace verified by hand on merged main.
- #5943 FIXED (#5966, armed) — LLM HTTP client had NO request timeout. connect_timeout(10s) + read_timeout(300s), deliberately no total timeout so long completions survive. Also stopped with_llm_retry retrying timeouts, which would have spent the budget 4x. Test duration 120.01s -> 7.24s.
- #5944 FIXED and closed (#5962) — $HOME isolation was bypassed by an unbounded current_exe() walk-up. Agent rejected both approaches I suggested with reasons and bounded the walk at the build root instead.
- #5934 CLOSED — branch protection 9 -> 11 -> 13 required contexts. The bobmatnyc account CAN write to the protection endpoint; CLAUDE.md's admin-only claim is wrong for it. Added trusty-agents-ui/trusty-audit-ui clippy, then trusty-mpm-gui/trusty-code-gui clippy after #5958 created those jobs.
- #5958 MERGED — dedicated Rust clippy CI jobs for trusty-mpm-gui and trusty-code-gui, the last two Tauri crates with no Rust coverage. Both already compiled clean. Also wired both into notify-main-failure, without which a red job on main would file no tracking issue.
- #5967 MERGED — CLAUDE.md: deleted the required-contexts enumeration entirely (it had rotted five times: six -> nine -> eleven -> thirteen), corrected the BEHIND-cannot-merge claim with measured evidence, added the Tauri-jobs rationale and the missing-job-definitions carve-out.
- SEMANTIC SEARCH RECOVERED — root cause was HF_HOME at ~/.zshrc:214 pointing at the hung /Volumes/SSD1, and fastembed::pull_from_hf OVERRIDES with_cache_dir. A complete working model cache was already on internal storage, unreachable behind that env var. unset HF_HOME + replaced the symlink with a real dir. No restart, no reindex, no download.
- Session renamed to tm-search. Linear CLI installed (schpet/tap/linear 2.5.0) — no official Linear CLI exists.
- Issues filed: #5890, #5891 (check_ignored_tests.sh silent build phase + missing trusty-audit-ui exclude), #5934, #5935, #5943, #5944. Commented rather than duplicated on #3655, #1681, #5911, #767.

## In Progress
- 🔴 PR #5966 IS ARMED AND WILL LAND UNATTENDED — auto-merge enabled 2026-08-18T12:46:03Z, mergeMethod SQUASH. Owner authorization is recorded verbatim in palace drawer 7b6970b3 (fact_key pr:5966/merge-authorization): 'merge 5966 when green.' At last read only `Clippy` was IN_PROGRESS; both GUI clippy contexts already SUCCESS. DO NOT re-dispatch a merge agent — VERIFY state first with gh pr view. On resume: confirm it merged, prove the squash SHA is an ancestor of origin/main, and confirm issue #5943 CLOSED. If it did NOT merge, read why before acting — see the missing-job-definitions trap in drawer a52f5dad.
- Merge agent a5c7d4bfa8fecdee2 is alive and idle, holding full context on the queue. Its rollback record for branch protection (the pre-change 11-context object) is at scratchpad/rollback_required_status_checks.json. Prefer re-engaging it over dispatching fresh.

## Next Steps
- 🔴 /Volumes/SSD1 IS STILL HUNG — timeout 3 ls exits 124; diskutil falsely reports it mounted and SMART-verified. Semantic search no longer depends on it (cache moved to internal storage) so this is no longer urgent, but ANY file I/O against that path still blocks with no ceiling. Guard every command that could touch it with timeout. Needs physical operator action: reseat or remount. Its contents were deliberately never read or deleted and survive intact.
- OWNER ACTION — Linear CLI is installed but unauthenticated. Create a key at linear.app/settings/account/security, run `linear auth login`, then `linear auth migrate` to move it into the system keyring. That third step matters: tga needed two fixes this month (#5772, #5776) to stop Linear credentials reaching Debug output and warn logs, so a plaintext key on this machine is a known-live risk shape. No agent touched a key.
- STILL OPEN, filed but unfixed: #5709 (pre-existing flake, neither_source_leaves_the_project_fallback_intact, reads trusty-tools from the ambient environment). #1681 (epic: canonical project/source-instance identity — a live basename-collision reproduction was commented there; #4062 was already closed NOT_PLANNED for keying on git remote, and remote-keying also collides since both checkouts share origin).
- UNFILED, offered and not taken up: /health's embedder_bootstrap still reads 'failed' after full recovery — a latched field with no re-check, reporting a state contradicted by the working embedder beside it. Same family as #5927. Cheap to fix if wanted.
- REMAINING CI EXPOSURE, now smaller: all four Tauri crates have dedicated Rust clippy jobs and all four are required contexts. What is NOT covered is any future crate added without one — the failure mode is invisible (a required context with no check run does not appear as pending), so adding a required context now also means update-branching every open PR. Both facts are in drawers a52f5dad and a4673d09.
- MERGE PROTOCOL CHANGED, measured today — put in every future merge brief. strict is FALSE, so a merely-BEHIND branch merges fine; do NOT update-branch for BEHIND alone, it restarts four ~15-minute Tauri jobs and lets main move again mid-cycle. Arm `gh pr merge --squash --delete-branch --auto` instead. THE EXCEPTION: a PR predating a newly-required job MUST be update-branched, because its own workflow snapshot lacks the job definition and the check can never exist.
- TWO PALACE DRAWERS CONTRADICT EACH OTHER ON AUTO-MERGE and neither should be trusted blind: one says --auto is the answer to the strict race, another says auto-merge stalls forever on a BEHIND branch and states strict:true. strict read FALSE today. Re-read it live before relying on either — one gh api call settles it.
- trusty-memory's secret detector false-positives on ONNX/HuggingFace cache path names (rejected `models--Qdrant--all-MiniLM-L6-v2-onnx` and a FETCH_HEAD:path string as credential tokens). Worth knowing before writing a drawer about model caches; reword rather than fighting it.

## Git Context
Branch: main
Last commit: e3fb4f153 chore: keep the tm harness scaffolding ignore block after the upstream .gitignore append
Uncommitted changes: 6 file(s) changed

## Tmux Window
tm-search:0:@30
