# Session Pause - 2026-09-22T20:44:18.038396+00:00

## Summary
Session tm-dogfood (tmux tm-dogfood:0:@4) pausing 2026-09-22 about 20:15 UTC on owner request ("stop all other agents and pause") in the middle of a keystroke-latency incident. This session owns the trusty-tools merge queue. Main checkout clean on main at 805c47f48, level with origin; only .pnpm-store/ untracked. INCIDENT: the owner sees seconds-per-keystroke lag in EVERY tmux pane on the host, native tmux included, with WindowServer stalls; none in vanilla claude or claude-mpm outside tmux. Owner narrowed it to tmux server configuration and memory: the 100k-line global history-limit that crates/trusty-common/src/tmux.rs:322-329 sets at server bring-up (origin #2398). Owner decision: "we don't need 100k lines, change to 10k". A local-ops agent, still running at pause, is applying it live: tmux set-option -g history-limit 10000, the matching key in ~/.trusty-mpm/config.toml, and tmux clear-history on every pane over 10k lines, with before/after tmux server RSS and round-trip numbers. Verify on resume with tmux show-options -g history-limit and the server RSS. The CODE default change (tmux.rs 100_000 to 10_000, doctor or docs mention, config override honoured) is NOT dispatched and has NO issue yet; the ticketing search was stopped before filing (search keys history-limit, scrollback, keystroke; origin #2398, #2414). Research ranked two other tm-only diffs from vanilla that touch every frame if 10k does not cure it: CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 (core/alt_screen.rs:64-88, #6495) and the tmux alternate-screen off window option (trusty-common/src/tmux.rs:339-360, #5364), compounding with the scrollback; plus the per-render tm statusline fork (100ms-bounded segments, no daemon call). Supervisor capture_pane cadence is 30s, not per keystroke. OTHER WORK: #8286 (tm ls new-session uses the old injected prompt; owner: every launch mode must use --append-system-prompt-file) claimed status:in-progress with a findings comment; research proved tm launch, connect, guided relaunch, daemon spawn/resume and DaemonClient already use the file flag, and the two divergent paths are the tm ls picker request and the non-repo in-place fallback at bin/tm/commands/session/start.rs:283 (builder model_inject.rs:433 has no prompt flag). Earlier: #8376 moved to milestone Issue management #94; #8021 rollup comment posted (16 items); the #8236 rustdoc fixes were checkpointed at 64cba93b6 on fix/8236-rustdoc; PR #8374 is red on the REQUIRED tm-capabilities generated-skill drift check, and the rustdoc lane is red but not required. All four gate engineers were TaskStop-killed mid-gate at owner request; their trees and commit states are in In Progress.

## Completed
- #8376 moved to milestone Issue management #94 (ticketing)
- #8021 prompt-feedback rollup comment posted, 16 new items, 4 dropped as duplicates
- #8236 rustdoc intra-doc link fixes checkpointed at 64cba93b6 on fix/8236-rustdoc (doc-comment-only, verified by version-control), not pushed
- #8286 claimed status:in-progress, findings comment posted; research inventory of every launch mode's instruction delivery (report in this session's transcript; two divergent paths named in Summary)
- Research diff of tm-launched vs vanilla claude config (alt-screen env, tmux alternate-screen off, 100k history-limit, statusline fork, hooks, 4 stdio MCP servers) ranked; findings in Summary

## In Progress
- LIVE tmux fix (local-ops agent running at pause): history-limit 10000 global + config.toml key + clear-history on panes over 10k; read its result on resume if the agent survived, else verify by tmux show-options -g history-limit, ps rss of the tmux server, and the config.toml diff
- #8286 file-injection engineer KILLED: worktree agent-a3e9f13ac0e6fbeb5 on fix/8286-file-injection-every-launch-mode at 4a034c024 (one commit past main; check git status there for uncommitted work). Scope: tm ls picker new-session path plus session/start.rs:283 in-place fallback both routed through the prompt-file builder, red-before tests, fail-closed write arm, fragment, allowlist test
- #7889 gate engineer KILLED: worktree agent-ae7b3cdf179890276 on fix/7889-landed-content-admission-cont at 0b5c15a57 (past checkpoint 49c3c9362; was collapsing a wrapper function; check git status). Owner nod on the #7275 round-2 reopening still owed before critic round
- #8376 gate engineer KILLED: worktree agent-a54976f2cd835bc45 on feat/8376-ticketing-md-cont still at 3070dc5f2 (no new commit; was at the changelog gate; check git status). Owed gates as in the 17:13 snapshot
- #5850 gate engineer KILLED: worktree agent-abaed5c59e5721e45 on fix/5850-user-alias-registry-gh-env-cont still at 5b3d46ad1 (no new commit; was on cargo test). Owed: stable clippy, cargo test, four red-before lines, check_sld, check_capabilities
- PR #8374 (#8236): needs a capabilities regeneration commit from the PR's own binary (required drift check red) plus the rustdoc checkpoint 64cba93b6 fast-forwarded onto fix/8236-launchd-plist-r3-cont by version-control, then CI, undraft, merge
- #8382 layout guardrail: unchanged since the 17:13 snapshot (checkpoint e623e0c1a on worktree-agent-aa4ee69a6331bbb5d; golden regen + fragments owed)
- Warm private target dirs KEPT: ~/.trusty-tools/cargo-target/bobmatnyc/trusty-tools-{7889,5850,8376,8286}; builds were killed mid-way so artifacts are partial

## Next Steps
- RESUME: session_context_catchup with tmux_window tm-dogfood:0:@4; ListAgents; confirm the tmux fix landed (history-limit 10000, server RSS down, keystrokes normal). If the lag persists, next candidates in order: tmux alternate-screen off (#5364), CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 (#6495), tm statusline per-render fork; measure with time tmux display -p x and the transient tmux client sampling
- Ticketing: search-then-file the issue for the tmux scrollback default 100k to 10k (labels bug, trusty-mpm, status:in-progress; origin #2398); then rust-engineer on crates/trusty-common tmux.rs (rung 4, cross-crate: trusty-common consumers) with config override and a doctor or docs mention; fragment in trusty-common
- Re-dispatch the four killed engineers from their tree SHAs (new -cont2 branches in fresh worktrees; check each tree's git status first and commit any uncommitted work via version-control), ONE OR TWO AT A TIME while the host recovers
- Per branch after gates: security scan three-dot, version-control push + PR (Refs #N, rung in body), code-critic round with trusty-review review_pr verdict, CI via gh workflow run CI --ref, merge on green with no outstanding verdict. Queue order: #8374 then #8382, #8376, #5850, #7889, #8286, then PHASE_1 #8387 last
- Owner decisions still open: four #8387 dispositions; #7889 #7275-round-2 reopening; #8372 non_exhaustive vs major bump (blocks trusty-mpm publish)
- Daemon restart when no builder slot is held (launchctl bootout/bootstrap per docs/reference/release-workflow.md:1039-1048, local-ops), live checks #8261 #8351 #8361, tm reinstall after the batch merges, tm generate capabilities; worktree cleanup list unchanged from the 17:13 snapshot plus the four killed-engineer trees once their commits are carried forward

## Git Context
Branch: main
Last commit: 805c47f48 docs: instructional-content tracking research and ADR-0064 draft (Refs #8378) (#8381)
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-dogfood:0:@4
