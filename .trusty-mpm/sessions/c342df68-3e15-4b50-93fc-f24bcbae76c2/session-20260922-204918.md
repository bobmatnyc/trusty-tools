# Session Pause - 2026-09-22T20:49:18.342283+00:00

## Summary
Session tm-dogfood (tmux tm-dogfood:0:@4) pausing 2026-09-22 about 21:05 UTC, owner /tm-session-pause, superseding the 20:44 snapshot (read that one for the full queue state; this one carries the latency incident delta). Main clean on main at 805c47f48. INCIDENT: seconds-per-keystroke lag in every tmux pane on the host, native tmux included, WindowServer stalls; vanilla claude and claude-mpm outside tmux are fine. FINDINGS SO FAR: tmux server is NOT the bottleneck (RSS 76 MB, 36 MB total scrollback, command round-trip 26-31 ms); the scrollback hypothesis is weak (only the PM pane exceeded 10k lines). Applied anyway, owner-authorized: history-limit 10000 global plus history_limit: 10000 under tmux: in ~/.trusty-tools/trusty-mpm/config.yaml (that file, NOT ~/.trusty-mpm/config.toml, is what the crate reads; clamp floor 1000, trusty_tools_config.rs:357-393); the PM pane's 33k-line history cleared. CURRENT EXPERIMENT (owner: "try alternate-screen on this session, I'll restart"): tmux alternate-screen ON for tm-dogfood:0 and globally (was off from #5364), alternate_screen: true persisted in config.yaml (drives the window option at session creation, tmux.rs:577/587). The env layer is SEPARATE: the launch line hardcodes CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN="${...-1}" (core/alt_screen.rs:65-88, #6495) and yields only to a value already in the spawning environment; a local-ops agent was live at pause adding CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=0 to the com.trusty.mpm launchd plist EnvironmentVariables, restarting the daemon per docs/reference/release-workflow.md:1039-1048, and running launchctl setenv. A second local-ops agent was live measuring the CLIENT path (tmux list-clients for tm-dogfood with owning terminal program per client, stopped or undersized clients, and pipe-pane bytes per second through the pane). Owner's insight to keep: the server answering fast does not clear it; the attached client and the emulator redraw volume were never measured before this. Remaining candidates if alt-screen does not cure it: per-render tm statusline fork, multiple attached clients, the terminal emulator itself (discriminator: attach the same session from a different terminal app).

## Completed
- tmux history-limit 10000 live + config.yaml tmux.history_limit: 10000; PM pane history cleared (no measurable server memory change)
- tmux alternate-screen on for tm-dogfood:0 and global; config.yaml tmux.alternate_screen: true (backup in scratchpad config.yaml.before)
- Research: tm-vs-vanilla config diff ranked (alt-screen env, tmux alternate-screen off, statusline fork, hooks, 4 stdio MCP servers); launch-mode instruction-delivery inventory for #8286
- #8286 claimed + findings comment; #8376 milestone; #8021 rollup; #8236 rustdoc checkpoint 64cba93b6 (all detailed in the 20:44 snapshot)

## In Progress
- local-ops (daemon env): plist edit CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=0 + documented daemon restart + launchctl setenv; verify on resume with launchctl print gui/$(id -u)/com.trusty.mpm and tm doctor daemon rows; if the plist is regenerated by tm daemon install the edit may be lost, agent was to name the tm-side setting
- local-ops (client path): tmux list-clients for tm-dogfood with owning program per client, stopped/undersized clients, pipe-pane bytes per second; pipe must be OFF afterwards (tmux display -p -t tm-dogfood:0 '#{pane_pipe}' must be 0) and /tmp/tm-dogfood-pane-bytes.log deleted; verify both on resume if the agent died
- Owner restarts the Claude session in tm-dogfood:0 AFTER the daemon restart so the new spawn inherits the env; then judge keystroke latency
- Everything else (four killed engineer trees, PR #8374, #8382, #8286 code, tmux 10k code default with no issue yet) unchanged from the 20:44 snapshot

## Next Steps
- RESUME: session_context_catchup with tmux_window tm-dogfood:0:@4; ListAgents; read both local-ops results if alive; confirm pane_pipe is 0 and the daemon env shows the variable
- If latency is cured by alt-screen: file the follow-up (search first) to make alternate_screen: true drive the env default too, or flip the #6495 default, and restore/decide history-limit; document in docs/reference/common-pitfalls.md
- If NOT cured: next discriminators in order: attach tm-dogfood from a different terminal app; detach extra clients (tmux detach-client -t <tty>) one at a time; temporarily remove the statusLine key from the session's settings and compare; then the emulator
- Then resume the merge queue per the 20:44 snapshot: ticketing for the 10k-default issue, engineers one or two at a time from their tree SHAs, PR #8374 capabilities regeneration, queue order #8374, #8382, #8376, #5850, #7889, #8286, #8387 last
- Owner decisions still open: four #8387 dispositions; #7889 #7275-round-2 reopening; #8372 non_exhaustive vs major bump

## Git Context
Branch: main
Last commit: 805c47f48 docs: instructional-content tracking research and ADR-0064 draft (Refs #8378) (#8381)
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-dogfood:0:@4
