# Session Pause - 2026-09-21T20:34:16.131513+00:00

## Summary
Session tm-dogfood (tmux tm-dogfood:0:@4) pausing on owner request 2026-09-21 evening with FOUR agents in flight (three rust-engineers mid-build, one version-control pass). Main checkout clean on main at b8c8d3df2, level with origin; only untracked .pnpm-store/. This session owns the trusty-tools merge queue. Worktree prune SKIPPED on purpose: live engineer trees and an in-flight removal pass. Day's arc: the TUI hung and the owner restarted; this pane IS tm-dogfood (no second PM existed). Supervisor poller was wedged since 14:45Z, stack-sampled, restarted via launchd (pid 41148), filed #8335. trusty-memory reinstalled 0.26.2 from main (crates.io still 0.26.1); THIS pane's MCP config points at a nonexistent socket, so palace access returns only after relaunch. Owner rulings today: 1.6.5 theme is BUILD EFFICIENCY, milestone retitled '1.6.5 · Build efficiency', organised in THREE LAYERS (framework #8343 / language-Rust #8261 #8344 #8329 #6868 #8297 #8340 / project #8341 #8345); non-build issues moved to 1.6.6 unless labelled P1 (28 moved incl. #8335; #8236 held as P1); 'the entire point of separate crates is a more efficient compilation process'; tmux-mcp NOT adopted, ideas filed as #8349 on new milestone 1.6.7; Dependabot held to 1.6.7 as #8350 (19 critical/high, none in Rust; cargo audit clean); #8236 risk accepted for now, finish the work. Merged today by this session: PR #8342 (skill crate-edge rule, squash 23ce71349). Pushed straight to main under the new-docs exception: docs/research/worktrees-best-practices.md (545e9af41, b8c8d3df2), 781 lines, scanned and redacted; the push bypassed branch protection via admin rights. Key findings: cargo keys path crates on absolute path, so every fresh worktree builds cold (#8261 timings 200 s cold / 103 s warm target other path / 17 s same path); sccache wired at 50 GiB with 0 hits, --remap-path-prefix is unsound, SCCACHE_BASEDIRS is the lever; machine-global build.jobs=4 on 16 cores; trusty-mpm has 53 integration-test binaries, two mutate HOME; five test-only workspace dev-dep edges found; WorktreeCreate hook dry run proved a fixed-path pool is feasible but the hook gets no agent type or branch and Claude Code trusts the returned path unconditionally; Keychain experiment proved a rebuilt binary's FIRST Keychain read raises a blocking dialog even under launchd (12 s clicked, 20 s timeout unattended). Guard lessons: worktree removal needs LITERAL paths (shell variables refused by the lexical worktree-scope check) and removals must run ONE AT A TIME (guard's own fetch has a 3 s budget); main-checkout commits need git add and git commit as SEPARATE Bash calls; a second file-mutating agent cannot be dispatched into the main checkout while one runs there.

## Completed
- Retired stale tm-code session record; supervisor sampled and restarted; #8335 filed with stack-sample comment (now on 1.6.6)
- trusty-memory 0.26.2 installed from origin/main and daemon restarted; install worktree removed
- PR #8342 merged (squash 23ce71349): crate-edge rule in bundled rust-build-performance skill v1.1.0; body brought to the nine-field contract
- docs/research/worktrees-best-practices.md committed and pushed to main (545e9af41, b8c8d3df2) after two credential scans, redactions, and clean-clone doc lints
- 1.6.5 re-themed and swept; issues filed: #8341 #8343 #8344 #8345 (1.6.5), #8349 #8350 (new milestone 1.6.7); #8340 #7914 #7771 #8261 #8329 #6868 moved onto 1.6.5
- WorktreeCreate hook dry run done, recorded on #8343 as acceptance 1 and as finding 12 in the research doc
- Keychain experiment done twice (second run launchd-first, conclusive), recorded on #8236 and #4570; scratch artifacts under the session scratchpad keychain-exp/, all launchd/Keychain test state cleaned up
- Worktrees: 33 -> 26 after removing 7 verified-safe trees; pass 1 made local WIP commits on three dirty trees; seven refs passed the pre-push credential scan
- #8236 research and code-analyzer review (NEEDS_IMPROVEMENT, 6-item gap list) done; research comment on the issue

## In Progress
- agent aa35fabf2547f3ef3 (rust-engineer, opus): PR #8334 round 5 for #8261, branch feat/8261-dynamic-build-slots-r5-cont (12 ahead), last seen polling its test-gate log at ~4 h. Scope: critic r3 MEDIUM (unique staging name + stale sibling sweep), LOW (SeedGuard drop guard), rustdoc intra-doc links. When it reports: version-control fast-forwards r4 from r5, posts critic APPROVE + gate evidence on #8334, then OWNER merge authorization needed (rung 5).
- agent a3146f7d48c04803d (rust-engineer, opus): #8341 test-only crate edges, branch fix/8341-test-only-crate-edges, all five instances plus scripts/check_dev_dep_edges.sh guard; last seen waiting on a cargo lock at ~2 h. trusty-review dev-dep lines in crates/trusty-review/Cargo.toml must be read before deletion (comments mention Svelte UI/pnpm).
- agent ae372e12d4a7e8a8b (rust-engineer, opus): #8236 round 3, branch fix/8236-launchd-secrets-r3 from -r2 (04527e4a1). Nine acceptance items incl. atomic plist rewrite, migrate-not-delete into default_store(), registry-driven detection + file mode, bplist00 handling, readers via resolve_key, never block the runtime, Fail-Open Check per error arm, doctor visibility row, tm-secrets skill correction. A refinement was SENT mid-task (time out the caller not the read, single-flight per key, brief negative cache, distinct doctor wording). Rung 5: a code-critic round is owed after its report, then security scan, then version-control opens the PR.
- agent a59a2a77348f1cb4f (version-control, sonnet): worktree pass 2 - push 6 branches + backup/2026-09-21/orphan-abb1027f, remove trees agent-a5224c4956053ce2d, agent-a91bb44d6d82d4905, agent-a7d5b667db800debb, agent-a670606e344ba469a, agent-ac203563274257151, delete run_fragment_check.sh then remove agent-a37b4a5fa3fb6dfad, and a full CI reading for main at b8c8d3df2 (only 4 of 8 workflows confirmed green so far). If this agent is gone on resume, re-check `git worktree list` and `git branch -r` before re-dispatching: some pushes/removals may already have happened.

## Next Steps
- OWNER DECISIONS PENDING: (1) rotate the two credentials named in #8236 (independent of code); (2) yes/no on the stable-signing experiment (needs a self-signed code-signing cert created in the login keychain, removed afterward) - could fix both the Keychain prompt and the #873 Full Disk Access loss on reinstall; (3) Keychain vs 0600 file for #4570; (4) dispositions for kept trees: ci/split-corpus-test-docs-policy (unique unmerged work, 11 files), fix/8205-homing-splash-critic-followup, original feat/8261 (remove after #8334 merges), external codex/search-context-exp tree (not tm's).
- Build-time plan for 1.6.5, in order (also in the research doc): idle-host baseline `cargo test -p trusty-mpm --no-run --timings`; job budget per admitted build handed out with the slot lease (#8344, overlaps #8340); #8341; #8334 then pair slots with fixed-path trees (#8343); dev-profile debug=line-tables-only when no long build is running; test-binary consolidation only after the baseline (#8345); cargo-hakari / nextest archive last. Do not start new cargo builds while three engineers are building: host load average was ~35.
- #8343 design inputs still unverified: ordering of several isolated dispatches in one message (needed before a per-session lease queue), and whether WorktreeRemove fires in an interactive session (it fired in none of three -p runs). ADR-0036 needs a follow-up note.
- Assign the two kept WIP trees: fix/7889-7885-8109-worktree-reclaim (agent-a2571c3170ea45e85, tip 4511b12e7) to #7889; fix/8297-builder-cap-process-detection (agent-aaabe18641a93c7c7, tip 8bfb4d1f2) to #8297. Backup refs (~26 plus today's) need an expiry rule; add it to #8343's reconciliation item.
- Carried from the prior snapshot, untouched today: publish train for the #8267 bumps (trusty-common 0.51.2, trusty-mcp 0.1.6, trusty-installer 0.13.7, trusty-memory 0.26.2) plus trusty-mpm after #8334, owner word required, local-ops via cargo-publish; #8261 items 5-6 then #8329; 1.6.4 tail (#8311 #8269 #8282 #7767 #8301 #8266 #8270 #6285 #6288); builders.max_concurrent is 9 per tm doctor (snapshot said 5) with 4 stale leases past TTL.
- #8021 rollup comment owed for this session (one dated comment, dedupe against earlier ones): tm doctor has no supervisor check and takes >2 min under load; cargo info from inside the workspace reports the local version; worktree-removal refusals append every condition after the failed one and recommend a fleet-wide forced sweep; guard's 3 s fetch budget is tight on a loaded host; tm pr merge calls a squash-merged branch '1 unpushed commit'; no validate-only verb for an open PR body; secret-file guard false-positives on prose mentioning .env/token, and one agent routed around it with placeholders; trusty-search grep ignored its path filter; CLAUDE.md rung-1 doc gates do not match the workflows' path filters and the line cap never covers markdown; the new-docs-to-main exception works only through admin bypass of branch protection; tm-secrets skill documents unshipped verbs; briefs that post text to GitHub must fence it and keep the prompt-feedback request out (a haiku agent posted its own feedback plus an invented figure); removal briefs must say 'sequential' and 'literal paths'; verify delete recommendations with git cherry / merge-base BEFORE presenting them; a pause snapshot misses work the session starts after writing it - resume should also read tracker claims by the same session id; experiments with a human in the loop need run labels the owner can quote back, and any step that grants persistent state must run last.
- After relaunch: call session_context_catchup with tmux_window tm-dogfood:0:@4, check agent survival via the pane status lines before assuming any in-flight agent is dead (three survived a relaunch on 2026-09-14), and re-read `gh pr list` plus `git worktree list` before re-dispatching anything.

## Git Context
Branch: main
Last commit: b8c8d3df2 docs: redact machine and session identifiers, add the WorktreeCreate hook dry run
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-dogfood:0:@4
