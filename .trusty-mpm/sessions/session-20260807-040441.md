# Session Pause - 2026-08-07T04:04:41.456418+00:00

## Summary
1.3.5 leg. main was e97e40e6 at pause (PR #5055 merge may have landed after — VERIFY). tm 1.3.5 signed-installed and behaviorally verified; the [hooks] prompt_context opt-out is LIVE for new sessions (this session still injects — its .claude/settings.json was written by the old binary at launch).

🔴 ELEVEN BACKGROUND AGENTS WERE IN FLIGHT AT PAUSE. They may or may not survive. VERIFY EVERY STATE DIRECTLY via `gh pr list` / `gh issue view` / `git worktree list` — do NOT trust this snapshot for in-flight status. Do not re-dispatch before checking; duplicate dispatch into a live worktree has cost real work before.

STANDING AUTHORIZATION from Bob: "merge the rest when they're green too." QUALIFIED by me and he did not object: green is necessary, not sufficient — rung-5 work also needs its code-critic round, and #5048 is under an active BLOCK with a live fix round. Do not merge a PR while its fix round is still gating.

SEVEN PRs MERGED THIS SESSION: #5033 CLAUDE.md trim (80d2068f), #5039 clippy (62d56fce), #5046 ADR-0031 (e8ea94ef), #5041 hooks opt-out (b69382f6), #5035 tm f + tm ls (b8746f59), #5051 agents doc-fix + default-bind test (e97e40e6), and #5055 semver gate (merge in flight at pause).

ARCHITECTURE DECIDED THIS SESSION — all Bob rulings, all recorded in drawers 576e5dc8 / 878303f8 / ad391346:
- TRANSPORT (ADR-0031, merged, status Proposed): native inter-crate → UDS, same-host only; ALL external comms → one shared HTTP server (console). All-or-nothing across every inter-crate link, never per-crate opt-in. Performance does NOT justify it (measured: loopback HTTP floor 0.26-0.6ms warm; the hook's ~30ms is dominated by process spawn) — the real arguments are port-lifecycle and failure-surface.
- INDEXING: worktrees do NOT embed. BM25+KG only, built AT WORKTREE CREATION. Embeddings live solely on the tm checkout; semantic queries from a worktree route to the base index. "Any variance between is understood as a possibility" — do NOT build staleness detection/labeling/reconciliation.
- #5024 (global embed cache) CLOSED as not-planned; its driving case is gone. Bob stopped the sibling session tm-trusty-tools-05 that was building it.
- DE-DAEMONIZATION: trusty-review daemon REMOVED, ships 1.0.0 (disposable — #5028 measured 782/782 reviews via POST /review, 0 webhook deliveries in 14d). trusty-analyze daemon STAYS (holds real state: in-memory SCIP overlay, warm ONNX embedder) and is NOT 1.0-ready. BOTH webhooks REMOVED, not moved behind console — "a webhook should be part of the calling application." GATED on confirming what code intelligence actually calls.
- Versions are trusty-review 0.11.1 / trusty-analyze 0.8.0, NOT the 0.9.1/0.7.4 I quoted from memory earlier.

🔴 THE PATTERN OF THE NIGHT — FIVE independent instances of "a check reports healthy over real loss", three caught only by adversarial review: (1) palace_reembed still_missing_ids tests vector-KEY presence, not retrievability — the sound test is the self-query sweep, 111/114 at rank 1-5, 3 at NO rank; (2) #5048 skips a palace on doc_count >= total, a COUNT standing in for a SET; (3) #5048's cap test passes with spawn_gate DELETED, and its SIGTERM test is #[ignore]d while CI omits --include-ignored; (4) trusty-agents guard tests all pass with 0.0.0.0 reinstated; (5) the semver gate with --default-features exits 0 over #4088's actual break because default=[] hides it.

🔴 STALE DOC COMMENTS IN A SECURITY FILE COST A FULL FALSE REPORT. trusty-agents' 0.0.0.0 was fixed 7 months ago by #3341; auth.rs:3, auth.rs:93 and the threat-model row still asserted the vulnerable state as fact. I relayed it to Bob as a live LAN exposure. #5051 corrects it. SLD lint catches stale TEST pointers; nothing catches a doc comment asserting an abandoned security posture.

MEMORY RETIREMENT (#4834) NEEDS THREE STEPS, NOT TWO — I told Bob two, twice. (a) land #5013; (b) WIRE AND RUN unalias() — exists in hnsw_store.rs, ZERO call sites, never run against a live palace; (c) re-embed. Nothing was deleted. Backup at /Users/masa/memory-backup-20260806-223001.tar.gz, 121 files.

## Completed
- 7 PRs merged: #5033, #5039, #5046, #5041, #5035, #5051, #5055(in flight at pause)
- tm 1.3.5 signed-installed from b8746f59, daemon bounced (bootout THEN bootstrap, pid 879, ppid=1, supervised:true), hook suppression verified before/after on a real tm sessions start
- 9 issues filed: #5036 #5037 #5038 (memory hook precision), #5042 #5043 #5044 (memory integrity), #5045 (search index coverage + doctor blind spot), #5049 (SCIP loss), #5050 (no semver gate), #5052 (SSE auth exemption), #5056 (MCP scip surface)
- 2 closed as obsoleted: PR #5026 (daemon-only in_flight fix, branch preserved), issue #5024 (global embed cache)
- ADR-0031 authored and merged — transport by purpose
- Root cause of the memory hook's poor precision established: recall is dense-vector ONLY; BM25 fusion exists in code (fuse_bm25_into_recall, RRF k=60) but has NEVER EXECUTED — TRUSTY_BM25_DAEMON=1 set in no plist/installer/shipped path, zero BM25 index dirs across ~99 palaces
- serve --stdio proven to be a PROXY to POST /rpc since #1078, not an independent path — so the hook's missing fusion was a ROUTING difference, never transport

## In Progress
- 🔴 VERIFY ALL OF THESE DIRECTLY — 11 agents were live at pause
- PR #5048 BM25 backfill — code-critic BLOCK (1 CRITICAL: count-vs-set skip predicate; recovery claim false — spawn_startup_backfill is the only caller; cap test passes with spawn_gate deleted; CI red on check_test_pointers). Fix round live in .worktrees/bm25-backfill. DO NOT MERGE until re-reviewed.
- PR #5054 SCIP persistence — critic WARN, 1 HIGH. Fix round live in .worktrees/analyze-scip. The HIGH: daemon refuses to start on a corrupt scip_overlays.redb, and the stated justification is FACTUALLY WRONG — FactStore does not recreate empty, it QUARANTINES (renames to .v2-incompatible) and logs. Recommended: quarantine on format class, refuse only on transient. Then re-review.
- PR #5055 semver gate — critic APPROVE (verified by downloading real crates.io tarballs AND mutating the self-test 3 ways). Merge agent dispatched at pause; VERIFY whether it landed.
- PR #5013 HNSW id-aliasing — rebase onto current main + wire unalias() to a callable surface, in .worktrees/hnsw-alias. Its earlier CI reds were INFRASTRUCTURE (all 8 died at 'Getting action download info / Service Unavailable' pre-checkout across 3 regions); Test never ran, so there is no green either. Needs critic after.
- #4595 PM instruction restructure — .worktrees/pm-instructions. Target derived from claude-mpm's ~11k self-added benchmark, NOT #4595's own ~11-13k posted ceiling (which lands ~60k and misses under-50k). Told to STOP and report the gap rather than cut a genuinely per-prompt rule to hit a number.
- #4715 index-not-ready contract — .worktrees/index-not-ready. JSON-RPC 425-equivalent, not HTTP 404; error payload carries the grep/find fallback. Index-CREATION timing deliberately left out of scope.
- Index-on-worktree-creation — .worktrees/index-on-create. Redirected twice: BM25+KG only, then base-checkout-embeds. Told to MEASURE a BM25+KG-only build before choosing sync-vs-async, and NOT to edit crates/trusty-search (sibling session was in there).
- MCP services audit — all trusty-* servers, does each actually work (registered / reachable / tools enumerate / results CORRECT / errors honest). New 1.3.5 goal.
- De-daemonization implementation — scoped but NOT dispatched. See #5028.

## Next Steps
- FIRST: `gh pr list --state open` and `git worktree list` before touching anything. Eleven agents were live; several may have completed, died, or pushed after this snapshot.
- Merge queue under Bob's standing authorization, each gated on its OWN critic round: #5055 (verify it landed), #5054 (after HIGH fix + re-review), #5013 (after rebase + critic), #5048 (after BLOCK fixes + re-review).
- 🔴 BOB MUST DO: add 'Public API / SemVer' to branch-protection required checks. Needs admin rights. Until then the gate is ADVISORY and a PR can merge red against it — the exact shape this session spent the night removing.
- Confirm what code intelligence actually calls before removing trusty-review's webhook — Bob says it uses the webhook, #5028 measured ZERO deliveries in 14 days. Removal is gated on this.
- Memory retirement stays BLOCKED until unalias() is wired AND run AND a re-embed follows. The self-query sweep (each drawer queried by its own first 400 chars, top_k 25) is the only sound gate — NOT still_missing_ids. Sweep script at scratchpad/sweep.sh.
- Open, unfiled, judged not worth a ticket: semver exclusions TSV has no mechanical check that a listed feature genuinely fails to build; semver diff-scoping keys on src/** so a public-API change arriving via a Cargo.toml dep bump would not trigger it (different shape from #4088).
- Under-50k needs the FULL stack: #4595 restructure + MEMORY.md retirement + skills catalog filtering + output-style trim. Any one missing and it lands ~55-60k.

## Git Context
Branch: main
Last commit: 8ab2c147a docs: evaluate migrating memory_core storage from redb to Lance
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-dogfood:0:@5173
