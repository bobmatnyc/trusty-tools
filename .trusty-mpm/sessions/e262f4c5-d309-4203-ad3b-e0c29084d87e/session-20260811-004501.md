# Session Pause - 2026-08-11T00:45:01.660734+00:00

## Summary
EIGHTEEN PRs MERGED, every SHA verified as an origin/main ancestor with branch deletion confirmed. origin/main = `a92bb941`. Rebuilt and installed from it; six daemons bounced and healthy.

ONE PR OPEN: **#5366** (CI Test-job sharding + cache fix), in a fix round after a code-critic BLOCK. Its prerequisite #5378 (trusty-memory test isolation) HAS LANDED, so nextest is unblocked. Worktree `.claude/worktrees/ci-nextest` holds that work — DO NOT prune it.

🔴 PRUNE DELIBERATELY DISABLED on this pause (`prune_worktrees: false`) because #5366's worktree and the install-source worktree must both survive.

🔴 FOUR OWNER RULES SET TONIGHT ARE NOW ON MAIN — do not re-litigate them:
1. **No issue churn.** If the fix ships this session, no issue; the PR is the record. "File and fix" is ONE instruction that collapses to "fix, and make the PR self-describing."
2. **No standalone SLOC-cap fix** (#5373). The split ships inside the PR that next adds to that file.
3. **No borrowed-metaphor jargon** (#5372) — "load-bearing" is the named instance; say the mechanism instead. In all three output styles + both BASE-AGENT.md copies.
4. **Test scope widens by stage** (#5377), in the `tm-workflow` skill: developing → new/changed code only; merging → the full test files that changed; publishing → the full corpus.

🔴 BRANCH PROTECTION CHANGED: `Test` was REMOVED from `main`'s required status checks (owner-authorized), so a pending `Test` no longer blocks a merge. Required contexts are now exactly `Format check`, `500-line file-size cap`, `Clippy`, `trusty-search daemon smoke test`, `MSRV check (1.94)`, `strict: true`. A FAILING check still blocks. Rollback: re-add `{"context": "Test", "app_id": 15368}` to the `checks` array.

INSTALL STATE: 13 binaries across 9 crates rebuilt from `ba2a6595` in worktree `.claude/worktrees/rebuild-install-20260810` (MUST PERSIST — binary_provenance reads it). `tm`/`trusty-mpm` re-signed Developer-ID via `tctl sign`; a plain `cargo install` silently reverts them to ad-hoc and drops the App-Data TCC grant. `tm doctor --quarantine-mcp` confirmed live.

trusty-search PID 1517 left running deliberately — see the dedicated drawer. Its launchd job IS armed and takes over automatically at the next natural exit; "nothing restarts it" was MY error and is false.

## Completed
- 18 PRs merged, all ancestry-verified: #5341, #5352, #5326, #5353, #5367, #5019, #5025, #5333, #5359, #5368, #4985, #5008, #5027, #5378, #5371, #5372, #5373, #5377, #5379
- #4978 CLOSED as verified-duplicate of #5352 — diffed head against the merge, four of seven files byte-identical, remainder pure additions plus a SLOC-cap extraction; lossless
- #5367 restored main's compile — main had not built since #5345 broke tests_4715.rs with E0369; two agents confirmed independently
- #5378 fixed test isolation: 64 tests (not the 17 CI showed) depended on a sibling seeding the process-wide SHARED_EMBEDDER cell. Measured with TRUSTY_EMBEDDER_INIT_TIMEOUT_SECS=0; 655/64 before, 719/0 after under real nextest
- Branch protection: `Test` removed from required contexts via the narrow required_status_checks endpoint, passing `checks` (not `contexts`) so the five survivors kept app_id 15368 pinning
- Rebuild + install: 13 binaries / 9 crates from ba2a6595; six daemons bounced healthy (analyze 0.9.0, memory 0.22.0, mpm 1.3.6 supervised, review 0.14.0, console 0.5.0)
- Scrollback root-caused: NOT history-limit (correctly 100000 everywhere) but tmux alternate-screen. #5151 shipped the fix opt-in with the bug as default; #5368 flips it. Third BASE-AGENT.md copy in trusty-code found by the drift gate
- CI linker proposal MEASURED AND REJECTED (#5379): CI already links with rust-lld; `ld --version` reports /usr/bin/ld which rustc never invokes. Link is ~12% of build; mold 4% worse; GNU ld would cost +344s
- trusty-search orphan diagnosed and left running; on-disk binary re-signed Developer-ID so an eventual natural handover keeps its FDA grant. Registry snapshot taken (107 entries)

## In Progress
- PR #5366 — CI `Test` sharding (nextest hash-partition ×4) + GitHub Actions cache-scoping fix. Head was `ccbe1807`; agent a5c5b007aee5fa8c6 owns it, worktree `.claude/worktrees/ci-nextest`. Critic BLOCKed it for a CRITICAL: `ci.yml:544` called `scripts/ci-preseed-fastembed.sh`, a file the author INVENTED — origin/main has 209 lines of inline pre-seed logic it replaced without ever reading. Restored verbatim (`identical to origin/main: True`), HF_TOKEN back, both error arms demonstrated. MEDIUM (duplicated shard count) fixed via `strategy.job-total`. Its measured numbers — Test 1370-1432s → 1099s cold, execution 420-454s → 148-165s — were taken on a run where the 17 HuggingFace-dependent tests happened to get their model, so treat them as the SHAPE of the win, not a green-suite result. Re-measure now that #5378 has landed. NEEDS a critic re-review before merge.

## Next Steps
- FIRST: ListAgents + `gh pr view 5366` before dispatching anything — background agents survive a pause and re-dispatching a duplicate is an error made twice in this repo today
- #5366: confirm the fix round pushed, re-measure Test duration now that #5378 landed, then dispatch code-critic for a re-review (rung 5). Merge only on APPROVE
- TWO OWNER DECISIONS OWED, neither blocking: (a) file the trusty-search retry-churn issue? Its launchd job retries every 10s forever against a lock it can never win — ~20,800 failures, stderr.log at 238 MB and growing. That is a code fix (backoff / defer on a foreign lock holder), not an ops action. (b) Keep or close #5364, the alternate-screen issue filed then fixed in the same session — it violates the no-issue-churn rule set an hour later; #5372's PR closes it on merge either way
- DO NOT prune `.claude/worktrees/ci-nextest` (#5366's live work) or `.claude/worktrees/rebuild-install-20260810` (install source; binary_provenance reads it)
- Scrollback in EXISTING panes is unchanged — the fix applies to sessions created after the install. Reach history with `prefix` `[` then Page Up; the mouse wheel goes to the TUI, which has SGR mouse tracking on
- `/private/tmp/.mcp.json` still present (1179 bytes, mtime Aug 8 13:44), read by every session with cwd under /tmp. Clear it deliberately with `tm doctor --quarantine-mcp /private/tmp/.mcp.json` then `--yes` — read it first, it declares the operator's apex and duetto-* servers alongside the framework ones
- SEMVER DECISION STILL OWED, inherited and unresolved all session: `check_semver.sh --crate trusty-mpm` reports BREAK — constructible_struct_adds_field on Delegation.isolation (core/agent.rs:275), from #4480, already on main. At 1.3.6 that needs the MAJOR position. preflight-publish.sh CHECK 5 is the hard stop at whatever release ships it
- ADR-0042 (#5362) MERGED as Proposed, not Accepted. Its Q4 resolved AGAINST the original design: deleting the MCP injectors breaks `tm launch` and `tm connect`, which pass --setting-sources project,local and thereby suppress the user-scope mcpServers map. The ADR now carries that as a precondition with two candidate remedies — picking one is an owner design call and gates the 1.3.6 MCP work

## Git Context
Branch: main
Last commit: 695f1b16 chore(trusty-analyze): bump 0.8.0 -> 0.9.0 for release (#5340)
Uncommitted changes: 3 file(s) changed

## Tmux Window
tm-dogfood:0:@230
