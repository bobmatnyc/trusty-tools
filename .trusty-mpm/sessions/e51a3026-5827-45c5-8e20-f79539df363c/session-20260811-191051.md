# Session Pause - 2026-08-11T19:10:51.389548+00:00

## Summary
Session tm-tga (resumed from 844256bf snapshot). SIX PRs MERGED, THREE AGENTS IN FLIGHT, ~30 ISSUES FILED, and a large architecture design converged for a new `trusty-audit` crate. The tga RELEASE IS DELIBERATELY HELD.

🔴 PRUNE DELIBERATELY DISABLED (prune_worktrees: false). THREE AGENTS ARE LIVE IN THEIR OWN WORKTREES at pause and pruning could touch them. These must survive regardless: `.claude/worktrees/feat-5250` (PR #5419's parked work — the branch IS pushed, so a prune may not see it as dirty), `.claude/worktrees/ci-nextest`, `.claude/worktrees/rebuild-install-20260811` (NOTE THE DATE: -20260811, NOT -20260810; earlier snapshots carry the stale name).

🔴 RE-DERIVE origin/main. It moved ~10 times this session and sibling sessions merge without warning. It was `5ed6c944a` when last read. NEVER trust a SHA carried forward.

⚠️ The main checkout holds another session's uncommitted work: modified `crates/trusty-code/src/assets/agents/ticketing.md`, `crates/trusty-mpm/src/assets/agents/ticketing.md` (BOTH ~152 lines changed), `tm-issues-prune.md`, `tm-ticketing.md`, `bundle_tests.rs`, `.gitignore`, plus untracked `.agents/`, `.codex/`, `.claude/settings.json`. DO NOT commit, revert, stash, or clean them.

=== 🔴 THE ONE BOTTLENECK, and the chain behind it ===
PR #5496 (semver gate ANSI fix, closes #5500) gates EVERYTHING tga. Chain: #5496 merges → #5458 merges (carries tga 2.16.0→2.17.0 + trusty-review 0.14.1→0.15.0) → the `PR version bump vs crates.io (#4421)` gate stops failing every tga src PR → #5485 and #5486 merge → #5501 → advisory mode.
WHY: main carries a PUBLISHED tga 2.16.0 with unpublished src changes accumulating. Any tga `src/**` PR fails version-parity until the 2.17.0 bump lands.

=== MERGED THIS SESSION, every squash verified as an origin/main ancestor ===
#5445 `d376fc00e` (another session), #5450 `5e471b50b`, #5412 `95c815378` (another session, WITHOUT update-branch — see the gap below), #5434 `3b1d378bc`, #5472 `5ed6c944a`.
🔴 THE #5412 GAP, CLOSED EMPIRICALLY: it was squash-merged onto a moved main without re-verification; its CI green predated #5450, which also touched trusty-common. A QA run at `3b1d378bc` from a fresh worktree proved main green — trusty-common --features memory-core 1015 passed, trusty-memory 708 passed, retract_triple 7/7, spawn_retry:: 11/11, workspace check + clippy + line-cap clean.

=== OWNER DECISIONS THIS SESSION — do NOT re-ask any of these ===
See palace drawers, all in room `status`. Key fact_keys: `handoff:architecture/current`, `repo:semver/policy`, `audit:credential/placement`, `semver-gate:work/sequence`, `release:tga-2.17.0/authorization`, `pr:5458/authorization`, `ws:tm-tga/standing-authorization`.
1. ALL CONTRIBUTOR PROFILING LIVES IN tga — a domain boundary, not a file move. Epic #5468, children #5463-#5467. Reverses a deliberately documented decision (`docs/trusty-git-analytics/spec/PRD.md:258-261` says tga is "data supply only").
2. INVERT FIRST, HOLD THE RELEASE — tga 2.17.0 / trusty-review 0.15.0 wait for the inversion.
3. SEMVER POLICY: majors need owner approval; minor/patch must work technically; override semver mechanics. PM call inside it: per-PR gate ADVISORY, preflight CHECK 5 stays BLOCKING with an explicit override.
4. #5501 BEFORE advisory mode — because a quiet gate decays invisibly, so the self-test becomes the only thing that would notice.
5. THE AUDITOR CLIENT: a self-contained Tauri app, macOS arm64 only, small zip (signed .app + README + readable config), which DOWNLOADS pinned tga/trusty-analyze/trusty-review/gh at runtime. It is "really an installer runner" — install leg is trusty-installer's as a LIBRARY; the workflow leg is not.
6. `trusty-audit` MILESTONE 62: crate exists + dependency downloads + repo selection. HEADLESS FIRST (library + thin CLI); 🔴 EVERY FEATURE MUST BE CLI-TESTABLE, PERMANENTLY — no GUI-only features, ever.
7. Bare `trusty-audit` with no arguments is the entry point.
8. THE PACKAGE SAVES STATE — amends the earlier "must stay open, restart on failure" choice; #5494 promoted from deferred.
9. 🔴 CREDENTIAL INVARIANT: the OpenRouter key is a LITERAL VALUE in the INPUT config (goes TO them), and NEVER in the OUTPUT manifest (comes BACK). Reverses #5470's key_env design; the input-vs-output artifact distinction is REINFORCED.
10. NO Provisioning API — the owner mints a spend-capped inference key by hand per engagement.

=== 🔴 UNRESOLVED SECURITY ITEM ===
The owner pasted a LIVE OpenRouter inference key (spend-capped, NOT a provisioning key) into this transcript in plaintext. Rotation was advised and is UNCONFIRMED. The value was never written to any file, issue, PR, or drawer and must never be. The design now places a key in a config handed to clients, so rotation matters before the first engagement package is generated.

## Completed
- PR #5450 merged (squash 5e471b50b, closes #5446) — ETXTBSY spawn-retry consolidated. 30 pass/0 fail, head unchanged between CI check and merge.
- PR #5434 merged (squash 3b1d378bc, closes #5399) — WordNet POS head-noun selection. Needed update-branch TWICE as main moved mid-sequence. Critic APPROVE.
- PR #5472 merged (squash 5ed6c944a, closes #5417 + #5461) — website changelog tests now DERIVE assertions instead of pinning content that ages out. Fixed the shape, not the symptom.
- Main verified green at 3b1d378bc by an independent QA run from a fresh worktree, closing the #5412 untested-tree gap empirically rather than by argument.
- Epic #5468 filed with children #5463-#5467 — contributor profiling moves to tga. Plus #5469 (trusty-review's crate::llm duplicates trusty_common::inference — two LLM stacks in one workspace).
- Epic #5473 filed with 12 children (#5474-#5484, #5487) — the audit handoff package, then restructured twice as the owner's design converged.
- #5477 restructured into a sub-epic with children #5495/#5497/#5498/#5499 plus standalone dependencies #5491/#5493/#5494.
- Milestone 62 created (trusty-audit core) with #5502 (crate scaffold) filed and #5491/#5495/#5476/#5487 assigned. #5497/#5498 deliberately DECLINED as Tauri-UI work out of a headless milestone.
- Defects found that nobody was looking for: #5500 (semver gate ANSI-blind, reporting NO VERDICT on every CI run), #5501 (the gate's self-test cannot run on gate-fix PRs), #5471 (ticketing agent's umbrella-label clause mislabels every tga issue trusty-mpm), #5475 (gh invoked from 5+ sites with no common entry point), #5460 (tga ```ignore doctest fences forced to run under --include-ignored), #5461 (website test asserts content that aged out of the DETAILED_RELEASES window).
- #5460 fixed in PR #5485 — both fences became `no_run` (keeps rustdoc type-checking) with a hidden `fn main()` wrapper for the bare `?`.
- #5486 opened and critic-APPROVED — the profiling data/orchestration port into tga. 1135 tests pass; the port DROPPED trusty-review's duplicate resolve_db_path rather than porting a second DB-path resolver into the crate that owns the first.
- PR #5496 opened and critic-APPROVED — root cause was NOT exit-code misreading (my hypothesis was wrong) but CARGO_TERM_COLOR=always splitting `Checked` from its trailing space, so verdict_computed matched nothing on EVERY run.
- #5250's stale `blocked` label cleared, with a comment recording that tga stays on 2.x.

## In Progress
- 🔴 THREE AGENTS LIVE AT PAUSE — all will be DEAD on resume. Verify each state directly via gh, never from this snapshot. (1) a4a28952 rust-engineer on PR #5496, broadening the CSI-strip regex per the critic's MEDIUM (private-mode sequences like ESC[?25l survive the current pattern). Last head 597a1e42d; a further commit may exist. It must add a self-test case proving red-before-green AND re-prove the gate still REJECTS a real break. (2) ad57eee1 rust-engineer on #5502, scaffolding the trusty-audit crate. (3) a6ca5370 rust-engineer on #5491, the pinned fail-closed download entry point in trusty-installer.
- PR #5496 — critic APPROVE at head d17f52279 with ONE MEDIUM dispositioned fix-here. The newer commit 597a1e42d is comments-only (#5500 re-attribution), so the verdict still applies. MERGE IT once the CSI hardening lands. 🔴 Its own self-test did NOT run on its own CI (that is #5501), so the LOCAL 18/18 is the only proof the fix works.
- PR #5458 — critic APPROVE (round 2), owner-authorized to merge, blocked ONLY by #5496's gate defect. Carries both version bumps. Head was c180d123 before the last update-branch.
- PR #5485 and #5486 — both gate-clear and critic-reviewed, blocked by `PR version bump vs crates.io (#4421)` until 2.17.0 lands on main via #5458.
- PR #5419 — parked draft, now UNPARKED by the semver policy but not yet acted on. Its worktree MUST survive.

## Next Steps
- FIRST: ListAgents + `gh pr list` + re-derive origin/main. Three agents were live at pause and are now dead; do not trust any state in this snapshot.
- Merge PR #5496 once the CSI hardening reports (owner authorized --admin merges for this session's work). Then merge #5458. Then #5485 and #5486 unblock — they will need update-branch since main will have moved.
- Then #5501 (make check_semver_selftest.sh run on gate-fix PRs). 🔴 NOT a one-liner: scripts/check-ci-helpers-selftest.sh:628 asserts the `have_work == 'true'` condition string appears exactly 5 times, so changing it drags that gate along.
- THEN advisory mode — per-PR semver gate advisory, preflight CHECK 5 blocking with an explicit override. Owner-sequenced AFTER #5501.
- 🔴 BRANCH PROTECTION MEANS NO PR EVER MERGES WITHOUT --admin. required_approving_review_count=1, every PR authored by bobmatnyc = the authenticated account, GitHub forbids self-approval. A code-critic APPROVE is NOT a GitHub review — `gh api .../pulls/<n>/reviews` returns [] on critic-approved PRs. Expected; not a reason to stop. The owner authorized --admin for this session's batch.
- 🔴 AN ADMIN MERGE MAY ONLY LAND AT A HEAD WHOSE CI HAS FULLY SETTLED, and the head must be RE-READ IMMEDIATELY BEFORE MERGING. Sibling sessions merge into this repo without warning; that check is what kept #5450 and #5434 honest and its absence is what created the #5412 gap.
- Continue milestone 62 after #5502 and #5491 land: #5495 (download+verify pinned tools), #5476 (gh detect-and-download), #5487 (repo-discovery endpoint).
- Epic #5468 (profiling inversion) continues: #5464 → #5465 → #5466 (🔴 the REMOVAL from trusty-review must NOT merge until #5463-#5465 are live, or there is a capability gap) → #5467 docs sweep.
- THE RELEASE STAYS HELD. tga 2.17.0 + trusty-review 0.15.0 wait for the profiling inversion. Publishing is delegated to local-ops via Skill(skill="cargo-publish"); verify by REGISTRY READ (cargo search + crates.io API), never by `cargo publish` exit code. Publish order is a CONSEQUENCE of the dependency edge, not a fixed fact — re-derive it from the manifests at release time.
- 🔴 REMIND THE OWNER TO ROTATE THE PASTED OPENROUTER KEY. Unconfirmed, and the design now puts a key in a client-facing config.
- Two questions recorded IN issues, deliberately unresolved: #5464's seam choice (trusty-review's review_diff MCP tool vs a one-shot CLI, if a seam is needed at all beyond trusty_common::inference), and #5465's auth question (tga's GitHub client is PAT-based; trusty-review's issue-upsert uses App JWT).
- OPERATING FACTS THAT KEEP BITING: there are 18 `scripts/check_*.sh` gates, not 4 or 9 — enumerate them, never remember them. `cargo test -p trusty-common` WITHOUT `--features memory-core` runs ZERO memory-core tests and reports success. `#4480` blocks a second file-mutating agent on the same git HEAD — dispatch with isolation: "worktree".
- Consider publishing the trusty-audit architecture as an artifact page — ~20 decisions accumulated, several reversing earlier ones, and the sequence matters for anyone picking it up.

## Git Context
Branch: main
Last commit: b9b99d316 docs: add website link to README (#5330)
Uncommitted changes: 10 file(s) changed

## Tmux Window
tm-tga:0:@540
