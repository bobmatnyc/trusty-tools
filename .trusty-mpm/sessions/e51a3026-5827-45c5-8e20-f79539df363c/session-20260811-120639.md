# Session Pause - 2026-08-11T12:06:39.832180+00:00

## Summary
Session tm-tga, milestone #47 (tga AUDIT — report quality and hardening). THREE ISSUES CLOSED, THREE FILED, TWO CRATES PUBLISHED, ONE PAGE PUBLISHED. ZERO AGENTS IN FLIGHT at pause.

origin/main = 1dfb28731156e0fdd89bbb5caa5bb200b1f91a1b.

🔴 PRUNE DELIBERATELY DISABLED on this pause (prune_worktrees: false). `.claude/worktrees/rebuild-install-20260810` is another session's install source (binary_provenance reads it) and I did not want a tidy-up to reach it. `.claude/worktrees/ci-nextest` held #5366, which has since MERGED, so it may now be prunable — decide that deliberately, not as a side effect.

RELEASE LIVE ON crates.io, verified by `cargo search` AND the crates.io API, never by `cargo publish` exit code: tga 2.15.0 (tag tga-v2.15.0) and trusty-review 0.14.1 (tag trusty-review-v0.14.1), both `published_by: bobmatnyc`, `yanked: false`, both installed and version-checked.

🔴 PUBLISH ORDER IS SETTLED — STOP RE-DERIVING IT. tga FIRST, then trusty-review. The edge is one-directional: crates/trusty-review/Cargo.toml:117 declares `tga = { workspace = true, optional = true }` under the default-on `profile` feature; tga has no dependency back. Proven at publish time — trusty-review's build resolved tga 2.15.0 from the live registry.

🔴 TWO GATE-INTEGRITY RULES LEARNED THE HARD WAY TODAY, both now binding on every merge verification:
1. A SKIP IS NOT A PASS, and `gh pr checks` exit 0 cannot distinguish them. Tally buckets explicitly.
2. READ THE Test JOB'S DURATION. Real runs here are 12-16 minutes; under ~60s on a Rust diff is a tell. But before concluding "untested", run `gh run list --commit <sha>` — TWO runs for one SHA means a webhook race (#5407) and the real evidence is the longer run. I got this wrong once today and had to correct it.

## Completed
- #5312 CLOSED via PR #5387, squash 11ba79f0 — refuted findings now marked in inline PR comments. EPISTEMIC-KEYED (VerifyOutcome::reader_caveat, exhaustive match, no `_` arm), which is the structural difference from PR #4212's subject-keyed fix that let this class recur.
- #5323 CLOSED via PR #5390, squash 31ea3250 — analyze-adapter findings scrubbed of credentials before the DD report renders. Took a code-critic BLOCK and a fix round: round 1 passed three wiring tests while leaking on every real run, because the tests asserted at model.repositories[0].metrics and the render path went through merge_investigation_prose.
- #5249 CLOSED via PR #5403, squash 1fec14a4 — agentic detection catch rate 47.70% -> 91.04% on this repo's own 2434 commits. Took a critic WARN and a combined descope+fix round.
- RELEASE CUT: PR #5410 (changelog assembly, squash 1dfb2873) merged, then tga 2.15.0 and trusty-review 0.14.1 published and registry-verified.
- #5391 FILED — fifth ETXTBSY instance, write_fake_tmux at crates/trusty-mpm/src/core/tmux_tests.rs:89, child of epic #3451.
- #5405 FILED — board/ticketing data never reaches the DD report. src/report/ has zero references to work_items, linear_issues, fact_ticket_transitions, or fact_jira_comment_detail.
- #5407 FILED — CI: a same-SHA PR-edit run can report Test SUCCESS without running it, masking the real run's result.
- PUBLISHED the tga audit product page as a private artifact: https://claude.ai/code/artifact/8331fcef-3efb-4c70-bb97-7de4600b1f57
- BOARD HYGIENE: six stale `blocked` labels cleared from milestone #47; #5242 correctly KEPT (real unmet dependency on #5241).

## In Progress
- NOTHING is executing. Zero agents in flight, no open PRs from this session, working tree clean.
- ONE UNVERIFIED ITEM: `.github/workflows/release.yml` was triggered by the two tag pushes (binary builds, GitHub release, homebrew bump) and was NOT waited on. Verify it went green — this is the only loose end from the release.

## Next Steps
- FIRST: ListAgents + `gh pr list` before dispatching anything. Background agents survive a pause, and re-dispatching a duplicate is an error already made twice in this repo.
- VERIFY release.yml went green for tags tga-v2.15.0 and trusty-review-v0.14.1. Binary builds, GitHub release, homebrew bump.
- 🔴 OWNER REQUIREMENT PARTIALLY UNSHIPPED, no issue covers it: the owner asked 2026-08-08 that the agentic footer detector be CONFIGURABLE so a target org's house footer can be added without a release. He then declined the tga 3.0.0 major that `constructible_struct_adds_field` on Config would have forced, so 2.15.0 ships the widened builtin set ONLY. The catch rate is fixed; configurability is not. That is #5249's acceptance criterion, partially unmet, and it needs either an issue or an explicit owner decision to drop it.
- PORT THE AUDIT PAGE to the site. Owner ruled: EXTEND routes/tools/trusty-git-analytics/+page.svelte, do NOT add a 7th TOOLS entry — tools.test.ts:29 pins TOOLS.length to exactly 6. Source file is scratchpad/tga-audit-src.html (edit that, not the 188KB inlined build). Convert to Tailwind + ToolPage.svelte conventions. `pnpm test` runs BY HAND from inside website/ (#5200, not in CI).
- CERTIFY PAGE deferred until certify exists. Owner chose audit-only. Certification is designed (DOC-67: public badge verified by reproducibility, non-disclosing diffs) but unbuilt — blake3 is declared in tga's Cargo.toml with zero call sites and the schema has no snapshot concept.
- MILESTONE #47 AT 11 OPEN. Report-trust group is finished except #5306 (DOC-67 §5 sweep order contradicts §8 and the shipped run_full_sweep order, docs-only). Remaining: #5302 (repo-selection TUI, rung 6, largest item), #5313 (four tga test modules mutate std::env unsynchronized), #5358 (publish-dry-run-order.sh misses optional deps), velocity chain #5240 -> #5241 -> #5242, agentic quartet remainder #5250/#5251/#5252, plus newly-filed #5405.
- AGENTIC QUARTET: #5250 (an `unknown` AgenticMode state distinct from `none`) is the natural next one — the engineer confirmed MarkerSet::detect returns None from exactly one arm, so the branch point exists. #5251 needs a NEW MIGRATION on fact_weekly_engineer. #5252's fabricated test pointer is ALLOWLISTED, not merely unnoticed — check_test_pointers.sh resolves 23631 citations with 0 dangling.
- TRACKED, NOT FILED — mixed-vintage databases. No schema marker records which detector version classified a row, and `tga collect` dedups per (repo, ISO-week) so already-collected weeks are never re-walked. A report after upgrade blends correctly-classified new commits with under-counted historical ones into one agentic_pct with no signal to the reader. code-critic rated it HIGH but separable; disclosed in the changelog, unmitigated in code. Worth its own issue once #5250's `unknown` state lands, since both touch the same gap.
- TRACKED, NOT FILED — the agentic corpus test is blind to 5 of 7 tool labels (zero real Devin/OpenHands/Aider/Copilot/Cursor markers exist in this repo's history). Disclosed in the test output and covered by synthetic unit tests in ai_markers.rs. Worth revisiting if tga is ever run against a corpus that actually contains those markers.
- DO NOT re-derive the tga<->trusty-review publish order. It is tga first; see the Summary.

## Git Context
Branch: main
Last commit: b9b99d31 docs: add website link to README (#5330)
Uncommitted changes: 3 file(s) changed

## Tmux Window
tm-tga:0:@540
