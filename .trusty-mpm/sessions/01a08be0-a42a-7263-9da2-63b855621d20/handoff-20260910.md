# Overview

# Trusty Agents handoff to tm — 2026-09-10

## Summary

The user requested all work be documented in issues and milestones for handoff to tm. Stop new delivery work in this session after committing/pushing the checkpoint. The user previously authorized tracking, pushing, merging and installing the related work. That authorization does not waive review/testing gates. The follow-on Agents batch is NOT ready to merge or install.

## Completed

- Assessed crate boundaries (#7359): retain trusty-agents and trusty-agents-common; common has other consumers. trusty-agents-local is a redundant launcher, but this task does not remove it.
- Foundation PR #7331 merged: dfb2d6a79f242867b003fe2799453545fe08cc78, reviewed head 8d03d49e4c0fd7e9835c4f4202683e17ac2b59ec. This includes the main merge of Slack commit; no independent Agents changes in that merge. Broader #4531/#7329 remain open.
- Slack fix PR #7392 merged at 6867de60805f6172aac633e850977c8712f5f4b5 and installed using cargo install from a clean merged checkout. #7391 CLOSED after installed verification. Installed slack-mcp SHA256 d0d374ac17f64086d824a31317cb678f0fb034955f0e611ab7eccba8b16f3b63; handshake 0.1.0/22 tools; channel list, user list and channel history limit 1 each returned one result. Existing processes/config preserved; owned test child stopped.
- Codex's Slack and Notion access uses already authenticated app plugins, independent of local Trusty connectors. Read-only identity checks succeeded. No messages or Notion pages were written.

## In Progress

Canonical Agents worktree: /Users/masa/trusty-mpm-projects/bobmatnyc/trusty-tools/.claude/worktrees/assistant-memory-project-settings

Branch: codex/assistant-memory-project-settings, originally based on b86f7ac0baae83f0221b188eaa4550be99507095 (pre-squash #7331 head). Git owner must append final checkpoint commit, push and draft PR links. Do not mistake the foundation merge for this batch being merged.

Accepted scope and canonical tracking:

| Issue | Scope |
| --- | --- |
| #7359 | Three-crate architecture assessment only |
| #7360 | trusty-memory-only facts, remember/write aliases, assistant namespace, optional cross-palace reads, direct memory API, effective permissions |
| #3931 | Persistent assistant projects in Settings and shared typed Settings/Concierge operations |
| #4358 | Arbitrary readable non-Git directories as chat projects; stable installed desktop root |
| #4283 / #4531 | Automatic entity extraction, protected assistant knowledge, job windows, publication/retry/withdrawal |
| #7361 | Assistants delegate platform configuration/health to Concierge |
| #7370 | Image and tabular clipboard attachments, standard FE libraries, authoritative preparation API, actual provider bytes and durable history/assets |
| #7379 | Search watcher per-index admission and tombstone handling |
| #7389 | Connector verification umbrella; Slack search lacks user token; no Trusty Notion configured in inspected registries |
| #7329 | Foundation delivery portion recorded with #7331 |

Ticketing is reconciling final milestone coverage. Prior #4283/#4531 claims could not previously be proven stale; do not silently take over an active peer. #3931/#4358 had documented milestone exceptions that must be reconciled with the latest user instruction, preserving history.

### Implemented but not fully verified or shipped

- Immutable assistant namespace with exclusive legacy binding preservation; cross-palace reads default off and writes always bound. Effective tool/skill/scope checks, write-lock revocation, one-time legacy grant repair bounded by Settings revision. No client namespace/admin overrides.
- Retired alternate local/session fact tools and local import/export execution. Existing files preserved without claiming migration. Internal context/code indexes and protected OKG remain distinct.
- Shared typed Settings service with revision CAS and closed Concierge operations. Persistent assistant project defaults and explicit chat memberships; arbitrary canonical readable directories.
- Automatic bounded model entity extraction with exact evidence quotes, checkpoints, protected publication, search retry and source withdrawal.
- Shared input/persisted attachment schemas, PNG/JPEG/WebP validation, CSV/TSV/HTML/XLSX preparation with bounds, trusty-memory-owned assets, typed provider image payloads, authenticated history/asset reads. FE uses standard libraries and bounded previews/retry handling.
- A permanent Python integration harness exists at crates/trusty-agents/tests/integration/chat_attachments.py. QA found fixture defects during its first execution; all six corrections are now applied to the permanent harness and syntax/help pass. The complete harness still needs execution against post-repair binaries.

### Review gate: unresolved BLOCK until re-review

Independent critic found three CRITICAL extraction defects and one HIGH attachment defect. Backend has written all four repairs and inherited permission replacements; extraction7 and final attachment/grant3 parent tests plus isolated child checks pass. Independent re-review is still required; this is not a cleared verdict.

1. Published generation identity was lost when replacement inference failed, so removing the source could leave old entities searchable. Required regression: publish → edit → replacement failure → remove source → withdrawal.
2. Project extraction scanned outside authorized time windows and marked all jobs/stages completed after one scan. Required per-window admission and completion evidence.
3. Status GET exposed full checkpoint output, including unpublished candidates/evidence. Required metadata-only projection and failure/revocation tests.
4. Unsupported image provider rejection happened after writing assets/user history, contaminating later turns and retries. Required preflight before persistence, unchanged history on rejection and accepted-turn retry behavior.

Additional live API defect: Settings PATCH scopes: [] was accepted, but inherited base scopes still permitted /memory/write (200/stored instead of403). Backend is adding explicit replacement flags for operator-selected scopes/tools/skills/delegate lists while preserving authored inheritance defaults. Verify empty and narrowed selections after reload/restart and through Concierge.

## Verification evidence and limits

- UI: 631 tests passing, check/build pass with three existing warnings, independent UI APPROVE. Browser clipboard test used an intercepted API; this is not installed/native UI proof.
- Current-snapshot SLD: zero errors/warnings; document paths/numbers, test pointers, line caps and 12 fragments passed before latest repairs. Re-run on final committed source. SLD link validity does not prove behavioral contract compliance (critic found concrete spec violations).
- Pre-review focused backend evidence: attachments7, direct operations1, checkpoint1, configured provider2, common assets1, image conversions3, watcher1, table-related29; Agents all-target clippy passed.
- Post-review extraction tests: 7 passed in /tmp/assistant-critic-extraction-after2.log; failing-first status test retained in /tmp/assistant-critic-extraction-before.log. Final attachment/inheritance focused run passed3 parent tests plus isolated child checks in /tmp/assistant-handoff-focused3.log. Source and Cargo ownership are released; no owned build/runtime processes remain.
- Direct raw preparation: 20 HTTP requests, zero failures, seven formats/combinations and exact400/413/422 negatives.
- Actual attachment chain passed exact provider image/table payload, memory-owned references/assets, foreign assistant/session denial, restart and follow-up hydration on PRE-REPAIR binaries. No real vision request was made in this QA slice. The full direct suite stopped at inherited scope revocation, so later Concierge/cross-recall/restart checks did not run.
- Normal fresh/edit/restart tombstone proof passed (three active notes; no deleted notes in chunks/query). Separate replacement503→source removal reproduced the critic failure: retryable/output:null and three index chunks remained.
- Full direct memory/Settings run found the inherited revocation bug after 57 requests: /private/tmp/qa-assistant-api-ac8g7nzw/qa-api-results.json.
- Full Rust consumer gates, final critic/security review, final API rerun, one real synthetic vision request, clean merge/install and installed UI/API verification remain outstanding. Do not close Agents tickets from these partial results.

## Next Steps

1. Read the final backend/API handoffs and final ticket/milestone matrix appended by this session. Establish branch/checkpoint provenance and preserve the worktree.
2. Finish and verify the four critic repairs plus inherited permission replacement. Promote queued QA fixture corrections. Run the permanent attachment harness against rebuilt exact binaries; retain failing-before evidence.
3. Re-review with an independent critic and security specialist. The prior BLOCK is not automatically cleared by passing focused tests.
4. Run rung6 gates. Common requires unconditional-only and feature-correct memory-core,embedder-test-support,inference-client,bedrock-client,axum-server,knowledge-document lanes. Run workspace check and all direct consumers; refreshed inventory is20 common direct consumers/22 package test set, including trusty-kb. trusty-code needs check/clippy/tests. Use --no-fail-fast and do not remove/ignore/exclude failing coverage. Relevant ignored API target is api_e2e with --include-ignored and controlled credentials.
5. Rebase the committed branch safely onto merged main as needed (base #7331 was squash merged). Preserve other sessions and never discard dirty main changes. Re-run gates affected by rebase.
6. Push final implementation, satisfy live required checks and review, squash merge, then install from clean known source. Versions/install/restarts belong to local-ops. Never cp over macOS binaries; use cargo install and required signing.
7. Verify installed app provenance, stable existing project root, facts/settings/projects/attachments restart durability, source withdrawal and API/native UI behavior. Advance tickets merged→tested→closed only with installed evidence.

## Runtime and environment boundaries

Shared build target (do not delete during handoff): /Users/masa/trusty-mpm-projects/bobmatnyc/trusty-tools/.claude/worktrees/agents-knowledge-pipeline/target

Pre-repair debug binaries (defect reproduction only): tagent466d745a825eb77c6218ff694c4c1a0ef7143a59fc5a858ca22f5f6173311eba; trusty-memory880c85cd02d10f29bcf46df5ec24be1386fd80348d780edaef4ab712be738f73; trusty-search59d3d83fd1726b96916235a3d8bf3cfcfc8897ed9051e16bdf8afe0b4df2b0ac. QA has explicitly released these paths after all owned child processes and socket owners were verified gone; they may now be rebuilt.

Production Trusty Agents app, memory daemon and search daemon were NOT upgraded by this batch. Existing app /Applications/Trusty Agents.app was observed old0.39.1/c7e38dbfa-dirty; refresh process provenance before operations. Preserve existing Slack tagent and unrelated services. Main checkout contains other sessions' work: no stash/reset/clean or source writes there.

Before Finder launch of a new app, preserve/seed only if absent the saved root JSON string at /Users/masa/Library/Application Support/com.trusty.assistant/project-root.json to the existing crates/trusty-agents root; never overwrite an intervening selection. Memory/search restart must be coordinated bootout → owned process/socket exit → bootstrap, not kickstart-k. Preserve existing palace/index data.

Slack search needs SLACK_USER_TOKEN; managed Slack currently has bot-token reads. No Trusty Notion server was configured in inspected managed/project registries or found on PATH. Do not claim native Notion verification or silently use Codex plugin success as that evidence. No external messages or pages have been sent/written.

## Local evidence index

- /tmp/trusty-assistant-expanded-gates-install.md — exact gate/install plan
- /tmp/trusty-assistant-direct-consumers.json — refreshed consumer inventory
- /tmp/trusty-assistant-backend-7370-handoff.md — pre-review implementation and evidence
- /tmp/assistant-final-critic-findings.md — exact review defects/citations
- /tmp/assistant-final-review.diff and /tmp/assistant-final-review-files.txt — pre-repair review snapshot
- /tmp/trusty-assistant-standards-status.md — evolving audit, not readiness approval
- /tmp/trusty-assistant-direct-api-acceptance.py and trusty_assistant_api_fixtures.py — direct API scratch tests
- /tmp/trusty-assistant-prepare-api-contract.md and trusty-assistant-direct-operation-contract.md — API contracts (also documented in repository)

Local /tmp evidence may disappear; the issues and committed handoff must carry the decisive results, commands and outstanding acceptance. Do not commit credentials or raw private payloads. No updates to Codex memory files were authorized or made.

Additional QA limit: plaintext history-probe outage proceeds without the prior image; the provider system prompt carries a memory-unavailable notice but the API has no structured warning. Deterministic output does not establish real model truthfulness. Keep this explicit in #7370 acceptance/review. Final QA handoff: /tmp/api-tm-handoff.md. All QA-owned children/socket listeners have been cleaned up.

Snapshot identity observed from environment: CODEX_THREAD_ID=01a08be0-a42a-7263-9da2-63b855621d20; no TMUX or TM_SESSION_NAME environment value. Git/PR ownership uses existing ws/codex-agents-assessment. For fallback snapshot, no tool_search, session_context_pause or trusty-mpm tools appeared in ALL_TOOLS; preserve this observed basis, not a daemon-disconnection guess.

Final handoff addenda: /tmp/backend-tm-handoff.md and /tmp/trusty-tm-issues-handoff.md are complete. All 12 canonical issues now have appropriate reused Backlog milestones (agents/search/mcp), with no metadata audit failures. Historical milestone exceptions on #3931/#4358 are reconciled; prior ownership on #4283/#4531 remains explicit. Git is preparing the checkpoint, draft PR and durable snapshot. Credential scanning must pass before push.


# Backend checkpoint

# Backend tm handoff — working checkpoint, not merge-ready

Worktree: `/Users/masa/trusty-mpm-projects/bobmatnyc/trusty-tools/.claude/worktrees/assistant-memory-project-settings`.
Base: original `b86f7ac`, related #7331 base resolution belongs to Git coordinator. No versions, commits, installs, main writes or live daemon restarts by backend engineer.

## Implemented scope

All explicit/automatic assistant memory routes through trusty-memory. Assistant namespaces preserve exclusive legacy CTO; cross-palace reads are opt-in. Memory remember/write aliases, automatic recall, tools and direct HTTP operations share enforcement. Old session/native keyed memory tool stores and CLI execution are retired without deleting legacy data. Persistent assistant projects, arbitrary directories, Concierge Settings parity, automatic grounded extraction, durable retries and shared search live filters/tombstones are implemented. #7370 provides API-authoritative raw image/table preparation, shared typed image provider messages, memory-owned durable assets/history and authenticated asset retrieval. Detailed contracts and earlier gate results: `/tmp/trusty-assistant-backend-7370-handoff.md`, `/tmp/trusty-assistant-prepare-api-contract.md`, `/tmp/trusty-assistant-direct-operation-contract.md`.

## Latest critic/QA repair checkpoint

Independent critic BLOCK remains unresolved pending re-review: `/tmp/assistant-final-critic-findings.md`.

- Extraction now keeps `materialized_fingerprint` independently of pending output and journals potential side effects before publication. Failed new inference cannot erase the generation needed for withdrawal.
- Project snapshots are admitted by captured mtime in requested half-open monthly intervals plus ongoing arrivals. Each job is advanced separately; failed items affect its own interval. Source execution split into `knowledge_pipeline/execution_sources.rs` to preserve the line cap.
- Extraction HTTP status uses a metadata-only projection, excluding candidate bodies/evidence and raw provider errors.
- Persona resolves provider credentials/client before attachment processing. Attachment preparation hydrates and validates before any asset/user writes. An identical last pending user message is reused under the manifest lock; a changed conversation rejects and requests retry.
- QA found `scopes: []` inherited base grants. Added explicit `permissions.replace_scopes`, `replace_tools`, `replace_skills`, `replace_subagents` flags persisted by the same Settings patch service. Authored overlays retain union defaults; Settings selections replace inherited lists exactly.

## Verification

- Before regression: `/tmp/assistant-critic-extraction-before.log`: `test result: FAILED. 0 passed; 1 failed; 0 ignored`; unpublished Maya entity/evidence returned by status.
- After extraction repairs: `/tmp/assistant-critic-extraction-after2.log`: `test result: ok. 7 passed; 0 failed; 0 ignored`. Includes failed refresh then withdrawal, interval admission/per-job completion, status projection and existing revocation/retry cases. Development `--lib` filter only, not full required suite.
- Final focused attachment/grant gate pending when this draft was written; see appended result below.
- `cargo fmt` EXIT0 `/tmp/assistant-handoff-fmt.log`.
- Focused line caps EXIT0 `/tmp/assistant-handoff-linecaps.log` (7 files including new split).
- Grants fragment validation EXIT0 `/tmp/assistant-handoff-fragment.log`.
- Earlier UI631 tests/check/build + independent APPROVE, agents Clippy and focused common/search/provider tests documented in previous handoff. These do not prove the latest Rust fixes or full consumer matrix.

## Frozen binaries predate critic repairs

All at `/Users/masa/trusty-mpm-projects/bobmatnyc/trusty-tools/.claude/worktrees/agents-knowledge-pipeline/target/debug/`:

- tagent: `466d745a825eb77c6218ff694c4c1a0ef7143a59fc5a858ca22f5f6173311eba`
- trusty-memory: `880c85cd02d10f29bcf46df5ec24be1386fd80348d780edaef4ab712be738f73`
- trusty-search: `59d3d83fd1726b96916235a3d8bf3cfcfc8897ed9051e16bdf8afe0b4df2b0ac`

Build logs `/tmp/assistant-attachments-build-{tagent,memory,search}.log`, all EXIT0. QA released all runtime processes/binary freeze. These binaries are defect-reproduction evidence, not final candidates.

## Remaining work for tm

1. Review all latest repairs, including exact-list flags and pending retry semantics. Run focused/full gates after any corrections. Add stronger concurrent accepted-turn/idempotent-write tests if required by review.
2. Rebuild the three debug binaries and rerun permanent API/attachment harness and extraction failed-refresh/withdrawal live proof. The new source fixes have not been exercised by process-level QA yet.
3. QA corrections from `/tmp/api-final-harness-corrections.patch` are APPLIED to the permanent harness. Packaged persona/discovery, case-insensitive headers, malformed-base64400, rejection-history assertion, and private UDS capability-arrival checks are included. Python syntax/help EXIT0; no API run after applying. `/tmp/assistant-handoff-harness-patch.log` and `/tmp/assistant-handoff-harness-help.log`.
4. Plain-text initial history-probe outage still degrades to prior text path. QA observed success with no image and a memory-unavailable system prompt notice, not a verified user-visible disclosure. Decide/fix before claiming durable attachment context on outage.
5. Full rung5/6 gates: `/tmp/trusty-assistant-expanded-gates-install.md` and `/tmp/trusty-assistant-direct-consumers.json`: common feature lanes, workspace check, all22 direct consumer/UI package tests, affected Clippy, relevant ignored API tests with authorized configured credentials, final doc/SLD/pointer/cap/changelog gates, security review, critic re-review. No full gate or final merge approval claimed.
6. New source `crates/trusty-agents/src/api/server/knowledge_pipeline/execution_sources.rs` needs Git intent-to-add/tracking; parent/Git coordinator owns commit/push WIP and issue/milestone updates. Source/API docs should describe exact Settings replacement flags and current status projection.
7. Resolve stacked base, final versions, merge and install only after reviewed green gates and clean committed source. User’s latest steering requests documented tm handoff now, so this checkpoint is intentionally unmerged/uninstalled.

Source ownership release and final focused verdict are appended below.

Focused first run `/tmp/assistant-handoff-focused.log` compiled production and passed grant replacement + inherited write-revocation, but attachment fixture failed discovered-assistant membership. Fixed fixture to extend the actual assistant base without loosening discovery; rerun `/tmp/assistant-handoff-focused2.log` is pending. Latest fmt EXIT0 `/tmp/assistant-handoff-fmt2.log`.

Second focused run `/tmp/assistant-handoff-focused2.log` again passed Settings replacement and inherited write revocation. Attachment fixture hit the strict symlink guard because macOS temporary paths contain a symlink; canonicalized only the child fixture root. Production path validation remains unchanged. Final rerun: `/tmp/assistant-handoff-focused3.log`. Tracked worktree aggregate before new split is tracked: Added8916 / Removed6671 / Net2245 (includes UI/docs/QA changes).

## Final checkpoint verdict and ownership release

`cargo test -p trusty-agents --lib --no-fail-fast -- attachment_rejection settings_grant_replacements memory_write_wait` EXIT0, `/tmp/assistant-handoff-focused3.log`. Parent process: `test result: ok. 3 passed; 0 failed; 0 ignored`; both isolated child tests also passed. The attachment regression observes provider rejection leaves zero asset/user writes, a subsequent plain-text turn remains available, and an identical pending retry yields exactly one asset and one user message. The other tests cover all4 narrowed/empty Settings lists on disk reload and inherited scope revocation while a memory writer waits for the mutation lock.

Latest production Rust compiles through this focused test target. All full-target/consumer gates and independent re-review remain outstanding. No binary was rebuilt after the four critic repairs. `cargo fmt` and final `git diff --check` EXIT0. No Cargo or owned runtime processes remain.

Engineer releases ALL source and Cargo ownership to parent/Git/tm now. Parent can track the split source, stage and push a WIP checkpoint. This is NOT approval to merge red/pending gates.

Improvement recommendation: both attachment fixture failures came from rebuilding an assistant fixture independently of the existing discovered-assistant/canonical-root helpers. Reuse one production-shaped canonical test fixture for new API/memory tests, with discovery asserted before the behavior under test; this would avoid the two failed compile/test iterations. Evidence: `/tmp/assistant-handoff-focused.log` and `/tmp/assistant-handoff-focused2.log`.


# API evidence

# Agents API QA handoff to tm

Pre-repair development binaries only. No final release approval, real vision call, install, Git mutation, or issue mutation occurred in this QA slice. All owned runtime processes are stopped. The three shared debug binary paths are released to implementation for rebuild.

## Frozen artifacts exercised

All binaries came from `.claude/worktrees/agents-knowledge-pipeline/target/debug/` under the trusty-tools checkout:

- tagent: `466d745a825eb77c6218ff694c4c1a0ef7143a59fc5a858ca22f5f6173311eba`
- trusty-memory: `880c85cd02d10f29bcf46df5ec24be1386fd80348d780edaef4ab712be738f73`
- trusty-search: `59d3d83fd1726b96916235a3d8bf3cfcfc8897ed9051e16bdf8afe0b4df2b0ac`

## Verified behavior

- Direct attachment preparation: `20 recorded requests; failures: 0`. Seven formats (PNG/JPEG/WebP/CSV/XLSX/TSV/HTML), combined preparation, canonical equality, and exact structured 400/413/422 rejections. Evidence: `/private/tmp/qa-assistant-api-ac8g7nzw/qa-api-attachment-preparation-results.json`.
- Actual image/table chain: raw prepare -> task -> observed OpenRouter-compatible provider image bytes and exact table JSON -> trusty-memory typed history -> owned asset bytes -> memory/tagent restart -> identical history/assets -> follow-up with prior image/table. Real application and real isolated memory/search, deterministic loopback provider. Auth checked missing/wrong bearer 401 and foreign Origin403. Foreign assistant and same-palace foreign session asset reads returned404. Evidence: `/private/tmp/qa-attachment-integrated-5fnvg04m/attachment-integration-proof.json`; this same run then fails the rejected-provider regression below.
- Corrected outage-only run: `EXIT=0`. The shim socket has0600 mode and logs actual capability requests. Old-daemon capability rejection and known historical asset retrieval failure stop before provider inference. Evidence: `/private/tmp/qa-attachment-integrated-bspa4vz_/attachment-integration-proof.json`, log `/tmp/api-final-prerepair-outages2.log`. This scoped run intentionally does not repeat the already-failing unsupported-provider regression; it cannot green the full harness.
- Tombstone fresh -> source edit -> tagent/search restart: exactly3 active entity chunks, no deleted-source note in chunks or search response. Evidence: `/tmp/api-final-prerepair-tombstone-proof.md`, step `fresh_edit_restart_tombstone_assertions_passed: true`.

## Product failures (all reported to implementation and parent)

1. **#7360 inherited empty scope revocation:** a valid packaged secondary assistant extends assistant. PATCH scopes:[] succeeds, but POST memory/write returns200/stored. Raw result: drawer_id `f42a0e89-01ee-4cc1-bffa-8906d2cb4d0e` in its own synthetic namespace. Expected403. Scope restored in finally. Evidence: `/private/tmp/qa-assistant-api-ac8g7nzw/qa-api-results.json`, `57 recorded requests`, failure `direct_memory_missing_scope`. Preserve this file; the direct suite stopped there, so later Concierge/cross-recall/restart assertions did not run.
2. **#7370 rejected image send persists history:** unsupported local provider is rejected before inference, but user history grows4->5. Expected unchanged. Evidence: `/private/tmp/qa-attachment-integrated-5fnvg04m/attachment-integration-proof.json`, `rejected_provider_history`, and `/tmp/api-final-prerepair-attachments5.log`.
3. **#4283 failed replacement withdrawal:** after successful publication, source edit returns503 from provider; then source is removed. After100s, checkpoint remains retryable/output:null and index chunk_count remains3. Expected withdrawal. Evidence: `/tmp/api-final-prerepair-tombstone-proof.md`, `failure_state`; log `/tmp/api-final-tombstone-repro.log`. This reproduces the independent critic finding, while normal tombstones passed above.

## Plaintext history-probe outage assessment

When the prior-history probe fails, a text-only follow-up proceeds without its prior image. Observed task result: status success, errors[], previous_image_sent:false. The system prompt supplied to the deterministic provider includes a memory-unavailable notice. The response text is scripted (`Synthetic attachment received.`), so this is not evidence of real model truthfulness or hallucination. The API does not expose a structured warning for omitted historical attachments. Parent must decide whether the prompt notice is sufficient or require visible context-loss status; do not claim image context survived this outage.

## Source harness and pending corrections

Worktree: `.claude/worktrees/assistant-memory-project-settings`.
New source: `crates/trusty-agents/tests/integration/chat_attachments.py`.
Instructions: `crates/trusty-agents/tests/integration/README.md`.

The source harness was promoted before runtime release. Actual execution found fixture defects. A consolidated patch is ready at `/tmp/api-final-harness-corrections.patch`. Apply in one serialized source window; no Git writes have been made by QA. The patch:

- Creates persona.md and removes package-incompatible system_prompt.content.
- Marks secondary fixture extends=assistant, as required by roster discovery.
- Compares HTTP header names case-insensitively.
- Expects400 for malformed task base64, matching the final contract.
- Asserts rejected provider leaves history unchanged (currently fails correctly).
- chmods the synthetic daemon socket0600 and proves capability RPC was actually reached.

Corrected full scratch reproduction: `/tmp/api-final-attachment-repro.py`.
Outage-only scratch: `/tmp/api-final-attachment-outages.py`.
Direct runtime owner: `/tmp/api-final-direct-runtime.py` (specific synthetic fixture, not portable).
Fresh extraction/tombstone reproduction: `/tmp/api-final-tombstone-repro.py`.

The direct preparation script also needs its packaged fixture corrected before creating new fixtures: remove inline system_prompt content, keep persona.md, and set secondary extends=assistant. These corrections were made only in the existing synthetic fixture for this run.

## Verification and cleanup limits

- New harness syntax, --help, generated PNG CRC/pixel count, and scoped whitespace checks passed.
- `bash scripts/check_sld.sh` returned0: `scanned 63 spec doc(s) + 3961 code file(s); resolved 42 frontmatter + 77 inline reference(s), rejected 0 + 0 on path shape; 0 error(s), 0 warning(s)`.
- Correction: that wrapper invokes Cargo internally. The earlier QA statement that no Cargo ran was inaccurate. It was not repeated after the build-owner conflict was identified.
- Latest outage cleanup: owned PIDs92286,92287,92303,92722,92750,92801; survivors[]. Independent lsof verified no socket owners under latest attachment, direct API, or extraction runtime roots. Earlier attachment runs also recorded survivors[].
- Full API acceptance, repaired regressions, full permanent harness, and real vision must run on the next frozen candidate. No final suite green or release readiness claim is supported yet.

## Improvement recommendations

- Symptom: the prepped harness stopped on three package/header fixture assumptions before exercising the full chain. Cause: syntax-only preparation did not validate the strict packaged-agent loader and UDS permissions contract. Change: promote the corrected fixture into the repository and keep explicit discovery/capability-arrival assertions before inference; these checks now live in the queued patch. Evidence: `/tmp/api-final-prerepair-attachments.log`, `attachments2.log`, `attachments3.log`, and first outage log.
- Symptom: SLD execution crossed the delegated Cargo ownership boundary. Cause: the wrapper's implementation was not inspected before treating it as a docs-only gate. Change: classify wrapper side effects before execution, or let the existing build owner run the SLD wrapper. Evidence: `/tmp/api-final-harness-sld.log` and parent correction.


# Issues and milestones

# tm issue handoff — 2026-09-10

Latest owner request explicitly requires issue and milestone coverage. Existing Backlog · agents/search/mcp milestones are reused; no release deadline is invented. Earlier #3931/#4358/#7391 milestone exceptions remain historical and are superseded by assigned milestones. All issues are assigned to bobmatnyc. States below are live reads, not readiness judgments.

| Issue | Milestone | State | Remaining acceptance | Claim / handoff |
| --- | --- | --- | --- | --- |
| [#7359](https://github.com/bobmatnyc/trusty-tools/issues/7359) | Backlog · agents | OPEN / status:in-progress | Assessment recommends two crates; retirement not authorized as implementation. | codex-agents-assessment; tm takeover coordination pending |
| [#7360](https://github.com/bobmatnyc/trusty-tools/issues/7360) | Backlog · agents | OPEN / status:in-progress | Verify sole trusty-memory path, effective empty/narrowed permissions, REST/chat/Concierge parity, namespaces, restart and installed behavior. | codex-agents-assessment; tm takeover coordination pending |
| [#3931](https://github.com/bobmatnyc/trusty-tools/issues/3931) | Backlog · agents | OPEN / status:in-progress | Verify persistent project defaults and all Settings parity, inherited grant replacement, migration and installed UI/API. | Existing prior claim preserved; coordinate before replacing |
| [#4358](https://github.com/bobmatnyc/trusty-tools/issues/4358) | Backlog · agents | OPEN / status:in-progress | Verify arbitrary readable non-Git folders persist in inventory/chat; defaults removal and installed stable root. | Existing prior claim preserved; coordinate before replacing |
| [#4283](https://github.com/bobmatnyc/trusty-tools/issues/4283) | Backlog · agents | OPEN | Clear critic BLOCK: failed replacement withdrawal, authorized windows, status privacy; final real-provider/API/corpus/restart proof. | trusty-tools-38 / Claude Code handoff unresolved; no lifecycle label |
| [#4531](https://github.com/bobmatnyc/trusty-tools/issues/4531) | Backlog · agents | OPEN | Foundation #7331 merged only; coordinate source children and clear extraction/review/API/installed acceptance. | trusty-tools-38 / Claude Code handoff unresolved; no lifecycle label |
| [#7361](https://github.com/bobmatnyc/trusty-tools/issues/7361) | Backlog · agents | OPEN / status:in-progress | Verify typed Concierge delegation and all settings/health return paths through API and installed app. | codex-agents-assessment; tm takeover coordination pending |
| [#7370](https://github.com/bobmatnyc/trusty-tools/issues/7370) | Backlog · agents | OPEN / status:in-progress | Clear rejected-provider history defect; corrected permanent harness, authoritative API, model bytes/assets, real vision and native UI. | codex-agents-assessment; tm takeover coordination pending |
| [#7379](https://github.com/bobmatnyc/trusty-tools/issues/7379) | Backlog · search | OPEN / status:in-progress | Final source-update/tombstone and live filter acceptance, full gates/review, merge/install proof. | codex-agents-assessment; tm takeover coordination pending |
| [#7389](https://github.com/bobmatnyc/trusty-tools/issues/7389) | Backlog · agents | OPEN / status:in-progress | Slack bot reads installed pass; search requires user token; Trusty Notion not configured. Resolve prerequisites or explicit disposition. | codex-agents-assessment; tm takeover coordination pending |
| [#7391](https://github.com/bobmatnyc/trusty-tools/issues/7391) | Backlog · mcp | CLOSED | None for listing transport fix: merged, installed, verified and closed. Search credentials separate. | Completed; no active claim |
| [#7329](https://github.com/bobmatnyc/trusty-tools/issues/7329) | Backlog · agents | OPEN | Calendar source collection, paging/delta, monthly batches and entity publication remain upstream; foundation control plane only. | Claude Code upstream handoff; bobmatnyc assigned; unclaimed |

## Native relationships

#4358 remains child of #4355. #4283 remains child of #4007 and blocked by #7379. #7329 is now a native child of #4531, matching its existing accepted scope. #7389 retains its dependency on closed #7391; that dependency is resolved. Existing #4531 children remain intact. No arbitrary parent reassignment was made merely to group the handoff.

## Delivery and evidence limits

#7391 is CLOSED after clean merged install/live MCP proof. #7331 foundation squash dfb2d6a79f242867b003fe2799453545fe08cc78 is merged, not the current follow-on Agents batch. The current batch must clear independent critic BLOCK, inherited-scope revocation regression, exact candidate direct API/permanent harness, consumer gates, real vision, and installed native UI/API verification. Written repairs and passing pre-repair subsets do not clear those gates.

Prior #4283/#4531 ownership cannot be proven stale from inspected session inventories; neither has a lifecycle label. tm must coordinate that handoff rather than silently take over. No synthetic coded/merged/tested labels were applied. #7359 remains an assessment. No Trusty Notion implementation is inferred from successful Codex plugin access.

## Metadata audits

```text
#7359
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents, trusty-agents-common, trusty-agents-local
  relationships    INFO  no parent; blocked-by 0; sub-issues 0

#7360
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  no parent; blocked-by 0; sub-issues 0

#3931
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-mpm, trusty-agents
  relationships    INFO  no parent; blocked-by 0; sub-issues 1

#4358
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  parent #4355; blocked-by 0; sub-issues 0

#4283
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  parent #4007; blocked-by 1; sub-issues 0

#4531
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  no parent; blocked-by 0; sub-issues 17

#7361
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  no parent; blocked-by 0; sub-issues 0

#7370
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents
  relationships    INFO  no parent; blocked-by 0; sub-issues 0

#7379
  project          PASS  trusty-search
  milestone        PASS  Backlog · search
  component label  PASS  trusty-search
  relationships    INFO  no parent; blocked-by 0; sub-issues 0

#7389
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  SKIP  (This read-only launch-context verification has not identified a defective workspace source component; a component label would imply an unsupported diagnosis.)
  relationships    INFO  no parent; blocked-by 1; sub-issues 0

#7329
  project          PASS  trusty-agents
  milestone        PASS  Backlog · agents
  component label  PASS  trusty-agents, trusty-gworkspace
  relationships    INFO  parent #4531; blocked-by 0; sub-issues 0

```

Git checkpoint, push, draft PR and committed session link are pending the Git-owner handoff.

## Open historical-attachment outage decision (#7370)

A plaintext turn succeeds without the prior image when history probing fails. The prompt includes a memory-unavailable notice, but the API has no structured context-loss warning. Real-model truthfulness was not tested. Decide whether prompt-only notice is sufficient or add visible structured status, then verify. This remains open, not passed; see /tmp/api-tm-handoff.md and issue comment https://github.com/bobmatnyc/trusty-tools/issues/7370#issuecomment-5624295649.


# Independent critic findings

# Independent critic: BLOCK

Read-only review, confidence above 95%. Full consumer/runtime gates remain pending.

| Severity | File:line | Finding | Required regression/fix |
| --- | --- | --- | --- |
| CRITICAL | crates/trusty-agents/src/api/server/knowledge_pipeline/execution.rs:74 | Withdrawal requires checkpoint.output.is_some(), but fingerprint replacement resets output=None at271-290; failed replacement inference followed by source removal leaves published entities. | Preserve published-generation identity separately from pending inference; publish -> edit -> failed inference -> remove source must withdraw. |
| CRITICAL | crates/trusty-agents/src/knowledge/execution.rs:76 | All project jobs/four stages complete after one scan; returned scan.items are not partitioned by authorized job intervals. | Partition captured snapshots by authorized intervals and advance only corresponding successfully completed jobs. Test current month/outside month/additional requested months. |
| CRITICAL | crates/trusty-agents/src/api/server/knowledge_pipeline.rs:304 | GET extraction serializes checkpoints.output including uncommitted candidates/evidence before publication/authorization recheck. | Dedicated status projection without output; GET during publication failure/source revocation never exposes candidates. |
| HIGH | crates/trusty-agents/src/ctrl/pm_task/dispatch/persona.rs:105 | Attachment prepare persists user turn/assets before resolve_provider support check; rejected send pollutes history and blocks later text-only turns, retries duplicate. | Validate provider before committing; separate hydration/validation from persistence; idempotent accepted-turn writes. Rejection leaves history unchanged, later text succeeds. |

Governing interval/status contracts: docs/specs/agent-config-five-sections.md:365 and:398. Re-review after fixes and regression proof. User explicitly requests fixing these features, verifying standards, and pushing/merging/installing; no merge while this verdict remains unresolved.


# Completion gates

# Final gate and local install plan

Preparation only. No gates, builds, versions, Git state, services, or live settings were changed. Source was actively changing during inspection; recheck the final diff and feature gates before execution.

## Required Rust gates

Run serially after the backend writer releases source/build ownership. Use the agreed shared CARGO_TARGET_DIR, SKIP_UI_BUILD=1 for Rust-only gates, and separate task-specific log files with observed exit status. Do not run multiple Cargo processes against that target. Run each command separately; do not pipe live gate output. Passing prior gates count only if their source and feature coverage still match the final change.

Common has no default features. The unconditional attachment types and feature-gated validation, memory storage, document extraction, inference messages, and Bedrock conversion all changed. Run:

```sh
cargo fmt --check
cargo check -p trusty-common --features unconditional-only
cargo test -p trusty-common --features unconditional-only --no-fail-fast
cargo clippy -p trusty-common --all-targets --features unconditional-only -- -D warnings
cargo check -p trusty-common --features memory-core,embedder-test-support,inference-client,bedrock-client,axum-server,knowledge-document
cargo test -p trusty-common --features memory-core,embedder-test-support,inference-client,bedrock-client,axum-server,knowledge-document --no-fail-fast
cargo clippy -p trusty-common --all-targets --features memory-core,embedder-test-support,inference-client,bedrock-client,axum-server,knowledge-document -- -D warnings
cargo check --workspace
cargo check -p trusty-kb -p trusty-code -p trusty-agents-common -p trusty-memory -p trusty-search -p trusty-agents -p trusty-agents-ui
cargo clippy -p trusty-kb -p trusty-code -p trusty-agents-common -p trusty-memory -p trusty-search -p trusty-agents -p trusty-agents-ui --all-targets -- -D warnings
```

`bedrock-client` is the changed inference adapter; `bedrock` alone does not compile it. `axum-server` enables the inference HTTP test server. `memory-core` includes chat-attachments; knowledge-document is a separate newly added feature. Do not use --all-features: ORT loading modes conflict.

Test all direct consumers from /tmp/trusty-assistant-direct-consumers.json, once per package, plus the changed desktop crate. The following single invocation covers every listed package and continues across failing targets. It may be split into serial per-package commands with the identical --no-fail-fast flag for clearer logs:

```sh
cargo test -p trusty-kb -p trusty-agents -p trusty-installer -p trusty-mcp -p trusty-memory -p tga -p trusty-code-gui -p trusty-console -p trusty-embedderd-py -p trusty-audit -p trusty-channels -p trusty-agents-common -p trusty-gworkspace -p trusty-analyze -p trusty-review -p trusty-code -p trusty-sld-lint -p trusty-mpm -p trusty-embedderd -p trusty-search -p trusty-agents-local -p trusty-agents-ui --no-fail-fast
```

Use package defaults: memory daemon is default; search bundled-ort is default. Never exclude a failing GUI or Python consumer to claim this set passed. If an environment failure occurs, establish the unchanged-base failure as project instructions require.

Rung 5/6 also needs ignored integration coverage relevant to the changed contract. Inventory the final changed modules' ignored tests and name their exact test targets before running `cargo test -p PACKAGE --test TARGET --no-fail-fast -- --include-ignored`. No changed asset/watcher location inspected currently contains an ignored test; new process-level API regression tests should run by default. Do not launch unrelated multi-minute throughput/soak benchmarks merely because they are ignored. User requires testable API behavior: provide real server tests for image upload/history/asset retrieval and isolation, invalid/oversized inputs, memory namespace/cross-palace enforcement and restart durability, extraction completion/error/retry/source revocation, and watcher create/change/delete/type filtering. Include a synthetic installed API/UI smoke after deployment.

UI: pnpm test:unit, pnpm check, pnpm build, and the package's Playwright test command against a temporary server. Run doc/SLOC/changelog/test-pointer gates from the final staged/committed change. Independent critic and security review remain prerequisites.

## Install and restart

Only after reviewed gates, version ownership handoff, #7331 base resolution, and a clean known committed source. No tag or crates.io publication requested. Version bumps inside the implementation PR use --no-changelog; local-ops handles manifests and lockfile, version-control commits. Preserve fragments.

Build release artifacts before stopping services. Each install uses cargo install through the signed scripts, never cp over an executable. From the clean deployment worktree, set TRUSTY_INSTALL_PATH to that worktree, TRUSTY_SIGN_IDENTITY to Developer ID Application: Bob Matsuoka (4JH68XUHC5), CARGO_TARGET_DIR to the agreed target, and leave TRUSTY_INSTALL_VERSION unset:

```sh
scripts/install-trusty-memory-signed.sh
scripts/install-trusty-search-signed.sh
scripts/install-trusty-agents-signed.sh
```

These install/sign but do not perform the coordinated restarts. Memory installs trusty-memory and trusty-memory-mcp-bridge (the release guide's third trusty-bm25-daemon binary is stale). Search installs trusty-search AND trusty-embedderd; preserve existing running embedder processes unless a demonstrated version contract requires their coordinated restart. Installing a binary does not update running processes. Check every expected signature independently because memory/agents scripts fail soft on signing. No restart of mpm, Slack, or unrelated services.

Desktop build from crates/trusty-agents/ui:

```sh
pnpm install --frozen-lockfile
APPLE_SIGNING_IDENTITY="Developer ID Application: Bob Matsuoka (4JH68XUHC5)" pnpm tauri build --bundles app
```

Custom CARGO_TARGET_DIR puts the bundle at TARGET/release/bundle/macos/Trusty Agents.app. install-trusty-agents-signed.sh searches only WORKTREE/target/{release,debug}, so verify the Tauri-signed custom-target bundle directly. Stage with ditto into a unique adjacent /Applications bundle, retain old bundle for rollback, and rename after CUA app quit. Seed the JSON-string desktop root selection before Finder launch as in prior plan; preserve chat data in the existing main-repo crates/trusty-agents root.

Before each daemon restart, compare launchctl-owned PID with actual socket/listener owner. Recheck active requests and coordinate a quiet point with parent; bootout permits graceful handling, but disconnected in-flight calls can still fail. Preserve existing plist, environment, root, palace/index files. Do not auto-kill an unexpected listener; establish ownership and report first.

Observed at preparation: com.trusty.memory PID66891 owns /Users/masa/Library/Application Support/trusty-memory/trusty-memory.sock; com.trusty.search PID67286 owns TCP127.0.0.1:7878. Plists are ~/Library/LaunchAgents/com.trusty.memory.plist (serve --foreground) and com.trusty.search.plist (start --foreground --no-auto-discover, cwd /Users/masa). These observations must be refreshed at deployment.

Restart sequentially, memory then search, only after signed install success:

```sh
launchctl bootout gui/$(id -u) /Users/masa/Library/LaunchAgents/com.trusty.memory.plist
launchctl bootstrap gui/$(id -u) /Users/masa/Library/LaunchAgents/com.trusty.memory.plist
launchctl bootout gui/$(id -u) /Users/masa/Library/LaunchAgents/com.trusty.search.plist
launchctl bootstrap gui/$(id -u) /Users/masa/Library/LaunchAgents/com.trusty.search.plist
```

Between bootout/bootstrap observe old owned PID exit and socket/listener release; after each bootstrap verify the new launchctl-owned PID matches the service owner and reported clean build. Memory health is memory.health over its existing UDS RPC client; search health is GET http://127.0.0.1:7878/health. Never use launchctl kickstart -k, despite a contradictory old orphan paragraph in release-workflow.md. Then launch the desktop and verify its sidecar health/SHA, saved chats, memory durability, uploaded image history after restart, extraction, arbitrary projects, and actual watcher update/removal using synthetic fixtures. Retain rollback app and old binary provenance; rollback by cargo install from the previous clean commit and graceful restart, never restore an old database over newly written data.


## Refreshed final inventory

Metadata refreshed from the live worktree with cargo metadata --no-deps --format-version 1 (exit0); no compilation. Updated /tmp/trusty-assistant-direct-consumers.json includes source/manifest changes and untracked new source files. trusty-common now has 20 direct consumers including trusty-kb. trusty-kb adds trusty-agents; changed trusty-code adds trusty-agents and trusty-code-gui (both already in the consumer test set). The full consumer/UI test list is now 22 packages. trusty-agents-common currently has no source/manifest diff from this worktree HEAD but stays in the test set as a common consumer; its prior baseline work remains relevant.

Delta: add trusty-kb to check/clippy/tests; promote trusty-code from test-only consumer to changed-crate check/clippy plus existing full tests. Both crates have no Cargo features, so their defaults cover the changed modules. Shared tombstone generation in common knowledge_document needs knowledge-document (already in the feature union). New Anthropic image request changes need inference-client, with axum-server for HTTP adapter test coverage (both already present). Bedrock conversion remains bedrock-client. No change to the common feature matrix above.

Ignored integration decision: trusty-kb and the changed trusty-code transcript module have no ignored tests. The memory asset persistence paths have no ignored tests. Their deterministic full suites must include the new regressions; ignored performance seeds and orphan-reentry helper functions do not test this feature and are not standalone gates. Do not invoke all ignored tests indiscriminately, which can directly run child-process helper entry points.

The relevant existing Agents live-provider API target is api_e2e, containing exactly three ignored tests: test_ctrl_chat_hello, test_ctrl_knows_self_project, test_ctrl_connects_to_test_project. Once the configured provider credentials are available without copying them to logs or files, run:

```sh
cargo test -p trusty-agents --test api_e2e --no-fail-fast -- --include-ignored
```

That runs the regular API tests plus those live-provider tests and supplies concrete ignored API integration coverage. It does not replace the synthetic image-history, memory namespace, tombstone watcher, extraction, and project-setting API regressions or installed UI smoke. If credentials/provider routing block the ignored target, report the actual block; do not count skipped tests as completed integration. Preserve unrelated runtime processes.


# Checkpoint clarification

The later backend ownership-release section supersedes earlier pending-run and queued-patch wording: the fixture patch is applied, final focused3 tests pass, and source/Cargo ownership is released. Independent critic re-review, full gates and final process QA remain outstanding. Original base b86f7ac is deliberately preserved for this checkpoint. Foundation #7331 is merged at dfb2d6a79f242867b003fe2799453545fe08cc78; origin/main was f02332b10623f2f631275bde03af0ffceee00de9 at checkpoint preparation. The draft PR and published session snapshot carry the resulting checkpoint SHA and final links. No production Agents install is authorized by this handoff.
