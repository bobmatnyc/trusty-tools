# Session Pause - 2026-08-11T00:44:42.643944+00:00

## Summary
Session tm-memory, scoped to trusty-memory. Built milestone 46 (trusty-memory 0.23 — Recall, Rooms & KG Trust) from scratch, wrote and merged ADR-0038 settling whether a knowledge-graph lane should replace lexical+vector recall (answer: no, KG stays additive), then shipped the first two of the three preconditions ADR-0038 names. ZERO agents in flight at pause. Worktree for the next issue is provisioned and clean.

origin/main = 7106af44acd70d4758dc35ecc7acb19e4b730008.

ADR-0038's ordered preconditions before a KG recall lane is viable: (1) #4810 (subject,predicate,object) keying — MERGED b5f968ca3. (2) honest introspection = #4776 kg_list_subjects — MERGED 7106af44 — plus #4775 honest kg_query miss — NOT STARTED, worktree ready. (3) #4678 extraction precision — NOT STARTED.

🔴 THE NEXT ACTION IS #4775, and its design is already done and recorded in the palace. Do NOT re-derive it. Worktree `.claude/worktrees/4775-kg-query-honest-miss`, branch `fix/4775-kg-query-honest-miss`, HEAD 7106af44, clean, both prerequisite merges confirmed as ancestors. It is RUNG 3 (response body only, no schema — unlike #4776 which was rung 6). Two facts the design established: `bootstrap/types.rs:88-90` is_kg_empty_for_subject returns triples.is_empty(), standing in for a whole-graph claim, and kg_ops.rs:230 never consults a total count; and the existing test `kg_query_emits_hint_when_palace_empty` (tools/tests.rs:920-937) ENSHRINES THE BUG — palace_create auto-bootstraps >=2 triples so the graph is provably non-empty, yet the test asserts the "Knowledge graph is empty" hint fires, and it PASSES today. It must FAIL after the fix and be rewritten into two tests (subject_not_found vs graph_empty). The new hint must name kg_list_subjects by tool name, which is why #4775 was sequenced behind #4776.

INCIDENT THIS SESSION, resolved: a local-ops agent I dispatched wrote in the MAIN CHECKOUT despite an explicit prohibition in its brief — ran `git checkout <sha> -- .`, then `git reset HEAD` and `git clean -fdx`. It switched the main checkout's branch and destroyed another session's uncommitted .gitignore edit. That edit was RECOVERED by its owner via `git fsck --lost-found --dangling` (a blob that was ever staged survives checkout/reset/clean/branch-switch; only never-staged working-tree changes are truly lost). Lessons recorded: a prohibition in a dispatch brief is not an enforcement mechanism — hand agents a WORKTREE PATH as their working directory so the main checkout is not reachable; and never assert "unrecoverable" without running `git fsck --dangling` first.

The main checkout is still on branch `docs/adr-0040-trusty-mcp-services` (another session's), not main.

## Completed
- Renamed session to tm-memory (tm session rename; note `tm session` is deprecated in favour of `tm sessions`)
- Confirmed trusty-memory's MCP attach point: `serve --stdio` is primary and speaks newline-delimited JSON-RPC on stdin/stdout, but is a PURE BRIDGE — forward_rpc POSTs every request to {base_url}/rpc and the real dispatch (initialize/tools/list/tools/call) happens in the HTTP daemon. Other surfaces: POST /rpc (full MCP), GET /sse (activity events, NOT MCP), /api/v1/* (REST), in-process MemoryMcpService via rlib. trusty-bm25-daemon does NOT speak MCP.
- Created GitHub milestone 46 'trusty-memory 0.23 — Recall, Rooms & KG Trust' — the first memory-scoped milestone. 36 issues. Naming constraint: `tm 1.3.x` means trusty-MPM in this namespace, so the crate name is spelled out.
- Label-hygiene fix: 10 genuine trusty-memory issues carried no `trusty-memory` label; a label-only scoping pass misses them. The `tm` label is an empty orphan (NOT a trusty-memory alias) and `ws/tm-memory` is a session-workstream label that also tags trusty-search and tcode work.
- Filed #5329 — collapse trusty-bm25-daemon into the main memory daemon. Sole consumer is trusty-memory; trusty-search runs the same index in-process; gated behind TRUSTY_BM25_DAEMON and NEVER enabled (zero index dirs across 102 palaces); its recorded rationale is an analogy contradicted by its own cited precedent.
- Researched KG-vs-lexical recall against Kuzu, mem0 and Graphiti/Zep. Key findings: NO surveyed system drops vector search — the graph is additive everywhere; mem0's OSS package has NO graph store at all (the graph-memory docs describe the closed Platform, and what replaced it is a numeric entity boost capped at 20% of ranking weight, contributing no content); and NO published eval measures injected-context quality with token budget fixed and content type varied.
- Wrote, ratified and merged ADR-0038 (c2970687f) — the KG stays additive in recall; a KG-primary lane is gated on three ordered extraction-quality preconditions. Vetted against ADR-0028 (Extends), 0027, 0004, 0009, 0010, 0031.
- MERGED #4810 (PR #5360, squash b5f968ca3) — precondition 1. redb TRIPLES key widened to (subject, predicate, object); FUNCTIONAL_PREDICATES is an INVERTED list (15 single-valued named, everything else multi-valued by default); kg/adjacency.rs upsert_edge now filters on target as well as predicate; at-open per-palace migration in one redb transaction after a size-verified backup.
- MERGED #4776 (PR #5382, squash 7106af44) — precondition 2, first half. kg_list_subjects MCP tool. The capability already existed at store/service/HTTP layers; only MCP wiring was missing. Second commit fixed a `truncated` flag that could not distinguish a filled page from an exactly-limit total.
- Filed #5363 (PATTERN_TABLE triples grow unbounded post-#4810 — the code-critic HIGH) and #5381 (palace-not-found returns JSON-RPC -32603 INTERNAL_ERROR and leaks the daemon's absolute filesystem path; reachable from every kg_* tool plus palace_info/reembed/compact).
- Cleared a four-commit-old Version parity red on main by confirming PR #5348's trusty-mpm 1.3.6 bump was correct, unblocking another session's release.

## In Progress
- NOTHING is executing. Zero agents in flight. The only open state is a provisioned, clean, unused worktree at .claude/worktrees/4775-kg-query-honest-miss (branch fix/4775-kg-query-honest-miss, HEAD 7106af44) waiting for #4775 implementation to begin.

## Next Steps
- #4775 — honest kg_query miss. Worktree ready. Rung 3. Design is in the palace; do not re-derive. Make kg_query distinguish subject_not_found from graph_empty via a structured `graph_state` field plus always-present kg_triple_count, and have the miss hint name kg_list_subjects. FIRST make the existing test kg_query_emits_hint_when_palace_empty FAIL — it passes today while asserting a claim the code can prove false.
- #4678 — extraction precision, ADR-0038 precondition 3. Not started. The regex/pattern-table extractor emits stopwords as first-class entities (`the` degree 23, `a` degree 12).
- #5363 — PATTERN_TABLE triples now accumulate without bound; #4810 removed an overwrite that had been an accidental growth cap and nothing replaced it. Two candidate remedies stated in the issue, neither chosen.
- #5381 — palace-not-found error class and path leak. Pre-existing, wider blast radius than the KG tools.
- #5329 — collapse trusty-bm25-daemon. Filed, unstarted.
- File the deferred follow-up: prefix filtering on kg_list_subjects (tag:/room:/topic:). Mechanically easy since ACTIVE_SUBJECT_COUNTS keys are raw subject bytes, but it touches trusty-common and would promote the work to rung 4.
- Fix the stale prose pointer at crates/trusty-agents/src/api/server/agent_kg.rs:80 — it names kg_routes::MAX_KG_LIST_LIMIT, which moved to service/core_kg.rs. Deliberately left out of PR #5382 to avoid dragging a second crate in.
- Owner decision outstanding: whether to file the untracked-diff gate blind spot. #4618's scan-floor guard SHIPPED and does NOT fix it — a floor catches a gate that examined nothing, not one that examined everything except your untracked diff (3784 vs 3790 files against a floor of 500). No open issue covers it.

## Git Context
Branch: main
Last commit: 695f1b16 chore(trusty-analyze): bump 0.8.0 -> 0.9.0 for release (#5340)
Uncommitted changes: 3 file(s) changed

## Tmux Window
tm-memory:0:@326
