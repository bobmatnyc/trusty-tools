# Session Pause - 2026-08-11T13:13:09.590664+00:00

## Summary
Session tm-memory, scoped to trusty-memory KG work. TWO PRs MERGED (#4775, #4678), ONE OPEN AND GREEN (#5412), a two-lane bake-off RESOLVED, and FOUR follow-up issues filed.

🔴 TWO AGENTS WERE IN FLIGHT AT PAUSE AND ARE NOW DEAD. Verify both directly on resume — do NOT assume either finished, and do NOT re-dispatch a duplicate before checking:
1. rust-engineer productionizing #5399 lane A in its own isolated worktree, branch `fix/5399-wordnet-pos-filter`. Check for that branch and any commits on it.
2. local-ops rebuilding + reinstalling the trusty-memory daemon from a NEW worktree `.claude/worktrees/rebuild-install-20260811` off origin/main, then `launchctl kickstart -k gui/$(id -u)/com.trusty.memory`. Check whether the install and restart actually happened — the acceptance test is BEHAVIOURAL, not a version string (see Next Steps).

🔴 PRUNE DELIBERATELY DISABLED (`prune_worktrees: false`) — both dead agents own worktrees, plus `5396-retract-triple` (PR #5412 open), `5399-lane-a-wordnet` (the winning spike, source for the productionization), `rebuild-install-20260810` (tm doctor's binary_provenance reads it), `ci-nextest` and other sessions' trees. Do NOT sweep.

origin/main was `fec999b6` (#4678's squash) at pause; it moves continuously — several sessions merge concurrently, so re-read it rather than trusting that SHA.

ADR-0038 STATUS: preconditions 1 (#4810) and 2 (#4776 + #4775) are COMPLETE. Precondition 3 is OPEN by owner ruling — #4678 shipped only the lexical half, and #5399 is the real gate. The KG recall lane stays blocked until #5399 lands.

## Completed
- #4775 honest kg_query miss — PR #5385 MERGED squash 41ca5c64, issue closed, branch + worktree cleaned. kg_query now returns graph_state (subject_not_found vs graph_empty) plus an always-present kg_triple_count; on a HIT graph_state is ABSENT, so its absence means the subject was found. code-critic APPROVE.
- #4678 KG extraction precision — PR #5402 MERGED squash fec999b6, issue closed, branch + worktree cleaned. Ships a ~190-entry closed-word-class STOPWORDS list + 3-char floor with short-entity allowlist, an opt-in `kg-rebuild --purge-stale-subjects` with --dry-run, and a token-edge fix (the old trim set omitted backticks/brackets/asterisks, so markdown-wrapped names kept their wrapping on BOTH edges).
- #5402 review found TWO CRITICALs, both fixed in-PR: (1) the purge could report a FAILED delete as succeeded — fail-open, no error-arm test; (2) --dry-run opened palaces via PalaceHandle::open, which HARD-DELETES expired SessionEvent drawers (handle.rs:359) — a data-loss path inside a flag whose whole purpose is to not write. Fixed in-crate by routing purge_one through KnowledgeGraph::open_with_intent (the prune lives in PalaceHandle::open, not KnowledgeGraph::open) and returning before load_palaces_from_disk.
- #5396 retract_triple — implemented, rung 4, PR #5412 OPEN and green. Adds retract_triple(subject,predicate,object) to trusty-common plus a KnowledgeGraph wrapper and adjacency remove_edge_to. REVERSES #4810's documented 'deliberate non-goal'.
- #5399 BAKE-OFF RESOLVED — two full prototypes built and measured on an identical 7-case eval set. Lane A (WordNet, pure Rust) WINS: +6.85ms start, +0.139us/call, +9.2MiB RSS, ZERO new dependencies, one self-contained binary. Lane B (resident spaCy sidecar): 450ms start, 1.19ms/call (~7500x), 110MB RSS, 171MB disk, 46 packages, 2x MPL-2.0, uv required.
- Filed this session: #5384 (count_active_triples fail-open), #5396 (object-side retraction), #5399 (context-aware extraction — the real ADR-0038 gate), #5401 (punctuated-twin dedup, likely depends on #5396). Commented evidence on #5078 (SKIP_UI_BUILD=1 not holding), #4125 and epic #3524 (embedderd).
- Verified trusty-memory owes NO version bump — main declares 0.22.0, crates.io latest is 0.21.2, so #4775's API removal rides a pending unpublished MINOR set on 2026-07-28. My earlier 'bump owed' claim was WRONG and is corrected in the palace.

## In Progress
- rust-engineer (DEAD at pause) — productionizing #5399 lane A onto a branch `fix/5399-wordnet-pos-filter` in its own isolated worktree, based on origin/main now that #4678 has landed. Four required changes beyond the spike: (a) SHIP RE-WALK ALWAYS, dropping the adjective-only reject rule entirely; (b) remove the OnceLock global (CLAUDE.md bans global state outside the tracing subscriber) by threading the table through KgExtractConfig; (c) fix the markdown terminator bug — `proxy.**` ends in `*` not `.` so the run walks into the next clause; (d) ship the 7x data reduction (6.07 MiB -> ~870 KB lemma-only projection) as a COMMITTED pre-generated file, NOT a build.rs step, since trusty-memory's build.rs already owns the UI build. Also: invert rather than delete the pinning test, and re-measure every number on the final tree.
- local-ops (DEAD at pause) — rebuild + reinstall trusty-memory and restart com.trusty.memory. Was told to build from a NEW worktree `.claude/worktrees/rebuild-install-20260811` off origin/main, `SKIP_UI_BUILD=1 cargo install --path ... --locked` (NEVER cp on macOS — stale cdhash cache makes the next exec SIGKILL), then `launchctl kickstart -k`, and to report what was connected before restarting since port 7070 is shared with 11+ other sessions.

## Next Steps
- FIRST: ListAgents + check for branch `fix/5399-wordnet-pos-filter` and worktree `.claude/worktrees/rebuild-install-20260811` before dispatching anything. Two agents died mid-flight; re-dispatching a duplicate is an error this repo has made repeatedly.
- VERIFY THE DAEMON REBUILD BEHAVIOURALLY, NOT BY VERSION STRING. All three changes (#4776, #4775, #4678) shipped with NO crate version bump, so the binary reads 0.22.0 before and after — a version check passes against the stale binary. The real tests: (1) does `kg_list_subjects` appear in the served tool list, where it was absent; (2) does kg_query for a nonexistent subject return graph_state=subject_not_found + kg_triple_count, rather than the old 'Knowledge graph is empty' hint against a ~13,830-triple palace.
- PR #5412 (#5396 retract_triple) is OPEN, rebased onto a5a87763, all four rung-4 gates green, body corrected. NOT reviewed and NOT merged. Needs code-critic, then merge. Merging requires --admin: the author bobmatnyc cannot self-approve, and code-critic's verdict is a tool verdict, not a GitHub review.
- #5401 (punctuated-twin dedup) and #5384 (count_active_triples fail-open) are QUEUED behind #5396 merging — #5401 needs the retract_triple primitive, and #5384 would collide with #5396 on trusty-common's version. Do not start either until #5412 lands.
- #5399 OUT-OF-SCOPE RESIDUES to file once its PR is up, all measured and pinned by tests in the spike: only `of` is NP-continuing (`a participant in the process group` still truncates, and widening the preposition set breaks `uses redb for persistence`); WordNet indexes BASE FORMS ONLY so inflected words silently skip the check (`parsers` -> mask 0, `quicker` indexed as an adverb); single-token heads truncate compounds (`system package managers` -> `managers`).
- A SECOND daemon rebuild will be owed once #5399 lands — the one in flight at pause covers #4776/#4775/#4678 only.
- OWNER DECISION STILL OPEN, conditional and now largely moot: 'make uv required' was ruled for the spaCy lane, which LOST. The general principle it produced stands and is UNCONDITIONAL — a degraded fallback is allowed, silent degradation is not. Already satisfied for embedderd (shipped 0.42.1); the remaining embedderd gap is SELECTION not reporting, recorded on epic #3524.
- DO NOT re-derive the #5399 design or the bake-off — both are recorded in the palace (drawer 4528af3a). Note especially that lane A's 7/7 was a LEXICOGRAPHIC ACCIDENT (WordNet records a noun sense for `fast`, none for `hard`), NOT semantic capability, and that MY eval row 1 was WRONG — re-walking yields the true triple `exhaustiveness is-a requirement`, and the corpus measurement (196 of 304 dropped pairs had a recoverable head noun) is what caught it.

## Git Context
Branch: main
Last commit: b9b99d316 docs: add website link to README (#5330)
Uncommitted changes: 10 file(s) changed

## Tmux Window
tm-memory:0:@326
