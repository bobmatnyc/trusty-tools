# Session Pause - 2026-09-20T23:41:07.118950+00:00

## Summary
Session tm-code (f6230f), trusty-code v0.7.0 (milestone 87, epic #7939, spec docs/specs/DOC-75-tcode-tui-mvp-parity.md). Owner instructions this session: "Merge" → chosen option: gate #8312 then merge it; "After #8312 merges, cargo install and run the DOC-75 §6 live runs"; "Kill the stalled build and rerun the gates" (sent to the running qa agent). Main checkout now at f9fe36ddf (origin/main moved past d642b0cb8 via other sessions: 1df52463f, then #8316); untracked .pnpm-store/ only. Two draft PRs open: #8312 (Refs #8293, branch feat/8293-shared-pm-routing-table-r2, tip 55c8c2113, rung 4) and #8317 (Refs #8204 #8289, branch feat/8204-8289-finish-task-evidence, tip a39332b6b, rung 4). Issues #8293 #8204 #8289 at status:coded with PR comments. CI on main d642b0cb8 and 5bbef0d01 was green (only opt-in embedder-cuda job skipped). Process facts learned: qa agents are provisioned into their OWN isolated worktree and the sandbox refuses git ops against another agent's worktree — a qa brief must say "fetch the branch, checkout --detach <tip> in your own worktree", never name the engineer's worktree; check_workspace_dep_versions.sh validates only root [workspace.dependencies] rows, not per-crate path pins (trusty-agents pinned trusty-agents-common = "0.8" and broke resolution after the 0.9.0 bump; fixed in 55c8c2113 by { workspace = true }); tm pr open has no --draft, so draft PRs use gh pr create --draft; a 0.x minor bump also needs the root workspace requirement bumped; cold gate runs take 2–3 h and up to 9 h under host contention, engineers' 3–4 h boxes are too small for a cold worktree; one cargo builder per session, other sessions build concurrently. Merge queue for trusty-tools is owned by session tm-trusty-tools-01 (Tm-dogfood); owner said Merge here — merge #8312 with gh pr merge --squash (never tm pr merge while agent worktrees exist) and tell Tm-dogfood in one message. Estimates given to owner: first live UAT (§6 runs on installed build) ~4–7 h after the #8312 gates pass; feature-complete UAT ~3–4 days of one-builder session time for the 10 remaining items.

## Completed
- #8293 r2 engineer (agent aa5a49a2038bd1097) closed critic findings F2–F7 on feat/8293-shared-pm-routing-table-r2 (F4/F7 with pre-fix red proofs); qa on 98763efb8 MERGE-READY (rustdoc, workspace check, three suites green, 3 load flakes re-ran clean)
- local-ops: version bumps trusty-agents-common 0.8.1→0.9.0, trusty-mpm 1.6.3→1.6.4, root workspace req 0.8→0.9 (b44114e6b); then trusty-agents Cargo.toml:215 → { workspace = true } (55c8c2113), cargo metadata --locked resolves, pushed
- security scans CLEAR on b44114e6b (#8312) and a39332b6b (#8317); both merge cleanly onto origin/main
- Draft PR #8312 opened; #8293 → status:coded
- #8204/#8289 engineer (agent af39f76668e6ae07c) delivered typed FinishReport / Event::TaskFinished / ReplEvent::TaskResult + verify_gate::evidence on feat/8204-8289-finish-task-evidence (a39332b6b); 22/23 trusty-code targets green, one red e2e (agents_e2e::never_run_session_returns_an_empty_roster_over_the_wire, session.create timeout, 3 of 4 runs, baseline on main unknown); workspace check not run
- Draft PR #8317 opened; #8204 and #8289 → status:coded
- One-shot CI read on main d642b0cb8 / 5bbef0d01: green
- Told Tm-dogfood twice that Tm-code builder slots freed (session 6af16fb2 holders are not ours)

## In Progress
- #8312 final gate run: qa agent a7d44ce63f826f400 in its own worktree on tip 55c8c2113, CARGO_TARGET_DIR=<its worktree>/target-qa. Gate 2 (SKIP_UI_BUILD=1 cargo check --workspace) stalled ~3h35m; owner ordered kill + rerun — message sent to the agent: kill the build, keep target-qa, rerun from gate 2, 45-min silence rule then NOT RUN and continue. Remaining gates: clippy (4 crates incl. trusty-agents), rustdoc, test -p trusty-agents-common / trusty-mpm / trusty-code / trusty-agents, four scripts. Named tests to confirm: fill_refuses_a_duplicated_placeholder, mpm_delegation_section_is_rendered_from_the_shared_rows, the_rendered_delegation_section_stays_within_its_byte_cap, pm_card_routing_block_is_rendered_from_the_shared_rows, delegate_mode_prompt_carries_the_routing_table. If the agent did not survive the relaunch: re-dispatch qa with the same brief (own worktree, checkout --detach 55c8c2113); have local-ops check pgrep -x cargo for an orphan first.
- #8317 owed gates (aborted to yield the builder; nothing produced): agents_e2e baseline 4 runs on origin/main vs 4 runs on a39332b6b with load counts → PRE-EXISTING / REGRESSION / LOAD-FLAKE; SKIP_UI_BUILD=1 cargo check --workspace on a39332b6b; named evidence/finish_verify tests + test -p trusty-code-tui (259). Re-dispatch after #8312's builder frees.
- Owed small follow-up once #8312 lands: one pm.md bullet telling the PM to keep a todo_write checklist (do NOT add todo_write to tcode_tools).

## Next Steps
- On #8312 MERGE-READY: version-control un-drafts and squash-merges #8312 (gh pr merge --squash), fast-forwards main checkout; ticketing moves #8293 → status:merged (rung 4, live proof owed); one message to Tm-dogfood that Tm-code merged #8312 on owner instruction
- Then local-ops: cargo install --path <clean main checkout>/crates/trusty-code --locked (never cp), report version + SHA
- Then qa: DOC-75 §6 runs on the installed build — solo (launch, request change, permission prompts, quit, relaunch, resume) and delegate (tcode tui --delegate, small change, PM hands research → engineer → qa, PM writes no source); record to docs/. Expected gaps: resume (#8185) not built, subagent panel (#8182) and typed slots (#8317) not merged. If the qa sandbox cannot drive a pty via tmux (#5784 guard), prepare the build + keystroke script and hand driving to the owner. Live results decide status:tested → close for #8207 #8238 #8235 #8287 #8227
- Re-dispatch #8317 gates (see in-progress); on PRE-EXISTING/LOAD-FLAKE + green workspace check: un-draft, merge, #8204/#8289 → status:merged
- Delegation order after #8317: #8182 (statusline + subagent panel, consumes ReplEvent::TaskResult / ReplApp::finished_tasks) → #8288 → #8294 → #8201; track-1 items #8185 #8221 #8222 #8223 #8203 can run in a second builder session; #8205 / PR #8247 (open, green, unreviewed, predates session) needs owner's call
- Docs pass after #8312: stale citations docs/specs/DOC-76-tcode-tui-parity-map.md:55 and crates/trusty-mpm/src/assets/skills/tm-circuit-breaker.md:326 now point at pm_routing.rs
- Prompt-feedback rollup comment on #8021 (one dated comment, dedupe): qa briefs must not name another agent's worktree; check_workspace_dep_versions.sh misses per-crate path pins; 0.x minor bump needs root workspace req bump; tm pr open lacks --draft; known-flake lists should say 'including'; engineer boxes too small for cold worktrees; 'one commit per issue' vs shared wire type; rustdoc parent-scope link fix example; tm pr open --issue takes one issue; worktree guard refuses grep/awk on scratch logs; rustdoc + version-bump gates missing from engineer briefs; cargo check multi -p stops at first error; SKIPPED CI contexts read as passing; split-PR issue lifecycle rule; pre-existing lock().unwrap() x3 in trusty-code-tui run/mod.rs; mcp_loader_e2e takes 971s per trusty-code test run
- Housekeeping: session rename to tm-code still not done; clean detached qa worktrees agent-ac203563274257151, agent-a670606e344ba469a, agent-a28faa97985ccdb49, agent-a9c1530756b438e71, agent-a9342a583c27544ed, agent-ab29e465ced4969a2 remain; worktree pruning skipped in this pause on purpose (live qa build)

## Git Context
Branch: main
Last commit: f9fe36ddf fix(trusty-search): refuse a new index root that overlaps an existing root (#4289) (#8316)
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-code:0:@32
