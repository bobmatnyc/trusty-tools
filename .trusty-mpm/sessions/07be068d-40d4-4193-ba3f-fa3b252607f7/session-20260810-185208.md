# Session Pause - 2026-08-10T18:52:08.433970+00:00

## Summary
🔴 NINE AGENTS WERE LIVE AT PAUSE and background agents survive — VERIFY EACH DIRECTLY via gh/git/ListAgents before re-dispatching anything. Worktree prune DELIBERATELY DISABLED (prune_worktrees: false) because most of them hold worktrees.

🔴 THE MAIN CHECKOUT IS NOT ON `main`. It is on branch `docs/adr-0040-trusty-mcp-services` with staged ADR work (the 0040 file, plus modified `0033-*.md` and `INDEX.md`), because an agent branched in place to rescue uncommitted work. HEAD = c2970687f = origin/main. Return it to `main` once that PR is open. Owner's own `.gitignore` + `.claude/settings.json*` changes are also uncommitted there and are NOT ours to commit.

SHIPPED THIS SESSION: ADR-0037 amendment merged (12b68ea5b) — main checkout is read-only except documents and configuration. PR #5314 merged (96123699e), closing #4480 — the shared-working-tree dispatch guard. Worktree cleanup 51 of 51, 786G freed, tga session untouched, `memory-serve-stdio` preserved on the remote at dc19d5098.

ARCHITECTURE THREAD, all recorded in three pushed ADRs: the CONSUMER CRITERION (consumer is the agent → MCP; consumer is the framework → native) is the decision rule behind all of it. ADR-0039 operator-named sessions unique by construction. ADR-0040 `trusty-mcp` extracted from `trusty-common`, `trusty-mcp-services` beside it, supersedes ADR-0033 as an ERROR not a supersession. ADR-0041 `trusty-okg` native, MCP service fronts it, search absorbs OKF indexing.

OWNER RULINGS TODAY, all binding: main-checkout guard PARKED until "we know it's a problem"; ADR PRs are `--admin` mergeable; a TIME-BOXED FREEDOM WINDOW to deprecate/restructure freely that ENDS AT PUBLIC RELEASE THIS WEEK; no gate for ADR numbering, a claim-then-populate instruction instead; `trusty-channels` is native by intent though its current contents are agent-shaped and unconsumed; label renames affect FUTURE work only.

RECURRING FAILURE SHAPE, unchanged from the last session and hit repeatedly today: A CLAIM OF VERIFICATION RATHER THAN A CODE DEFECT. A stale ci.yml comment cost us the nextest lever for ten days. A false doc comment in `trusty-common/src/mcp/openrpc.rs` would have made a migration that silently breaks 61 of 62 tools look safe. Three separate questions today were answered by decisions already on record that nobody retrieved first.

## Completed
- ADR-0037 amendment MERGED (12b68ea5b) — main checkout read-only except docs/config; row-4 corrected: tm never created agent worktrees
- PR #5314 MERGED (96123699e) — dispatch guard denying a second unisolated engineer into one working tree; #4480 auto-closed; code-critic APPROVE; Fail-Open Check 6 of 6 with both new tests verified causal by mutation
- Worktree cleanup COMPLETE 51 of 51 — 786G freed, 65 → 14 worktrees; tga's three worktrees untouched; memory-serve-stdio preserved on remote at dc19d5098; provider-pinning force-removed after preservation check (5359 files deleted out-of-band, cause unexplained, owner ruled remove-and-move-on)
- Milestone tm 1.3.6 corrected: criterion c satisfied-by-ADR, criterion d DROPPED as unbuildable-as-stated, criterion a2 recount to five tests across four mechanisms (the prior 'six tests across three' was wrong even before #5328 was added)
- ADR-0039 written + revised + pushed (1c6a8b9ac) — named sessions skip the serial, attach-if-live/create-if-terminal, SlotRegistry gains a name index
- ADR-0041 written + pushed (75dfa0f5) — trusty-okg native, MCP service in trusty-mcp-services fronts it, trusty-search absorbs OKF indexing
- Issues filed: #5301 tm launch --name-hint + normalizer, #5307 subagent cwd defects, #5310 stray listener state, #5311 branch-protection design pass, #5324 dispatch reservation step, #5325 CI Test duration, #5327 slot retention, #5328 trusty-embedderd-py flake, #5331 gworkspace OpenRPC duplication
- #5299 CLOSED as superseded — its premise died when ADR-0037's row 4 established tm never created agent worktrees

## In Progress
- a21c6adc9fb43b8af version-control — renumbering our ADR to 0040 IN THE MAIN CHECKOUT on branch docs/adr-0040-trusty-mcp-services. Peer session tm-memory merged a different 0038 (c2970687f, PR #5332) while ours sat uncommitted. Staged: 0040 file, 0033 status line, INDEX.md. MUST end with the main checkout returned to `main`.
- a7aab658fca545113 version-control — opening PR for ADR-0039, branch docs/adr-0039-session-name-identity, head 1c6a8b9ac
- a2d0b66397ff15ddb version-control — opening PR for ADR-0041, branch docs/adr-0041-trusty-okg, head 75dfa0f5
- a49c9af9ae7a52fd6 rust-engineer — ADR claim-then-populate instruction. CORRECTED MID-FLIGHT: check_doc_numbers.sh DOES cover ADRs (job 'DOC-N / ADR number allocation lint'); it was blind because collect_claims() uses `git ls-files`, which sees tracked files only. So the instruction must say `git add` the stub AT CLAIM TIME. The 0021 pair is deliberately grandfathered via .doc-number-allowlist.tsv, not an oversight.
- a9b359d97abc7cd64 rust-engineer — rebasing PR #4978 (n [name] picker) onto main after a real conflict with #5035's picker rework. MUST preserve all three hardening properties from commit 7fdbf4d7 and open a NEW PR rather than force-pushing #4978.
- a357cf9c665973999 rust-engineer — #5324 atomic record-and-answer for the dispatch guard. MUST invert rather than delete pm_guard_admits_both_dispatches_that_query_before_either_is_recorded; must not regress the Fail-Open count from 6.
- aa2fb117a9225a5d2 rust-engineer — #5327 slot retention. Rung 5, hard-deletes records. tm launch --worktree still provisions a worktree so the workspace-gone precondition may narrow but NOT be deleted.
- abf169878c410d44b rust-engineer — live issue status: resident PM-section trigger POINTING at tm-ticketing's Lifecycle section (not restating it), plus in-progress/blocked labels created at session launch via ensure_workstream_label's precedent.
- a4edaac26f9c6fe4a rust-engineer — PR #5326 two approved fixes (explicit numeric validation in measure_avail_gb WITH a selftest case; correct the over-attributed -11.4% headline to the ~86s actually attributable), then nextest adoption. NEXTEST IS UNBLOCKED — the ci.yml:826-843 comment describing the blocker is STALE; #4413 fixed it via PR #4482 on 2026-07-31, re-verified today at 83 passed under isolation.

## Next Steps
- FIRST: ListAgents + gh/git to verify all nine directly. Background agents survive a pause. Do NOT re-dispatch a duplicate — that error has been made twice in this repo.
- SECOND: return the main checkout to `main`. It is on docs/adr-0040-trusty-mcp-services. Leave the owner's .gitignore and .claude/settings.json* alone.
- 🔴 OWNER DECISIONS OUTSTANDING, none blocking: (a) DOC-N spec allocation for the consumer criterion — it governs all three ADRs and is normative nowhere; (b) kb_put_entity placement — exposing it gives agents an unguarded write path bypassing ledger/TrustLabel, not exposing it leaves a native write nothing can reach; (c) matrix sharding, which needs a branch-protection change; (d) the CI build phase, 43% of the Test job and untouched; (e) which trusty-okg piece moves first — rename, MCP registration, or the scheduled runner; (f) crate-ownership milestone reorg, which is why milestone edits are paused.
- 🔴 THE FREEDOM WINDOW ENDS AT PUBLIC RELEASE THIS WEEK. Breaking work — the trusty-mcp extraction (breaking to published trusty-common 0.30.0), the trusty-kb → trusty-okg rename (free: never published, 51 call sites/18 files/3 Cargo.toml/20 docs) — is cheap now and costs a deprecation cycle after. Non-breaking work can wait; breaking work cannot.
- CI: #5326 approved and ~86s attributable. nextest ~200s now unblocked. The BUILD phase is 43% of an 18-minute job, compiles 25 crates and links 194 test executables with a full dependency cache hit, and NOTHING targets it — 194 executables and test-profile debuginfo are the two coverage-neutral levers.
- PRs awaiting merge: #5326 (approved, two fixes pending), #5330 README website link, #5334 (OpenRPC golden artifact; the refactor itself is BLOCKED and correctly did not land — OpenRpcBuilder renames x-google-scopes to x-scopes and hardcodes it, which would scope-deny 61 of 62 gworkspace tools silently). Owner ruled: extend OpenRpcBuilder with a caller-settable extension name and an info.license slot.
- GATE-INTEGRITY GENERALIZATION worth acting on: check_doc_numbers.sh, check_test_pointers.sh and check_line_cap.sh ALL use `git ls-files` and therefore pass vacuously over unstaged work. Stage before gating; a gate table in a PR body is not evidence the gate saw the diff.
- trusty-kb is NOT obsolete — trusty-agents' OKG feature is built ON its library. Its standalone MCP binary was never once wired into .mcp.json in the repo's whole history. The canonical model's 'indexed by trusty-search' is NOT automatic: no default binding, no scheduled runner despite DOC-63 §9 specifying one and the owner approving #4538, no file-watching.

## Git Context
Branch: docs/adr-0040-trusty-mcp-services
Last commit: c2970687f docs: ADR-0038 — KG stays additive in recall, gated on extraction quality (#5332)
Uncommitted changes: 6 file(s) changed

## Tmux Window
tm-dogfood:0:@230
