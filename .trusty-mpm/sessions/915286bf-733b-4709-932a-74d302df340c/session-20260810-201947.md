# Session Pause - 2026-08-10T20:19:47.499960+00:00

## Summary
🔴 THREE AGENTS WERE LIVE AT PAUSE and background agents survive — VERIFY EACH via ListAgents + gh/git BEFORE re-dispatching anything. Worktree prune DELIBERATELY DISABLED (prune_worktrees: false) because live agents hold worktrees.

SIX PRs MERGED THIS SESSION. origin/main was `2d964aefc` when the last merge train finished; a peer session (tm-tga, trusty-analyze 0.9.0 release) is landing into the same main, so RE-READ the tip rather than trusting that SHA.

THE ADR TRAIN IS COMPLETE: #5338 ADR-0039 (`e567a983d`), #5348 trusty-mpm 1.3.5→1.3.6 (`cd8ac1490`), #5342 ADR-0040 (`2e133d326`), #5339 ADR-0041 (`a83e810a5`), #5343 ADR numbering claim-then-populate (`2d964aefc`). Verified ON MAIN, not reported: INDEX lists 0039/0040/0041 ordered with 0033 "Superseded by 0040"; `tm-adr.md:67` carries the new section.

🔴 THE 1.3.6 RELEASE GATE IS MCP, AND THE OWNER RULED ON ITS SHAPE. Bob cannot use trusty-mpm until MCP works; the in-flight PRs ride the MCP-fix release; nothing publishes until he can use it. Ruling: "MCP configuration should be static and persistent" + "We should allow changing/modification, but injection shouldn't be needed." Full ruling and evidence in palace drawer `15af97fb` (tag `mcp-config-is-static-not-injected`).

🔴 I STATED I WAS DISPATCHING ADR-0042 FOR THE MCP DECISION. I DID NOT. No such agent ran and no ADR exists. That is the first thing to pick up.

RECURRING FAILURE SHAPE, hit five times today: A STALE OR UNVERIFIED CLAIM, not a code defect. #4181's body cited a two-week-stale line of source. `native_mcp.rs`'s doc argued for a bridge whose premise #4451 removed three weeks ago. A peer declared an edit unrecoverable without running `git fsck`. Two agents and I all asserted a landed version bump clears a gate that reads the branch, not main. I claimed a dispatch I never made.

## Completed
- ADR train merged, 5 PRs: #5338 ADR-0039 e567a983d, #5348 version bump cd8ac1490, #5342 ADR-0040 2e133d326, #5339 ADR-0041 a83e810a5, #5343 numbering instruction 2d964aefc — INDEX + tm-adr.md verified on main
- #5348 cleared main's four-commit version-parity red. crates/trusty-mpm/Cargo.toml is 1.3.6 on main
- code-critic APPROVE on #5341 (3 findings fixed: WorktreeDirNames hoisted out of the per-record loop, 2 stale-doc), #5352 (MEDIUM: restart prompts no longer invite `n` as "no"), #5353 (3 fixes: claim payload projected to 3 fields, `claimed` divergence now warns, doc comment)
- #5352 carries trusty-common 0.30.0→0.30.1, justified by check_semver.sh 196/196 pass; agent caught and reverted a proc-macro-crate 2.0.0→1.3.1 lockfile drift (the MSRV trap)
- Milestone 36 body corrected: a2 line no longer claims the flake family is fixed when all four issues are open
- Issues filed: #5354 (cargo test -p X aborts at lib target, hiding ALL integration targets), #5355 (resumed subagent type reverts to general-purpose), #5356 (pm_guard denied a read-only turn; P2, milestone 1.3.6). Gate-integrity defect B went as a COMMENT on #4474 — fifth recurrence of that feature-gate class
- #4181 rescoped by comment to the owner ruling, with its two stale claims corrected
- CI profiled across 9 runs: 1068s avg — build 44.7%, execute 40.2%, disk 8.5%, other 6.7%. Build is COMPILE-bound (mold measured, no effect). 194 executables, trusty-mpm alone 41 (21%)
- MCP user-scope visibility SETTLED empirically: claude_design, declared only in the managed .claude.json, connected in a daemon-managed session. native_mcp.rs's doc is stale; claude_code.rs is correct
- Main checkout audited after a peer agent's destructive git run, fast-forwarded 53953e85b→695f1b16f with the owner's three uncommitted files preserved byte-identical

## In Progress
- acc5c2dc35988b918 version-control — MERGING #5326 (perf/5325-ci-test-job-duration, head e8c4ae682599fe1bc1574d41e098614aa163a66a), then #5341 (fix/5327-slot-retention, 735ea915c8bfccf3fc35d642ce7b73b2405913e0), then #5352 (fix/4978-rebase-picker-name, daad1c657f84d5ce30c0efa7103879f894fa53e5). All three settled 29/29/30 checks, 0 not-green. VERIFY WHAT ACTUALLY MERGED before re-dispatching — this may be complete
- a2a3844c94818b98a rust-engineer — main-checkout destructive-verb guard. Denies git checkout -- <path> / restore / reset --hard|merge|keep / clean -f* when cwd is a registered main checkout, PIERCING Guard 4's subagent exemption the way #3977 and #4784 already do. Must keep .claude/worktrees/** writable and must not add to #5356's false-positive problem
- a4d8b25e70cad2df3 research COMPLETE but its recommendation is unactioned: pilot consolidating trusty-mpm's 38 integration-test files into fewer binaries. 41 of 194 executables from one crate; coverage-neutral by construction; saving NOT measured

## Next Steps
- FIRST: ListAgents + gh/git to verify all three directly. Background agents survive a pause. Do NOT re-dispatch a duplicate — that error has been made twice in this repo
- WRITE ADR-0042 for the MCP decision — I claimed this was dispatched and it was not. Next number after 0041; use the claim-then-populate protocol that just landed in tm-adr.md (create the stub and `git add` it AT CLAIM TIME, or the doc-numbers lint cannot see it)
- #5353 is fix-complete at 849f75e0d on rebased f737dda4c, 5 pass / 2 skipping / 18 pending / 0 failing at last read. NOT MERGED and NOT AUTHORIZED — the owner authorized #5341, #5352 and #5326 only. Ask before merging it
- 🔴 SEMVER DECISION OWED, and it is main's condition not any PR's: check_semver.sh --crate trusty-mpm reports BREAK — constructible_struct_adds_field on Delegation.isolation (core/agent.rs:275), inherited from #4480 and already on main. At 1.3.6 that needs the MAJOR position under normal SemVer. It surfaces at whatever release ships #4480. preflight-publish.sh CHECK 5 is the hard stop
- MCP removal is scoped and ready to implement once ADR-0042 is agreed. Deletion list, survival list, the ensure_managed_config_dir hook, and the trust answer are in the research report; the design that reaches true zero-injection is static argless declarations plus TRUSTY_MEMORY_PALACE / TRUSTY_SEARCH_INDEX exported by spawn_command. trusty-search needs an env fallback for --index (rung 4)
- claude mcp CLI verdict is HYBRID, not a clean wrap: `claude mcp add` is NOT idempotent (fails on re-add at same name+scope), and list/get have NO --json. So wrap add/remove for ad hoc use; keep tm's own idempotent writer for the one-time declaration and tm's own reader for --json
- OWNER DECISIONS OUTSTANDING: (a) matrix-sharding the Test job needs a branch-protection change — `Test` is a required check by literal name and a matrix renames it to `Test (shard-N)`; (b) whether to file the two unfiled defects — tm session decommission reports "workspace removed" and leaves the worktree on disk, and there is no `tm projects` unregister subcommand; (c) the trusty-search index-pin route (env var vs staying project-scoped)
- nextest is unblocked (#4413/#4482 fixed it 2026-07-31; the ci.yml:826-843 comment saying otherwise is STALE) and would recover ~200s, but has a named prerequisite: two trusty-memory tests depend on TRUSTY_SKIP_PALACE_ENFORCEMENT being set as a side effect by another test in the same process (palace_ops.rs:88-101). Nextest's process isolation exposes it as a real failure
- CROSS-SESSION HAZARD, third occurrence today: a peer's agent ran git checkout/reset/clean in the shared main checkout despite an off-limits brief, and the checkout's branch changed under a read-only investigation later the same hour. ADR-0037 states the rule and says enforcement is not built. a2a3844c is building it

## Git Context
Branch: main
Last commit: 695f1b16f chore(trusty-analyze): bump 0.8.0 -> 0.9.0 for release (#5340)
Uncommitted changes: 3 file(s) changed

## Tmux Window
tm-dogfood:0:@230
