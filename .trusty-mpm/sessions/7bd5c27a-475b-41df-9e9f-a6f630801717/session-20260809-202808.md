# Session Pause - 2026-08-09T20:28:08.843570+00:00

## Summary
HANDOFF to the new session now running on the MAIN CHECKOUT. Written by session 7bd5c27a (worktree .worktrees/7bd5c27a-475b-41df-9e9f-a6f630801717), which is now safe to remove. origin/main = 5a0553802. THE RATIFIED MODEL, owner-decided today: the PM runs from the project's main checkout; only agents that WRITE get a worktree, under .claude/worktrees/. Precedence: an explicit user request (tm launch --worktree) wins; otherwise the PM takes the main checkout and NO project config layer can override it. The per-project `worktree` flag survives with a narrowed meaning — it governs AGENT isolation only. trusty-tools stays `worktree: true`, and the 2026-08-08 ruling saying so is NOT superseded; it was always about agents. `worktree: false` means the whole project works on the main checkout, typical for writing projects. tm is BUILT, SIGNED-INSTALLED and BEHAVIOURALLY VERIFIED from 5a0553802: a spawn with no worktree request returns workspace_path=/Users/masa/trusty-mpm-projects/bobmatnyc/trusty-tools, daemon PID 34207 supervised on 7880, agent isolation confirmed still true. NOTE `tm --version` still prints 1.3.5 — #5274 shipped without a bump, so the version string proves nothing; sha256 and `tm launch --worktree` in help are the real evidence.

## Completed
- SEVEN merges to origin/main today, in order: #5268 -> fadcaf890 (track root AGENTS.md), #5269 -> 091246085 (dedupe worktree discipline into tm-workflow, CLAUDE.md 95 lines -> 25), #5270 -> b98950a3 (ADR-0036: all worktrees are siblings under .claude/worktrees, amends ADR-0020 via header note only), #5271 -> efab9add0 (ADR-0030 point 7 + DOC-66 section 5: parentage is a recorded parent_workstream_id, NOT path containment), #5273 -> 1a75111a1 (session snapshots resolve per-session, never across sessions; closes #5272), #5274 -> 5a0553802 (session starts on the main checkout, with an explicit --worktree override).
- .base RETIRED AND DELETED. ~850G reclaimed (disk 90% -> 67%). It was NOT a legacy object store: 892G, of which objects were 175M and the rest nested checkouts with Rust target/ dirs. 775 branches were unique to .base (not the 239 first estimated), 13 worktrees were dirty (not 2). Classification needed three verdicts: 712 IN-GIT (tip reachable from a .git ref, mostly via remotes/origin/*, nothing to preserve), 56 MERGED, 7 genuinely UNIQUE. Verified bundle at /Users/masa/trusty-tools-archive/base-retire-20260809.bundle (152M, 2249 refs, sha256 1512dce725db772533761cbb2b0042a3db6a43e4e8be4a84f18a7998cb1afe5c). A check nobody specified saved it: .git had no objects/info/alternates, so rm -rf did not destroy .git.
- preserved/* on origin pruned to the 7 real branches: preserved/docs/adr-0032-webhook-relay, preserved/fix/4203-launch-connect-deploy-tier, preserved/fix/5037-recall-relevance-floor, preserved/fix/5038-injection-tag-cap, preserved/fix/5045-doctor-index-check, preserved/fix/pm-guard-memory-md, preserved/fix/session-rename-500. All Aug 6-8 work that existed ONLY in .base; three name issues possibly already fixed on main, so they need triage. The 15 preserved/wip-* and preserved/wt-* snapshot branches were deleted by owner ruling.
- tm signed-installed from 5a0553802 via scripts/install-trusty-mpm-signed.sh. Both binaries re-signed under the SAME identifiers (com.trusty.tm, com.trusty.trusty-mpm, OU 4JH68XUHC5), so the macOS App Data TCC grant still matches and does not re-prompt. Daemon restarted with launchctl bootout+bootstrap, NOT kickstart -k (the install script says SIGTERM drains in-flight requests). PID 1760 -> 34207, single listener, session fleet intact.
- Issue #5272 filed and closed by #5273. Its disposition was NEW REGRESSION rather than reopening #2731: #2731 introduced the fallback chain and was correct under one-session-per-checkout; this is a new failure mode the same design produces once sessions share a store.

## In Progress
- NOTHING is in flight. Zero agents running, zero open PRs from this session. Everything listed above is merged and verified.

## Next Steps
- RELEASE BLOCKER, do not miss: trusty-common must go to 0.31.0, NOT the 0.30.0 currently bumped in-tree and untagged. #5273 narrowed its public API (latest_trusty_mpm_snapshot now refuses None; resolve_latest_snapshot's contract tightened), and Cargo's 0.x rule makes MINOR the breaking position. preflight-publish.sh CHECK 5 would stop a patch upload at publish time, but #4088 is what that costs when it is not caught early. NEVER run bump-version.sh; both crates are already bumped in-tree. trusty-mpm 1.3.5 is bumped and UNTAGGED, and now contains #5274.
- THE PM-PLACEMENT PRECEDENCE TABLE HAS NO ADR. ADR-0036 decides worktrees are siblings under .claude/worktrees and says NOTHING about where a PM session runs — the implementing agent correctly dropped that attribution. The four-case contract (PM+true -> main; PM+false -> main; PM+explicit request -> worktree; agent+true -> its own worktree) is recorded only in PR #5274's body. Record it as an ADR before the 1.3.5 cut.
- Small revert PR owed: crates/trusty-memory/ui/package-lock.json lost 39 lines inside squash 5a0553802 — npm rewrote it when build.rs ran the embedded Svelte build during cargo check --workspace, and the agent staged with git add -A. Dev-only optional-dependency metadata (libc arrays on @rollup/rollup-linux-arm-* entries), UI checks passed, no runtime effect. Restore to a223b5b00's state. Prevention: git add <paths>, never git add -A, in a workspace whose build.rs mutates tracked files.
- THIS REPO HAS NO COMMIT-GRAPH, DELIBERATELY. The stale cache was producing 161 'could not read' fsck errors; the three files under .git/objects/info/commit-graphs/ were archived to /Users/masa/trusty-tools-archive/stale-commit-graph-20260809/ and deleted. fsck now exits 0 and agrees with and without the cache. But `git commit-graph write --reachable` is a NO-OP here — four variants, all exit 0, zero bytes, zero files, GIT_TRACE shows the builtin dispatch and return. Cause unexplained. Effect is performance only on 4807 commits. Do not treat a missing commit-graph as damage.
- .worktrees/ CLEANUP NOT STARTED: 23 entries, 7 dirty, 2 decommissioned-but-never-deleted (what tm session prune-worktrees exists for). Give it the same preserve-first treatment .base got — that job turned a 2-file estimate into 13. This session's own worktree .worktrees/7bd5c27a-475b-41df-9e9f-a6f630801717 is now safe to remove.
- .claude/worktrees/ TRIAGE: 42 entries, all git-clean, 38 unmerged, 4 detached HEAD, and ALL registry-invisible by design (they never carry trusty-mpm's ownership sentinel, so prune correctly never proposes them). Needs human branch-by-branch judgment. Worked example of what is in the pile: wt-discipline on docs/worktree-discipline-to-skill is doubly superseded — its one commit moves discipline into tm-pr-workflow, which #5202 deleted, toward the same end #5269 reached properly.
- MAIN CHECKOUT STATE, left deliberately: branch docs/spec-updates-20260809 at 6478021f4 (not main), untracked AGENTS.md (now identical to what is tracked on main, so it reconciles to clean once the checkout returns to main), M .gitignore, untracked .claude/settings.json + .bak, and THREE stashes. stash@{0} 'release-135-preop-1786232779' is a SUPERSEDED draft of committed 6478021f4 — six of seven files identical, and its DOC-62 wording predates #5202 landing, so it is safe to drop. stash@{1} is unrelated feat-4026 WIP.
- DESIGN DIRECTION, not defects, both owner-raised: (1) MIRROR SESSION IDENTITY WITH CLAUDE CODE'S OWN. This session had two ids for one session — tm's 7bd5c27a (also the worktree and branch name) and Claude Code's ffe824c6, the key for ~/.trusty-tools/trusty-mpm/claude-config/projects/<slug>/<claude-session-id>/. That duplication is the root of the #5272 bug. Critically it is 1:N, not 1:1 — a tm session deliberately outlives one Claude conversation, which is the whole purpose of pause/resume-into-fresh-context — so record the SET of Claude session ids that served a tm session, never equate them. (2) MIRROR /color and /rename so the label you see matches the identity tm records. Matters more now that simultaneous sessions share one main checkout and no longer have distinct paths to tell them apart. Feasibility question to check first: whether those commands can be driven at spawn time or are interactive-only.
- GOTCHA that will look exactly like a regression: passing a REMOTE repo URL to a spawn routes to the clone path and returns a .worktrees/<uuid> path. spawn_managed_routed gates the main-checkout branch behind is_local_workdir(&params.repo_url). Use the local path.
- GOTCHA: spawning a session onto the main checkout deploys framework files INTO it — ~140 skills, a .claude/ refresh, and a TASK.md. Expect movement in the untracked .claude/settings.json files noted above.
- STANDING AUTHORIZATION from the owner, still live: --admin merge on any green PR in this sequence. It covers the review-approval requirement ONLY — never red, pending, or unrun checks. Always verify the head SHA the checks ran against equals the branch head before an --admin merge; this repo has a recorded incident of an admin-merge landing on a head CI never saw.

## Git Context
Branch: docs/spec-updates-20260809
Last commit: 6478021f4 docs: land DOC-59 §§11-12 baseline and cascade to specs/architecture
Uncommitted changes: 4 file(s) changed

