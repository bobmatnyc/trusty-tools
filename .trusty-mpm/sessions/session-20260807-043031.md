# Session Pause - 2026-08-07T04:30:31.213008+00:00

## Summary
1.3.5 leg, SECOND pause. SUPERSEDES session-20260807-040441.md. main = e97e40e6 at pause; #5055 and #5054 merges were IN FLIGHT and likely landed after — VERIFY.

🔴 ~12 BACKGROUND AGENTS IN FLIGHT. VERIFY EVERY STATE via `gh pr list`, `gh issue list`, `git worktree list`. Do NOT trust this snapshot for in-flight status and do NOT re-dispatch before checking.

🔴 A RESEARCH AGENT READ A STALE WORKTREE AND REPORTED "ADR-0031 DOES NOT EXIST". It does — docs/adr/0031-uds-for-inter-crate-transport-http-for-external.md, e8ea94ef is an ancestor of main, verified by `git ls-tree origin/main`. A worktree's FILES are frozen at creation while origin/main is a SHARED ref, so an agent can report a current SHA and stale content in one breath. ALWAYS verify with `git show origin/main:<path>`. This cost a full round of wrong transport conclusions.

STANDING AUTHORIZATION: "merge the rest when they're green too." QUALIFIED and unobjected: green is necessary, NOT sufficient — rung-5 work also needs its code-critic round, and never merge while a fix round is gating.

BOB DECISIONS THIS LEG (all new since the first pause):
- CONSOLIDATE ALL MCP SERVICES INTO trusty-mcp. It is a PROTOCOL ADAPTER, peer of trusty-console — console adapts the daemon layer to HTTP for browsers, trusty-mcp adapts it to MCP for MCP clients. Neither owns state. This kills the link-everything-into-one-binary concern: trusty-mcp never links ONNX/redb/usearch. Epic #5066.
- NO trusty-mcp-services crate. The 07-27 ruling already named ONE crate. Call-site test found exactly ONE complication: tc-services → cto-assistant calls trusty_gworkspace as a LIBRARY, so gworkspace needs a stable public API inside trusty-mcp's [lib]. Channels/kb/tickets have no external consumers, can be private modules.
- tickets-mcp → "trusty-tickets" is the SUBCOMMAND (`trusty-mcp tickets`), NOT a crate name. 07-27 said do not publish these under their own names. #5058's removal of the dead .mcp.json entry is PERMANENT, not a stopgap.
- REMOVE THE DAEMON FROM ANALYZE AND REVIEW. Fleet drops 6 → 3 (search, memory, mpm).
- MOVE THE ANALYZE UI TO CONSOLE. Recorded on #5066; console's detect/analyze.rs and proxy/routes.rs need REWRITING, not deleting.
- WEBHOOKS REMOVED from both review and analyze — "a webhook should be part of the calling application." STILL GATED on confirming what code intelligence actually calls (#5028 measured ZERO webhook deliveries in 14d while Bob says it uses the webhook).

🔴 THE PATTERN OF THE NIGHT, now FIVE instances: CLOSING A FAIL-OPEN RELOCATES IT ONE LAYER OUT unless you check the layer above.
1. palace_reembed still_missing_ids tests vector-KEY presence, not retrievability.
2. #5048 skipped a palace on doc_count >= total — COUNT standing in for SET. Fixed by a missing_docs op (coverage by identity).
3. #5048 AGAIN, after the fix: the startup sweep enumerates registry.list() — the LRU key set of OPEN handles, capped at 64, on a ~99-palace host. ≥35 never probed, then logs "all coverage verified". The repo ALREADY fixed this shape at service/helpers.rs:244 (#4637): "would silently drop ~98.9% of the corpus".
4. #5013: engineer fixed unalias's filter_map(...ok()); alias_audit ONE LAYER UP still has it. Critic built a real collision — is_clean()=true, repair "clean", is_success=true, freed_ids=[], collision untouched.
5. #5065: forcing stage=semantic on a skip_vector index returns HTTP 200 WITH BM25 RESULTS. Not empty, not an error. Precedent for the fix already exists in-crate: search_kg returns 503 kg_unavailable for skip_kg.

🔴 MEMORY-RETIREMENT DIAGNOSIS STILL UNCONFIRMED. I told Bob twice it is aliasing. The #5013 critic: `missing: 0` over unretrievable drawers is EQUALLY CONSISTENT with #852 degenerate embeddings, and cold_restart_recalls_beyond_l1_snapshot fails in that crate with every hit at ~0.3257 — the degenerate signature. #5013's e2e uses MockEmbedder, proving plumbing not retrievability.

THREE VACUOUS-GREEN PATTERNS FOUND, each able to make a gate report a lie:
- `bash script > file 2>&1; echo "EXIT=$?"` reports the ECHO's status. Caught check_test_pointers.sh failing with 2 dangling while the harness read EXIT=0.
- `cargo test --lib memory_core` WITHOUT --features memory-core: "0 passed; 294 filtered out".
- A non-blocking-spawn test asserting timing on the PUBLIC entry point could not catch a `.join()`, because the daemon write is refused instantly under the harness (#4255) so it finishes in microseconds either way.

trusty-analyze's neural embedder: NOTHING uses method=neural (console omits the param, gets bow). But NeuralEmbedder::new() runs UNCONDITIONALLY at every boot when ORT is compiled in — every shipped asset — and makes an UNTIMED hf-hub network call even when cached. Observed 31m46s stall on 2026-08-04 gating daemon startup; four other boots <1s. This CORRECTS the recorded "32-min boot is normal ONNX load" note — it is neither normal nor the ONNX load. #5067 to remove.

## Completed
- 7-8 PRs merged: #5033 #5039 #5046 #5041 #5035 #5051, plus #5055 and #5054 in-flight at pause
- tm 1.3.5 signed-installed and behaviorally verified; hook suppression confirmed by before/after on a real tm sessions start. THE CONFIG KEY IS READ BY THE DAEMON, NOT THE CLI — a bounce is mandatory or the fix looks failed
- #5062: PM instruction restructure 52,921 → 33,974 chars (~20.4k → ~13.1k tokens, -7,290). BEATS #4595's own posted 11-13k ceiling. Framework floor kept resident, only elaboration moved
- #5065: index-on-worktree-creation, always-async. Measured cold BM25+KG-only build of this workspace at 35.0s (5,865 files → 75,282 chunks). skip_vector already existed (#2984) so NO trusty-search change was needed
- #5057: JSON-RPC INDEX_NOT_READY contract, 425-equivalent not 404, carrying the grep/find fallback. The discriminator lives at the MCP layer because tools/list is the only place that knows which index it advertised
- ~20 issues filed: #5036-#5038, #5042-#5045, #5049-#5053, #5056, #5058-#5061, #5063, #5064, #5066, #5067
- MCP audit complete: memory_recall given NONSENSE scores 0.505 while an EXACT token in the palace scores 0.401 and never surfaces its drawer. kg_query is the in-house counter-example — returns a hint, not a bare []

## In Progress
- 🔴 VERIFY ALL DIRECTLY — ~12 agents were live at pause
- PR #5048 BM25 — 2nd critic round WARN. Prior 5 findings verified fixed by reproduced mutation. TWO NEW HIGHs (registry.list() enumeration cap; bm25_repair using registry.get without hydrating). Fix round live in .worktrees/bm25-backfill
- PR #5013 HNSW — critic WARN, 1 HIGH: alias_audit's filter_map one layer above the fixed unalias. Fix round live in .worktrees/hnsw-alias
- PR #5054 SCIP persistence — critic APPROVE after a WARN round. Merge agent dispatched at pause; VERIFY it landed. Extracted a SHARED redb open policy rather than mirroring FactStore's — 7th impl workspace-wide, #5063 tracks consolidation
- PR #5055 semver gate — critic APPROVE (verified by downloading real crates.io tarballs AND mutating the self-test 3 ways). Merge dispatched; VERIFY
- PR #5057 index-not-ready — critic WARN, 1 HIGH promoted to #5061 (index_status_handler lacks cold-store fallback). Fix round live in .worktrees/index-not-ready; told NOT to route list_chunks blindly since it shares the same shape
- PR #5062 instruction restructure — critic reviewing. THE RISK: package tests caught 6 dropped phrasings but 3 were lost purely to LINE RE-WRAPPING, so goldens are formatting-sensitive and a rule with NO assertion could vanish silently
- fix/mcp-provisioning-scope — #4181 P1 (machine-absolute paths into a git-tracked .mcp.json in an ephemeral worktree) + #5058. Told to keep it minimal since consolidation changes its shape
- trusty-mcp epic #5066 scoped, NOT started

## Next Steps
- FIRST: `gh pr list --state open` and `git worktree list`. Several agents may have completed, died, or pushed after this snapshot.
- Merge queue, each gated on its OWN critic round: #5055/#5054 (verify landed), #5062, #5065, #5057, #5013, #5048.
- 🔴 BOB MUST DO: add 'Public API / SemVer' to branch-protection required checks — admin rights. Confirmed ABSENT: required = Format check, 500-line file-size cap, Clippy, Test, trusty-search daemon smoke test, MSRV check (1.94). Until then the gate reports and cannot block. It matters concretely NOW: trusty-analyze owes a 0.9.0 bump from #5054 and trusty-search a 0.43.0 from #5057, both routed to local-ops.
- 🔴 BOB MUST DO: confirm what code intelligence actually calls before trusty-review's webhook is removed.
- Memory retirement stays BLOCKED and the DIAGNOSIS is unconfirmed. The sound gate is the self-query sweep (each drawer queried by its own first 400 chars, top_k 25), NOT still_missing_ids. Distinguish aliasing from #852 degenerate embeddings with a read-only palace_unalias dry run, reading vector_key_rows/distinct_vector_ids off palace_reembed — NOT alias_audit's is_clean(), which is the HIGH on #5013.
- Under-50k needs the FULL stack: #5062 (-7.3k, in review) + MEMORY.md retirement (~7k, blocked) + skills filtering (~3k) + output-style trim (~2k). Any one missing lands ~55-60k.
- OPEN DESIGN GAPS from #5065, all filed: the tm checkout has NO index though the embeddings-live-on-base model presumes one; allowlist::check_path has ZERO production call sites so the 06-05 opt-in model is unenforced; ad-hoc `git worktree add` is observed by no code path.

## Git Context
Branch: main
Last commit: 8ab2c147a docs: evaluate migrating memory_core storage from redb to Lance
Uncommitted changes: 1 file(s) changed

## Tmux Window
tm-dogfood:0:@5173
