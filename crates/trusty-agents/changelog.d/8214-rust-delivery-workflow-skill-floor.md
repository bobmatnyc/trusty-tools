Fixed

- The newly bundled `rust-delivery-workflow` skill (#8196) is now classified as engineer-only, off the assistant skill floor, so `every_bundled_skill_is_classified` no longer fails every `cargo test -p trusty-agents` run (#8214).
