Changed

- The REPL TUI now builds against ratatui 0.30 and crossterm 0.29, up from 0.29 and 0.28, dropping the vulnerable transitive `lru 0.12.5` from the graph (#2872). Both pins moved from local versions to `{ workspace = true }`, so this crate can no longer drift from the other TUI crates. No `tagent` source changed — ratatui 0.30's facade re-exports the widget, layout, style, and text types this crate names.
