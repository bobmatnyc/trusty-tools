Fixed

- Apply each index's current subtree, extension, exclusion, and ignore rules to filesystem watcher updates and dropped-event reconciliation, so excluded metadata stays out of search after configuration changes.
