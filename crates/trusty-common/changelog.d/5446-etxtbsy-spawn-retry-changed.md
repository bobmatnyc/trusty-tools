Changed
- The embedder sidecar spawn (`embedder_client::spawn_retry::spawn_embedderd`) now spawns through the shared policy instead of its own copy ([#5446](https://github.com/bobmatnyc/trusty-tools/issues/5446))
