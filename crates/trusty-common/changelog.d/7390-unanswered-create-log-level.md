Fixed

- A `search.index.create` the trusty-search daemon has not answered within the one-second client budget now logs at INFO instead of WARN, and the line says the client is checking the registry for a late registration rather than reporting the call as having gone unanswered. That line fires before the #7237 confirm loop has run, so a session whose index the daemon registered a few seconds late — a cold-parked corpus reloading inside the create handler, which is the case the confirm loop was built for — ended up correctly pinned while its log claimed a failed registration. The confirm loop's own two outcome lines are unchanged and still carry the verdict: INFO when it pins the index the daemon registered, WARN when it exhausts its four-second deadline and withholds the id. The create's one-second budget is unchanged (#7390)
