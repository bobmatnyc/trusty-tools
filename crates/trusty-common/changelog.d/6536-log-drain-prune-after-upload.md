Added

- `log_drain` gains destination-side pruning (#6536, Phase 4 of #6533): `LogDestination::delete` (implemented for `ObjectStoreDestination`, idempotent — deleting an already-absent key is `Ok(())`), `DrainManifest::prunable`/`remove_entry`, and the two entry points `prunable_candidates` (read-only: manifest-confirmed object keys at least a given age) and `prune_confirmed` (deletes each surviving key and drops its manifest entry in the same save). Nothing here decides retention or debounces the delete — that stays the caller's, per the module's existing "the core does not schedule" stance.
