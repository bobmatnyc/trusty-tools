Changed

- Sessions tab: the `stopped`, `errored`, `decommissioned` and `deleted` groups now start collapsed behind a counted header, and each group's expand/collapse choice is remembered per browser. A collapsed group still shows its name and row count, so a folded `errored` group still reports that sessions failed (#8282).
