Fixed
- Fourteen more tools answer a bare JSON-RPC method name instead of `Method not found (-32601)`: `kg_list_subjects`, `kg_retract_triple`, `palace_update`, `palace_dream`, `dream_consolidate_room`, the room surface (`room_list`, `room_create`, `room_rename`), the wing surface (`wing_list`, `wing_create`, `wing_rename`), and the task surface (`task_add`, `task_list`, `task_complete`). All were registered in `tools/list` and reachable through `tools/call`; the `TOOL_METHODS` allowlist was written by #149 and only ever extended by targeted fixes, so every tool added afterwards was absent for no reason. `chat_*` stays off the raw path — a contract test pins it to the `tools/call` envelope — and `palace_delete` and `upgrade` are held there pending an owner decision, each with the open question written beside it (#6397).
