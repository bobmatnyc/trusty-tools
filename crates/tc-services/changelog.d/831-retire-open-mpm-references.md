Fixed

- Updated doc comments that still named `open-mpm` as a consumer of the
  host-agnostic service adapters (`cto_db`, `gworkspace`, and the crate-level
  overview) to say `trusty-agents` (renamed in #831). Doc comments only — no
  behavior change.
