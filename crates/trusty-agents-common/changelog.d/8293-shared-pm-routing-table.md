Added

- `pm_routing`: the one agent-routing table trusty-mpm's PM instructions and
  trusty-code's delegate-mode PM card are both rendered from. `ROUTING_ROWS`
  holds one row per class of work with a per-consumer route; `fill` substitutes
  the rendered table and pipeline chain into each product's authored template
  (#8293).
