Fixed
- A dispatch the ADR-0048 worktree grant denies in a main checkout no longer leaves a running delegation record, so later dispatches there are no longer refused on agents that never ran (#7487).
