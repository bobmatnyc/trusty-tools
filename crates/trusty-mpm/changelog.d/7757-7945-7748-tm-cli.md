Fixed

- `tm doctor`'s `session_scope` row now prints `tm project trust --dir <path>`, the form the CLI parses, instead of the positional form it rejects ([#7757](https://github.com/bobmatnyc/trusty-tools/issues/7757))
- `tm pr merge` re-reads the pull request when `gh pr merge` exits non-zero: a squash that GitHub already landed exits 0 with the local-branch cleanup reported as a warning naming the worktree, rather than exit 2 on a merge that succeeded ([#7945](https://github.com/bobmatnyc/trusty-tools/issues/7945))
- `tm pr open` verifies `origin/<base>` against `git ls-remote` and fetches before any three-dot diff, refusing with both shas when the base stays stale or cannot be verified; the pre-push credential scan carries the same rule ([#7748](https://github.com/bobmatnyc/trusty-tools/issues/7748))
