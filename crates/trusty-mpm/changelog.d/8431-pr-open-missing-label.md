Fixed
- `tm pr open` no longer fails in a repository that has no `trusty-mpm` label. When `gh pr create` refuses with `could not add label: 'trusty-mpm' not found`, `tm pr open` creates the label (without `--force`) and retries; when the label genuinely cannot be created, it opens the PR without that label and prints a warning. A missing `ws/<session>` label, or any other `gh pr create` failure, still fails the command as before (#8431).
