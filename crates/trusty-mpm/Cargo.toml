[package]
name = "trusty-mpm"
# 1.5.6 (#4165): patch — 1.5.5 is already published on crates.io and this PR
# edits `src/**`, so the `PR version bump vs crates.io` gate (#4421) requires
# the bump here. Patch because the change is a bug fix inside
# `provisioner::workspace::provision_in`; no public item changed signature.
# #5044 rides the same 1.5.6: it adds `ImportOptions::refresh`, two
# `ImportStatus` variants and `core::memory_import::refresh`, and the crate
# has no out-of-workspace consumer to break.
# #5949 rides the same 1.5.6: the worktree registry repair it adds to
# `session_manager::decommission` is `pub(super)`, so no public item changed
# signature.
# 1.5.8 (#6288): patch — 1.5.7 is published and this PR edits `src/**`. Patch
# because the removed items (`supervisor::http`, `SupervisorConfig::metrics_addr`)
# have no out-of-workspace consumer, and every in-workspace caller moved with them.
# 1.5.11 (#6391): patch — 1.5.10 is published and this PR edits `src/**` to
# harden worktree removal and unwedge the reap wait; no public item changed
# signature.
# 1.5.16 (issue #4421, post-publish wave 2026-09-01): patch — 1.5.15 is
# already live on crates.io and several queued PRs (#6562, #6576, #6578,
# #6579) edit `src/**` under it. Bumped on `main` first so the
# `PR version bump vs crates.io` gate clears once each PR updates its branch
# onto this commit.
# 1.5.17 (refs #6671, #6663, #6667, #6661): patch — test-flake stabilization
# changes in the test suite under an already-published version.
# 1.5.18 (refs #6892): patch — 1.5.17 is published and this PR edits `src/**`.
# The edits are doc comments only, so no public item changed signature.
# 1.5.20 (owner-authorized reinstall, refs #7209 #7198 #7180 #7185 #7196
# #7172): patch, version-only — 1.5.19 is already published and this bump
# exists solely so `tm --version` identifies the reinstalled build.
# 1.5.21 (owner-authorized reinstall, refs #7244 #7237 #7262 #7274): patch,
# version-only — 1.5.20 is already published and this bump exists solely so
# `tm --version` identifies the reinstalled build.
# 1.5.22 (owner-authorized reinstall, refs #7295 #7294 #7296): patch,
# version-only — 1.5.21 is already published and this bump exists solely so
# `tm --version` identifies the reinstalled build.
# 1.5.23 (owner-authorized reinstall, refs #7311 #7314): patch,
# version-only — 1.5.22 is already published and this bump exists solely so
# `tm --version` identifies the reinstalled build (rtk pipe-mode fix).
# 1.5.24 (owner-authorized reinstall, refs #7311 #7313 #7312 #7278 #7282
# #7275 #7237 #7325 #7319): patch, version-only — 1.5.23 is already
# published and this bump exists solely so `tm --version` identifies the
# reinstalled build (statusline savings percentage format).
# 1.5.28 (owner-authorized reinstall, refs #7418): patch, version-only —
# 1.5.27 is already published and this bump exists solely so `tm --version`
# identifies the reinstalled build.
# 1.5.29 (owner-authorized reinstall, 2026-09-11): patch, version-only —
# 1.5.28 is already published and this bump exists solely so `tm --version`
# identifies the reinstalled build.
# 1.5.30 (owner-authorized reinstall, refs #7422 #7471 #7399 #7424): patch,
# version-only — 1.5.29 is already published and this bump exists solely so
# `tm --version` identifies the reinstalled build.
# 1.5.31 (owner-authorized reinstall, refs #7488): patch, version-only —
# 1.5.30 is already published and this bump exists solely so `tm --version`
# identifies the reinstalled build.
# 1.5.32 (owner-authorized reinstall, refs #7490): patch, version-only —
# 1.5.31 is already published and this bump exists solely so `tm --version`
# identifies the reinstalled build.
# 1.5.33 (owner-authorized reinstall, refs #7461 #7464 #7491 #7497 #7498
# #7479 #7499 #7185 #7487 #7518 #7504): patch, version-only — 1.5.32 is
# already published and this bump exists solely so `tm --version` identifies
# the reinstalled build.
# 1.5.34 (#7514): patch — 1.5.33 is already published on crates.io and this PR
# edits `src/**`, so the `PR version bump vs crates.io` gate (#4421) requires
# the bump here. Patch because the change is a bug fix inside the existing
# savings producer and compiled-prompt writer.
# 1.5.36 (Refs #7616 #7617 #7658): patch, version-only — owner-authorized
# reinstall so `tm --version` identifies the build carrying the fold/doctor,
# statusline core-setup, and ledger dedupe fixes merged since 1.5.35.
# 1.5.37 (Refs #7751): patch — trusty-mpm is excluded from the semver gate
# (binary-only consumer surface, no library dependents), so the feat commit
# in this PR does not require a minor bump; required by the "PR version bump
# vs crates.io" check since 1.5.36 is already published.
# 1.5.38 (owner-authorized reinstall, 2026-09-15): patch, version-only — 1.5.37
# is the in-tree tip (crates.io is still at 1.5.36) and this bump exists solely
# so `tm --version` identifies the locally reinstalled build. Not published.
# 1.6.1 (#8192): patch — 1.6.0 is already published on crates.io and this PR
# bundles the rust-delivery-workflow skill and wires it to rust-engineer,
# touching src/**, so the "PR version bump vs crates.io" check (#4421)
# requires the bump. Patch: additive skill bundling, no breaking change.
# Also #8135/#8156 (this PR): same 1.6.1 patch bump applies here too, since
# this PR also edits src/** (tm-delegation-patterns.md skill asset) after
# 1.6.0's crates.io publish.
# 1.6.3 (milestone #89, refs #8274): patch — the release cut. crates.io is at
# 1.6.0; 1.6.1 and 1.6.2 were bumped in-tree but never tagged or published, so
# this release skips past both rather than reusing a number a reader might
# expect to find on crates.io. Patch: trusty-mpm is excluded from the semver
# gate (binary-only consumer surface, no library dependents), and the changes
# folded in here are fixes plus additive skill/agent assets.
# 1.7.1 (milestone #92, Refs #5850): patch — 1.7.0 is already published on
# crates.io and this PR edits `src/**` (gh-account registry lookups), so the
# "PR version bump vs crates.io" check (#4421) requires the bump. Patch:
# trusty-mpm is excluded from the semver gate (binary-only consumer surface,
# no library dependents), and the change is a bug fix with no signature break.
# 1.7.2 (Refs #4421): patch — 1.7.1 is already published on crates.io (at
# 4c2f99bff) and main has since gained further src/** changes (batch B #8487,
# batch G #8481, #8465, #8488), so the "PR version bump vs crates.io" check
# requires the bump. Patch: trusty-mpm is excluded from the semver gate
# (binary-only consumer surface, no library dependents); changes are fixes
# plus docs/changelog assembly, no signature break.
# 1.7.3 (Refs #8439): patch — 1.7.2 is already published on crates.io (at
# cfb3cf0f6) and this PR edits `src/**` (`tm hook --pm-guard` read-only
# dispatch allowlist), so the "PR version bump vs crates.io" check requires
# the bump. Patch: trusty-mpm is excluded from the semver gate (binary-only
# consumer surface, no library dependents); the change is a security-hardening
# fix with no signature break.
# 1.7.4 (Refs #8563): patch — 1.7.3 is already published on crates.io and
# this PR edits `src/**` (scoped launchd secrets fix), so the "PR version
# bump vs crates.io" check requires the bump. Patch: trusty-mpm is excluded
# from the semver gate (binary-only consumer surface, no library
# dependents); the change is a security-hardening fix with no signature
# break. Ships alone per owner ruling.
# 1.7.5 (Refs #8482): patch — 1.7.4 was published to crates.io at
# 2026-09-24T22:37:43Z and this PR edits `src/**` (tm doctor bundled-asset-lag
# detection), so the "PR version bump vs crates.io" check requires the bump.
# Patch: trusty-mpm is excluded from the semver gate (binary-only consumer
# surface, no library dependents); the change is a new doctor check with no
# signature break.
# 1.7.6 (Refs #8638): patch — 1.7.5 is published on crates.io and this PR
# edits `src/**` (`tm pr queue-check`/`tm wait` rollup: per-GraphQL-type
# deciding-run selection, an allow-list `failed()` check, and an
# `is_unfinished = !settled()` fix), so the "PR version bump vs crates.io"
# check requires the bump. Patch: trusty-mpm is excluded from the semver
# gate (binary-only consumer surface, no library dependents); the change is
# a bug fix with no signature break.
version = "1.7.7"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "trusty-mpm: unified multi-agent orchestration platform (core, daemon, CLI, TUI, Telegram)"
readme = "README.md"
homepage = "https://trustytools.dev"
keywords = ["orchestration", "multi-agent", "cli", "daemon", "mcp"]
categories = ["command-line-utilities", "development-tools"]
exclude = ["/.claude/", "/.trusty-mpm/", "/CLAUDE.md"]

# ── Feature flags ─────────────────────────────────────────────────────────────
#
# Default features build the unified `tm` / `trusty-mpm` binary with daemon,
# TUI, and Telegram support baked in. Users who only want the libraries can
# opt out with `--no-default-features`.
#
# Each optional library module is gated behind a matching feature flag so
# users only pay the compile cost for the components they actually need.
[features]
default = ["cli", "daemon"]

# `cli` gates the `tm` / `trusty-mpm` binary (embeds daemon + tui + telegram + slack).
cli = ["daemon", "tui", "telegram", "slack"]

# `daemon` gates the daemon library module and the `daemon` subcommand.
# Activating `daemon` also pulls in `mcp` (the daemon implements OrchestratorBackend).
daemon = ["mcp",
          "dep:axum", "dep:tower", "dep:tower-http",
          "dep:tokio-stream", "dep:futures",
          "dep:dashmap", "dep:parking_lot",
          "dep:notify", "dep:tracing-appender",
          "dep:tokio-util"]

# `swagger-ui` adds utoipa-swagger-ui to the daemon HTTP server so the
# built-in API explorer is reachable at `/api-docs`. NOT included in
# `daemon` by default so offline or air-gapped builds (which cannot pull
# the swagger-ui static assets) can compile with `--no-default-features
# --features daemon`. Enable with `--features swagger-ui` when you want
# the in-process explorer (closes #1694).
swagger-ui = ["dep:utoipa-swagger-ui"]

# `mcp` gates the MCP server library module.
# (`async-trait`, used by the OrchestratorBackend trait, is now an always-on
# dependency because the SM providers' `LlmProvider` trait needs it too — SM-2.)
mcp = []

# `tui` gates the TUI library module (`tm tui`).
tui = ["dep:ratatui", "dep:crossterm"]

# `telegram` gates the Telegram bot library module (`tm telegram`).
telegram = ["dep:teloxide", "dep:tokio-util"]

# `slack` gates the Slack bot library module (`tm slack`, DOC-20 adapter #1294).
# Pulls the WebSocket client used for Slack Socket Mode plus the streaming
# futures combinators it needs; kept opt-in (in `cli`'s default set, mirroring
# `telegram`) so a `--no-default-features` build pays no Slack cost.
slack = ["dep:tokio-tungstenite", "dep:futures-util"]

# `bedrock` gates the Session Manager's AWS Bedrock provider (SM-2, DOC-14 §5.1).
# NOT in `default`: it pulls the heavy `aws-sdk-bedrockruntime` stack, so the
# Bedrock provider weight is strictly opt-in. Build/test with `--features bedrock`.
#
# As of #2411 the SM Bedrock provider is a thin bridge onto the shared
# `trusty_common::inference::bedrock` Converse adapter (#2407) instead of a
# private aws-sdk port — so this feature now just turns on the commons
# `bedrock-client` feature, which pulls the AWS SDK stack in ONE place. The
# direct `aws-config`/`aws-sdk-bedrockruntime`/`aws-types` deps were dropped here.
bedrock = ["trusty-common/bedrock-client"]

# `sm-memory` gates the Session Manager's dedicated memory palace (SM-4, DOC-14
# §8). NOT in `default`: it turns on `trusty-common/memory-core`, which pulls the
# heavy Memory Palace storage engine (usearch/hnsw_rs HNSW vector index, redb +
# postcard stores, git2, and the bundled-ORT FastEmbedder). Keeping it opt-in
# means a `--no-default-features` build — and even the default daemon build —
# does NOT pay the ONNX/usearch compile or runtime cost unless the operator
# explicitly opts the SM memory subsystem in. Build/test with `--features
# sm-memory`. The `core::sm::memory` module is compiled only under this flag.
#
# Production hygiene (SM-4 review): this feature deliberately does NOT enable
# `trusty-common/embedder-test-support`. That test double (and its
# `seed_shared_embedder_with_mock` seam) must never link into a production
# `--features sm-memory` build. The SM-4 unit tests still need the mock
# embedder to run without the ONNX model, so the test-support feature is wired
# in via `[dev-dependencies]` below — Cargo unifies dev-dependency features only
# for test/bench/example builds, so `cargo test` gets the mock while a plain
# `cargo build --features sm-memory` does not.
sm-memory = ["trusty-common/memory-core"]

# `manager-memory` gates the Layer-3 `tm manager` PORTFOLIO memory palace (WI-5,
# #2582, DOC-36 §3.4) — a single portfolio-scoped `trusty-memory` palace holding
# digest history, escalation dispositions, and portfolio chat turns, distinct
# from the SM palace and every per-project palace. Like `sm-memory` it is NOT in
# `default`: it turns on `trusty-common/memory-core` (the heavy HNSW/ONNX Memory
# Palace engine), so a default daemon build pays no ONNX/usearch cost. Without
# this feature the portfolio palace reports "unavailable" and the manager surface
# still works (DOC-36 §4 degrade bar). The `daemon::manager::memory` live binding
# is compiled only under this flag. Build/test with `--features manager-memory`;
# the `embedder-test-support` dev-dependency below supplies the mock embedder so
# `cargo test --features manager-memory` runs without the ONNX model.
manager-memory = ["trusty-common/memory-core"]

# `gui` is intentionally a no-dep CLI-gating feature. The `tm gui` subcommand
# is ALWAYS available; this flag exists only so the subcommand can optionally
# advertise itself. The Tauri GUI lives in the separate, publish=false
# `trusty-mpm-gui` crate (it owns `build.rs` + `tauri.conf.json` which cannot be
# published cleanly to crates.io). Rather than pull that unpublishable crate in
# as an optional Cargo dependency — which blocks `cargo publish` for trusty-mpm,
# because cargo requires even optional deps to resolve on crates.io — `tm gui`
# shells out to a separately-installed `trusty-mpm-gui` binary (Single-Install
# convention: `cargo install trusty-mpm-gui`). See src/bin/tm/main.rs.
gui = []

# ── Library ────────────────────────────────────────────────────────────────────
[lib]
name = "trusty_mpm"
path = "src/lib.rs"

# ── Binaries ───────────────────────────────────────────────────────────────────

[[bin]]
name = "tm"
path = "src/bin/tm/main.rs"
required-features = ["cli"]

[[bin]]
name = "trusty-mpm"
path = "src/bin/tm/main.rs"
required-features = ["cli"]

# Why (issue #1318): the `trusty-console` `[[bin]]` shim was REMOVED here.
# Single-owner-per-binary — the standalone `trusty-console` crate is now the
# sole producer of the `trusty-console` binary, so `cargo install trusty-mpm`
# no longer collides on `.crates2.json` binary ownership (#1262). Install the
# console separately (`cargo install trusty-console`) or via `tctl` (#1316).

# ── Integration tests ─────────────────────────────────────────────────────────
[[test]]
name = "e2e"
path = "tests/e2e/main.rs"
required-features = ["daemon"]

# Services discovery integration test (gated #[ignore]; run with --include-ignored).
[[test]]
name = "services_integration"
path = "tests/services_integration.rs"

# WI-7: isolation-invariant integration tests for the standalone managed driver.
# No special features required — uses only the always-on `core::standalone` module.
[[test]]
name = "standalone_isolation"
path = "tests/standalone_isolation.rs"

# ── Always-on dependencies ─────────────────────────────────────────────────────
# Required by the `core` and `client` library modules (compiled unconditionally).
[dependencies]
trusty-agents-common = { workspace = true }
# JSON-RPC / MCP primitives, extracted from `trusty_common::mcp` by ADR-0040 (#5803).
# `memory_rpc` did NOT move — it arrives through `trusty-common/catchup` below.
trusty-mcp = { workspace = true }
trusty-common = { workspace = true, features = [
    "cli-help",
    # #5811: the single palace-resolution entry point. Session launch exports
    # TRUSTY_MEMORY_PALACE, which outranks the committed pin — so it must be
    # resolved through the same code that reads that pin.
    "palace-resolve",
    # #5475: the workspace's single `gh` entry point (`trusty_common::gh`),
    # which core::gh_account / gh_account_enforce / workstream_label and
    # session_manager::worktree_reclaim all route through.
    "gh-cli",
    "bug-capture",
    "crate-config",
    "console-metrics",
    # #7497: `host_metrics::mount_for_path` — the usage of the mount a path sits
    # on, which `core::disk_usage_guard` reads before any new worktree is
    # created. Adds no crate to the lockfile: `sysinfo` is already an
    # unconditional dependency here, and this only turns on its disk/network
    # features.
    "host-metrics",
    # #8261: `load_average::read_load_average` — the 1-minute load average the
    # builder capacity formula reads. `host-metrics` above cannot answer it;
    # its CPU figure is an instantaneous sample, not a sustained one.
    "load-average",
    "intent-source",
    # DOC-28 catch-up engine (PR1, #1762): moves the catch-up module out of
    # trusty-mpm/src/core/ into trusty-common so trusty-code can share it.
    "catchup",
    # SPEC-ONESM-01 / DOC-33: canonical tmux-session naming was hoisted into
    # trusty-common so trusty-agents can reuse it. `core::names` re-exports it.
    "session-naming",
    # #2486: reuses `trusty_common::update::is_launchd_supervised` for the
    # daemon-restart-race supervision signal (see daemon_run.rs / api/types.rs).
    # Zero extra transitive deps — see the feature's Cargo.toml comment.
    "update-check",
    # #2405: the universal `tm config keys …` inference-provider credential CLI.
    "config-cli",
    # #2580/#2581 (DOC-36 §3.3): the Layer-3 `tm manager` digest/chat handlers
    # resolve providers and issue chat calls through the unified
    # `trusty_common::inference` adapter/Configurator. `config-cli` already implies
    # `inference-client`; listing it explicitly documents the direct dependency so
    # a future config-cli change can't silently drop the manager's inference core.
    "inference-client",
    # Shared best-effort trusty-search index register+reindex helper
    # (`search_index::register_project_index` → `trusty_common::search_index`).
    # Promoted out of trusty-mpm so trusty-code can reuse the ONE implementation.
    "search-index",
    # #3304: the shared router-wide same-origin write guard
    # (`trusty_common::server::guard_write_origin` / `SelfOrigins`) applied to the
    # daemon router in `daemon/api.rs`. Already enabled as a dev-dependency below;
    # promoted to the main dep so the guard compiles into the production daemon.
    "axum-server",
    # #6288 (ADR-0032): the daemon's own hardened Unix-socket listener
    # (`daemon::socket`) — `bind_singleton_hardened` plus `uds::server`'s
    # `RpcRouter` / `serve_until`. Reached transitively today through
    # `catchup` → `memory-rpc` → `uds`; listed explicitly so a future change to
    # the catch-up engine's deps cannot silently remove the daemon's transport.
    "uds",
    # #6535: the cloud log drain core (`trusty_common::log_drain`) — the
    # destination adapters, URI parser, manifest, and `run_once`. The daemon's
    # `log_drain_loop` scheduler and the `log_drain` doctor row are the only
    # consumers. Unconditional rather than feature-gated here so the scheduler
    # compiles into every daemon build; the drain itself stays OFF unless
    # `log_drain.enabled` is set in config.
    "log-drain",
    # #6892: `trusty_common::machine_tier::MemoryTier` sizes the default
    # machine-wide builder cap (`core::builders`). Pure code with no extra
    # transitive deps — the feature is `machine-tier = []` — and it is the same
    # sizing input trusty-search and trusty-memory already read, so the three
    # daemons agree about the machine they share.
    "machine-tier",
] }
# `bug-capture` enables Phase 1 ERROR event capture to JSONL + in-memory ring (bug-reporting #478)
# `console-metrics` enables `trusty_common::console_metrics` (used unconditionally by
# `daemon/mcp_console.rs`). Without it, `cargo check -p trusty-mpm` and `cargo publish`
# fail to resolve the module — workspace feature-unification masks this under `cargo test`.
# Why (issue #1318): the `trusty-console` dependency was REMOVED. The console is
# no longer bundled into trusty-mpm (single-owner-per-binary); the standalone
# `trusty-console` crate is its sole producer.
serde.workspace = true
serde_json.workspace = true
serde_yaml.workspace = true
toml.workspace = true
anyhow.workspace = true
thiserror.workspace = true
tracing.workspace = true
uuid.workspace = true
chrono.workspace = true
gray_matter.workspace = true
walkdir.workspace = true
dirs.workspace = true
# #4964: `core::binary_provenance` must ORDER two crate versions, not merely
# compare them for equality — which of the running binary and cargo's ledger
# record is newer decides whether a mismatch is a stale ledger or a real
# downgrade. Use the canonical parser rather than a second hand-rolled one.
semver.workspace = true
# Used by statusline/compaction.rs for atomic state-file writes (NamedTempFile::persist).
tempfile.workspace = true
sha2.workspace = true
regex.workspace = true
# Glob compilation for the `tm ls` picker's `d <glob>` bulk delete (#5539).
# Already a workspace dep (trusty-code) and already in the lockfile, so this
# adds no new dependency to resolve.
globset.workspace = true
# #2734: quote-aware shell word-splitting for the PM Bash guard's git-argv
# parsing (`src/bin/tm/commands/pm_guard_bash/shell_lex.rs`).
shlex.workspace = true
jsonwebtoken.workspace = true
utoipa.workspace = true
reqwest = { workspace = true, features = ["blocking"] }
tokio.workspace = true
# #2602: bounds the manager ChatStore's distinct conversation-key set with a
# capacity-evicting LRU cache — see daemon/manager/chat_store.rs.
lru.workspace = true
# async-trait is needed by the SM providers' `LlmProvider` trait (SM-2), which is
# compiled unconditionally in the `core` library. (The `mcp` feature also lists it
# below for the OrchestratorBackend trait; declaring it here makes it always-on.)
async-trait.workspace = true

# Always-on CLI/binary support deps.
# These are pulled in by the bin targets; having them always-on is acceptable
# because the library itself does not depend on them directly.
clap.workspace = true
tracing-subscriber.workspace = true
colored.workspace = true
sysinfo.workspace = true
libc.workspace = true
unicode-width.workspace = true
# Portable executable lookup on PATH (replaces the non-portable `which` shell-out
# in the tcode runtime adapter's availability probe — see #1213).
which.workspace = true
# Shared rustup/cargo-style spinner used around the blocking managed-spawn POST
# in the guided `tm` on-ramp so first-run clone/provisioning is no longer a
# silent multi-minute hang (issue #1904).
trusty-progress = { workspace = true }
# rusqlite: formerly a direct dep for the claude-mpm session-registry reader
# (DOC-28 cutover bridge, #1762). Now consumed transitively via
# trusty-common/catchup feature — removed from direct deps (PR1, refs #1762).

# ── Optional dependencies (feature-gated) ────────────────────────────────────

# bedrock feature (SM-2): the AWS SDK stack is no longer a direct dependency
# here. As of #2411 the SM Bedrock provider bridges onto
# `trusty_common::inference::bedrock` (the shared #2407 Converse adapter), so the
# `aws-config`/`aws-sdk-bedrockruntime`/`aws-types` crates are pulled transitively
# via `trusty-common/bedrock-client` (see the `bedrock` feature above) — the one
# place the AWS SDK weight lives now.

# daemon feature
axum = { workspace = true, optional = true }
tower = { workspace = true, optional = true }
tower-http = { workspace = true, optional = true }
tokio-stream = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
dashmap = { workspace = true, optional = true }
parking_lot = { workspace = true, optional = true }
notify = { workspace = true, optional = true }
tracing-appender = { workspace = true, optional = true }
utoipa-swagger-ui = { workspace = true, optional = true }

# tui feature
ratatui = { workspace = true, optional = true }
crossterm = { workspace = true, optional = true }

# telegram feature
teloxide = { workspace = true, optional = true }
tokio-util = { workspace = true, optional = true }

# slack feature (DOC-20 adapter #1294): Socket-Mode WebSocket client + stream
# combinators. Same pinned version/features the trusty-agents crate already uses,
# so no new Cargo.lock entries are introduced.
tokio-tungstenite = { workspace = true, optional = true }
futures-util = { workspace = true, optional = true }

# NOTE: trusty-mpm-gui is deliberately NOT a Cargo dependency. It is a
# publish=false Tauri crate that cannot resolve on crates.io, so declaring it
# (even as an optional dep) blocks `cargo publish` for trusty-mpm. The `tm gui`
# subcommand shells out to a separately-installed `trusty-mpm-gui` binary
# instead — see the `gui` feature comment above and src/bin/tm/main.rs.

# ── Platform-specific always-on deps ─────────────────────────────────────────
[target.'cfg(unix)'.dependencies]
# `user` (#5784): `getpwuid` for the current uid, the signal that tells a
# scratch `$HOME` from the operator's real one — see `core::host_state_gate`.
nix = { version = "0.29", features = ["signal", "user"] }

# ── Dev dependencies ───────────────────────────────────────────────────────────
[dev-dependencies]
# Re-declare trusty-common with `embedder-test-support` (SM-4 review): Cargo
# merges dev-dependency features ONLY for test/bench/example builds, so this
# turns on the mock-embedder seam (`seed_shared_embedder_with_mock`) for
# `cargo test --features sm-memory` WITHOUT leaking it into a production
# `cargo build --features sm-memory`. `memory-core` is repeated so the unified
# feature set still includes the palace storage engine the SM-4 tests exercise.
trusty-common = { workspace = true, features = [
    "memory-core",
    "embedder-test-support",
    # #2584 (DOC-36 §4): the hermetic manager CI suite drives the digest/chat
    # routes with `ScriptedAdapter` and, for real HTTP-client mechanics,
    # `test_support::MockInferenceServer` — the latter is gated behind
    # `axum-server`. `inference-client` is listed for clarity (the runtime dep
    # already enables it via `config-cli`); `axum-server` is the dev-only
    # addition that exposes the loopback mock server.
    "inference-client",
    "axum-server",
] }
# #8341: the `trusty-review` dev-dependency was REMOVED. It existed for the
# cross-gate AC-18 golden test (DOC-15 C5 / #1362), which now lives in
# `crates/trusty-crate-contracts/tests/conformance_cross_gate.rs` over NORMAL
# dependencies on both gates. It added zero cost to a production `cargo build`
# and eight crates (four AWS SDK crates, redb, hmac, arc-swap, trusty-review) to
# every `cargo test -p trusty-mpm` and `cargo clippy -p trusty-mpm
# --all-targets`, in every worktree — which is the cost that mattered.
# `scripts/check_dev_dep_edges.sh` refuses a new edge of this shape.
tempfile.workspace = true
axum.workspace = true
http-body-util.workspace = true
# serial_test: run env-mutating tests serially to prevent env-var races across
# parallel Rust test threads (used by bug-report token resolution tests).
serial_test.workspace = true
# test-util enables tokio's paused-clock (`tokio::time::pause` / `start_paused = true`)
# used by the deterministic auth-timeout tests in actor_tests.rs (#1649) and by
# `telegram::supervisor_tests::healthy_run_resets_transient_backoff` (#4306).
tokio = { workspace = true, features = ["test-util"] }
